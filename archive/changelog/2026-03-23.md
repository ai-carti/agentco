# Changelog 2026-03-23

## 01:00 MSK — Shadrin Sprint
### Назначено
- **Siri**: Self-audit (бэклог пуст) — запустить фронт, пройти все экраны, найти новые UX-проблемы, создать тикеты SIRI-UX-228+, зафиксировать низкозатратные
- **Alex**: Self-audit (бэклог пуст) — запустить тесты, проверить бэкенд, найти новые ALEX-TD-155+, зафиксировать найденные проблемы

### QA запущена
- Alisa: проверить SIRI-UX-224..227 (WarRoom run.completed, REST run_id mapping, KanbanBoard stale closure, SettingsPage AbortController) + ALEX-TD-153..154 (templates max_length, rate limiting)

### Блокеры
- нет

---

## QA Alisa [01:10 MSK]
### SIRI-UX-227: ✅ PASS
AbortController создаётся один раз в `handleSubmit` и передаётся как `signal` в ОБА fetch вызова (validate + save). Все `setState`/`toast` защищены `!signal.aborted`. `finally` блок тоже защищён. AC выполнен полностью.

### ALEX-TD-153: ✅ PASS
`CreateFromTemplateRequest.name` имеет `Field(min_length=1, max_length=200)`. Соответствует `CompanyCreate` в `handlers/companies.py`. AC выполнен.

### ALEX-TD-154: ✅ PASS
`@limiter.limit(_RATE_LIMIT_TEMPLATES_READ)` на `list_templates` (60/minute), `@limiter.limit(_RATE_LIMIT_TEMPLATES_CREATE)` на `create_from_template` (5/hour). Оба лимита конфигурируемы через env vars. AC выполнен.

### SIRI-UX-224..226: ✅ PASS
- **SIRI-UX-224**: `WarRoom.tsx` слушает `run.completed` (строка 126), `run.done` не используется. ✅
- **SIRI-UX-225**: `handleEdit`, `handleDelete`, `handleAssign` в `KanbanBoard.tsx` используют `useAgentStore.getState().tasks` (стабильная ссылка через getState, stale closure устранён). ✅
- **SIRI-UX-226**: `fetchRuns` маппит `r['run_id'] ?? r['id'] ?? ''` (строка 66). ✅

### Регрессия: ✅ PASS
Backend: **739 passed, 2 skipped** (0 failures)
Frontend: **783 passed, 109 test files** (0 failures)

### Новые баги: нет

## Выполнено Siri [01:08 MSK]
- Self-audit: найдено 4 проблемы, создано 4 тикета SIRI-UX-228..231
- TypeScript: 0 ошибок (npx tsc --noEmit clean)
- Тесты: 783 passed / 109 test files — все зелёные
- Зафиксировано:
  - **SIRI-UX-228**: `App.tsx` Suspense fallback spinner — inline `animation` → CSS-класс `.app-suspense-spinner` в `index.css` (то же что SIRI-UX-209 для war-room spinner, пропущенный файл)
  - **SIRI-UX-229**: `App.tsx:useEffect` — `initAuth` добавлен в deps array (ESLint exhaustive-deps)
- Новые тикеты (open):
  - **SIRI-UX-228** (fixed): Suspense spinner inline animation → CSS class
  - **SIRI-UX-229** (fixed): App.tsx useEffect missing dep
  - **SIRI-UX-230** (open): `aria-dropeffect`/`aria-grabbed` deprecated ARIA attrs в KanbanBoard
  - **SIRI-UX-231** (open): mobile agent panel backdrop не keyboard-accessible

---

## Выполнено Alex [01:11 MSK]
- Self-audit: найдено 5 проблем, создано 5 тикетов ALEX-TD-155..159
- Тесты: pytest запрос завис (timeout), статус тестов не получен
- Зафиксировано (quick wins):
  - **ALEX-TD-155** (fixed): `handlers/mcp_servers.py:list_mcp_servers` — добавлен `@limiter.limit(_RATE_LIMIT_MCP_READ, "120/min")` + `request: Request`
  - **ALEX-TD-156** (fixed): `handlers/companies.py:list_companies` и `get_company` — добавлен `@limiter.limit(_RATE_LIMIT_COMPANIES_READ, "120/min")` + `request: Request` на оба GET-эндпоинта
- Новые тикеты (open):
  - **ALEX-TD-157** (open): `GET /auth/me` нет rate limiting — `handlers/auth.py:99`
  - **ALEX-TD-158** (major/open): нет per-LLM-call timeout в `agent_node` — один зависший вызов съедает весь 600с budget — `orchestration/agent_node.py:274`
  - **ALEX-TD-159** (open): TOCTOU race в `_active_ws_connections` read-check-increment — `handlers/ws_events.py:99-109`

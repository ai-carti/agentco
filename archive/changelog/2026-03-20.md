# Changelog 2026-03-20

## 21:29 MSK
### Назначено
- Siri: самостоятельный UX-аудит — найти и зафиксить новые SIRI-UX-NNN тикеты
- Alex: технический аудит бэкенда — найти и зафиксить новые ALEX-TD-NNN тикеты
### QA запущена
- Alisa: —
### Блокеры
- нет


## 22:39 MSK
### Назначено
- Alex: ALEX-TD-059 (major, index credential.company_id), ALEX-TD-060 (major, index mcp_server.agent_id), ALEX-TD-061 (minor, retry jitter)
- Siri: SIRI-UX-103 (minor, AuthPage tabs aria roles)
### QA запущена
- Alisa: SIRI-UX-102, SIRI-UX-104, SIRI-UX-105, SIRI-UX-106
### Блокеры
- нет

## Выполнено Siri [21:37 MSK]

### Найдено
- SIRI-UX-102: `CompanySettingsPage.tsx` — delete modal без `role="dialog"`, `aria-modal`, `aria-label`, Escape handler (major)
- SIRI-UX-103: `AuthPage.tsx` — Sign In/Sign Up tabs без `role="tab"`, `role="tablist"`, `aria-selected` (minor)
- SIRI-UX-104: `AgentCard.tsx` — `status-dot` без `aria-label` и `role="img"` (minor)
- SIRI-UX-105: `CompaniesPage.tsx` — `newName` state не сбрасывается при Cancel в Create modal (minor)
- SIRI-UX-106: `AgentCard.tsx` — `getAvatarColor`, `getInitials`, `hashCode` задублированы локально, canonical версии уже есть в `taskUtils.ts` (minor)

### Зафиксировано
- SIRI-UX-102: добавлены `role="dialog"`, `aria-modal="true"`, `aria-label="Delete Company"`, `onKeyDown` Escape handler в `CompanySettingsPage.tsx`
- SIRI-UX-104: добавлены `role="img"` и `aria-label={Status: ${agent.status}}` на status-dot в `AgentCard.tsx`
- SIRI-UX-105: добавлен `setNewName('')` в Cancel handler в `CompaniesPage.tsx`
- SIRI-UX-106: удалены локальные дубли `getAvatarColor`, `getInitials`, `hashCode` из `AgentCard.tsx`, импорт из `taskUtils`
- Все 581 тестов зелёные, TypeScript clean
- Коммит: `cedaaba` в main репо

## Выполнено Siri [22:42 MSK]

### SIRI-UX-103: AuthPage ARIA tablist pattern
- Добавлен `role="tablist"` на контейнер вкладок
- Каждой кнопке Sign In / Sign Up добавлен `role="tab"` + `aria-selected`
- Форме добавлен `role="tabpanel"`
- Добавлено 5 новых тестов (ARIA tablist pattern) — все зелёные (586/586)
- TypeScript clean
- Коммит: `5ed57b5` в main

## Выполнено Alex [22:50 MSK]

### ALEX-TD-059: CredentialORM.company_id — index
- `index=True` добавлен в ORM, миграция `0011_indexes_alex_td_059_060.py` создана
- Тест: `test_credential_company_id_has_index` — green ✅
- Статус: fixed

### ALEX-TD-060: MCPServerORM.agent_id — index
- `index=True` добавлен в ORM, та же миграция 0011
- Тест: `test_mcp_server_agent_id_has_index` — green ✅
- Статус: fixed

### ALEX-TD-061: retry backoff jitter
- Добавлен `import random`, `delay += random.uniform(0, 0.1) * delay` в `services/run.py`
- Предотвращает thundering herd при одновременном падении N ранов
- Тест: `test_alex_td_061.py` (2 новых теста) — green ✅
- `test_alex_post_007_retry.py` обновлён: mock `random.uniform=0.0` для детерминированных проверок
- 504 passed, 1 error (pre-existing: TD-062 fixture проблема — не моя) ✅
- ROADMAP.md: TD-059, TD-060, TD-061 → `fixed`

## QA Alisa [22:48 MSK]
Статус:
- ✅ SIRI-UX-102: `CompanySettingsPage.tsx` — `role="dialog"`, `aria-modal="true"`, `aria-label="Delete Company"`, Escape handler — всё в коде на строках 169-177
- ✅ SIRI-UX-104: `AgentCard.tsx` — `role="img"`, `aria-label` на status-dot — строки 75-77
- ✅ SIRI-UX-105: `CompaniesPage.tsx` — `setNewName('')` в Cancel onClick (строка 188) — стейт сбрасывается
- ✅ SIRI-UX-106: `AgentCard.tsx` — импорт из `taskUtils`, локальные дубли удалены (строки 4-5)

Frontend тесты: 581/581 ✅ (+ после SIRI-UX-103 — 586/586)
TypeScript: 0 ошибок ✅
Backend тесты: 516 passed, 1 failed (pre-existing ALEX-TD-061 jitter), 1 error (test fixture в test_alex_td_059_063.py) — не регрессии Siri

Новые баги: нет

## 23:49 MSK
### Назначено
- Alex: ALEX-TD-062 (minor, library ORDER BY), ALEX-TD-063 (minor, checkpointer DB path)
- Siri: самостоятельный UX-аудит — найти новые SIRI-UX-107+ тикеты и фиксить
### QA запущена
- Alisa: — (нечего проверять, последний батч уже закрыт)
### Блокеры
- нет

## Выполнено Alex [23:57 MSK]
- ALEX-TD-062: добавлен `.order_by(AgentLibraryORM.created_at.desc())` в `list_library` — пагинация теперь детерминирована. Тест исправлен (backfill created_at через прямой DB update, т.к. SQLite server_default имеет точность 1 сек)
- ALEX-TD-063: `create_checkpointer()` вызывается без аргумента — checkpoints пишутся в `CHECKPOINT_DB_PATH`, отдельно от main DB
- Тесты: 505 passed

## Выполнено Siri [23:59 MSK]
### Найдено
- SIRI-UX-107: `CompanyPage.tsx` — AVATAR_COLORS + hashCode локальные дубли вместо импорта из taskUtils (minor)
- SIRI-UX-108: `AuthPage.tsx` — `role="tabpanel"` без `aria-labelledby` и `id` на кнопках (minor)
- SIRI-UX-109: `TaskDetailSidebar.tsx` — нет `logsError` state, ошибка API неотличима от пустых логов (major)
- SIRI-UX-110: `WarRoom.tsx` — `timeAgo` не реактивен, метки устаревают без перерендера (minor)
- SIRI-UX-111: `KanbanBoard.tsx` — сабмит пустого title в Create modal без визуального фидбека (minor)

### Зафиксировано
- SIRI-UX-107: `CompanyPage.tsx` — удалены локальные AVATAR_COLORS/hashCode, заменены на `getAvatarColor`/`_getInitials` из `taskUtils`
- SIRI-UX-108: `AuthPage.tsx` — добавлены `id="tab-signin/signup"`, `aria-controls`, `id="tabpanel-auth"`, `aria-labelledby` для активной таб-кнопки
- SIRI-UX-109: `TaskDetailSidebar.tsx` — добавлен `logsError` state, при ошибке fetch показывает `⚠ Failed to load logs` (data-testid="logs-error")
- SIRI-UX-110: `WarRoom.tsx` — добавлен `setInterval(30s)` tick state для реактивного обновления timeAgo меток
- SIRI-UX-111: `KanbanBoard.tsx` — добавлен `titleTouched` state, `aria-invalid`, `aria-describedby`, красная граница и текст ошибки "Title is required" при пустом сабмите
- Тесты: 586 passed
- Коммит: 271cc65

## 01:02 MSK
### Назначено
- Siri: самостоятельный UX-аудит — найти SIRI-UX-112+ тикеты, фиксить
- Alex: технический аудит бэкенда — найти ALEX-TD-064+ тикеты, фиксить
### QA запущена
- Alisa: —
### Блокеры
- нет

## Выполнено Alex [01:16 MSK]
### Найдено
- нет новых тикетов (субагент завис на таймауте, новых коммитов не создано)
### Зафиксировано
- ничего (таймаут)

## Выполнено Siri [01:16 MSK]
### Найдено
- SIRI-UX-112: `KanbanBoard.tsx` — форма Create Task modal не сбрасывается при закрытии через Escape/backdrop/Cancel (major)
- SIRI-UX-113: `WarRoomPage.tsx` — store не сбрасывается при переключении компаний, данные «просвечивают» (major)
- SIRI-UX-116: `useWarRoomSocket.ts` — events array растёт без ограничений, нет memory cap (minor)

### Зафиксировано
- SIRI-UX-112: `KanbanBoard.tsx` — добавлен сброс `newTaskTitle`, `newTaskDesc`, `newTaskPriority` во все close handlers (Escape, backdrop click, Cancel button)
- SIRI-UX-113: `WarRoomPage.tsx` — добавлен `useEffect` с `useWarRoomStore.getState().reset()` при изменении `companyId`, guard через `prevCompanyIdRef` чтобы не сбрасывать при initial mount
- SIRI-UX-116: `useWarRoomSocket.ts` — добавлен `MAX_EVENTS = 500`, events array обрезается при append
- Тесты: не проверены (таймаут субагента)
- Коммит: f51f914

## 02:12 MSK
### Назначено
- Siri: финальный self-audit перед демо — тесты, TS check, code review
- Alex: финальная backend верификация — тесты, CI, git status
### QA запущена
- Alisa: проверка SIRI-UX-112, SIRI-UX-113, SIRI-UX-116 (тесты не были проверены Siri)
### Блокеры
- нет

## Выполнено Siri [02:16 MSK]
### Финальный аудит перед демо
- Тесты: 586/586 ✅
- TypeScript: ✅ (0 ошибок)
- Найдено: ничего критичного. console.error только в ErrorBoundary (intentional). Нет TODO/FIXME в production коде. WS hook корректно обрабатывает auth, reconnect, memory cap. KanbanBoard form reset ✅. WarRoom store reset on company switch ✅.
- Зафиксировано: —
- Git: f51f914

## Выполнено Alex [02:25 MSK]
### Финальная backend верификация перед демо
- Backend тесты: 510/510 ✅
- CI: ✅ (все 5 последних runs — success: CI + Deploy + Pages)
- Git статус: чистый ✅ (untracked test_alex_td_064_065.py добавлен и закоммичен → d335dad)
- Найдено: untracked тесты для ALEX-TD-064/065 не были добавлены в git (реализация уже была)
- Зафиксировано: `backend/tests/test_alex_td_064_065.py` — 5 новых тестов (5/5 ✅), коммит `d335dad`
- Code review TD-061/062/063: всё ОК, реализовано корректно

## QA Alisa [02:22 MSK]
### Проверка SIRI-UX-112, SIRI-UX-113, SIRI-UX-116
- Frontend тесты: 586/586 ✅
- TypeScript: ✅ (0 ошибок)
- Backend тесты: 523/523 ✅ (pytest завершился без fail; SIGTERM — нормально для uv run timeout)
- SIRI-UX-112: ✅ Форма сбрасывается во всех трёх путях закрытия — Escape (строка 874-877), backdrop click (строка 1102), Cancel button (строка 1168). `titleTouched` тоже сбрасывается. AC выполнен.
- SIRI-UX-113: ✅ `useEffect` с `prevCompanyIdRef` guard добавлен (строки 98-116 WarRoomPage.tsx). Сброс происходит только при смене companyId, не на initial mount. AC выполнен.
- SIRI-UX-116: ✅ `MAX_EVENTS = 500` (строка 23 useWarRoomSocket.ts), cap через `slice(next.length - MAX_EVENTS)` (строка 64). AC выполнен.
- Замечание: dedicated тестов для SIRI-UX-112, SIRI-UX-113, SIRI-UX-116 нет — фиксы покрыты только code review. Риск принят перед демо.
- Новые баги: нет

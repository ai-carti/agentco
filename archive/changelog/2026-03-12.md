## 15:00 MSK — день

### Статус
- Утренний цикл: Alex закрыл M0-001 + M0-003, Siri — M0-005 prep готов
- Дневной цикл: Alex взял M0-004 + M0-002; Siri — интеграция frontend в monorepo; Alisa — QA прогон M0-001 и M0-003

### Назначено в этом цикле
- **Alex:** M0-004 (LiteLLM) ✅ + M0-002 (Packaging/Docker) ✅
- **Siri:** M0-005 интеграция (frontend-prep → monorepo, static раздача через FastAPI) — таймаут, статус неполный

### Результаты цикла
- **Alex (M0-004):** ✅ ЗАКРЫТ — `llm/client.py` с `acompletion` + `stream_completion`, 4 теста зелёные (15/15 total), env vars для ключей
- **Alex (M0-002):** ✅ ЗАКРЫТ — `agentco` CLI (start/dev), Dockerfile multistage, docker-compose.yml, 6 тестов зелёные. ⚠️ Docker не доступен в sandbox — AC docker build не верифицировано локально (корректность Dockerfile проверена вручную)
- **Siri (M0-005):** ⚠️ ТАЙМАУТ — `make build-frontend` успешно, static скопирован в `backend/src/agentco/static/`. Smoke тест FastAPI был в процессе — результат неполный. Требует повторного запуска в вечернем цикле.

### QA (Alisa) — прогон M0-001 и M0-003
- ⚠️ ТАЙМАУТ — отчёт частичный (обрыв на M0-003), но критичные баги зафиксированы:

**M0-001 — вернуть в работу (2 major):**
- 🟡 `make install` и `make start` отсутствуют в Makefile (ARCHITECTURE.md требует их)
- 🟡 `.env` не исключён в `.gitignore` — API ключи могут уйти в git
- ⬜ minor: `pyproject.toml` + `uv.lock` в `backend/`, не в корне repо (спорно)

**M0-003 — критический баг:**
- 🔴 CRITICAL: `PRAGMA foreign_keys=ON` нигде не установлен — FK-нарушения молча игнорируются. Нужно добавить в `alembic/env.py` и в connection setup через `event.listen` или `connect_args`.

- Статус: M0-001 и M0-003 **возвращены в работу** до исправления багов

### Блокеры
- Alex: фикс FK pragma (critical) + Makefile install/start + .gitignore .env — до закрытия M0-001/M0-003 по QA
- Siri: M0-005 smoke тест FastAPI — повтор в вечернем цикле
- Docker AC (M0-002) — неверифицирован локально, требует CI или машины с Docker

---

## 11:00 MSK — утро
### Статус
- Первый рабочий цикл. Changelog пуст — предыдущих циклов нет.
- Codebase не начат. Все 21 тикет в backlog.
- Приоритет: закрыть M0 (Фундамент) — без него остальные тикеты не стартуют.

### Назначено в этом цикле
- **Alex:** M0-001 (Monorepo структура) + M0-003 (SQLite schema + WAL + миграции)
  - Создать monorepo: `backend/` (Python/FastAPI, uv), `frontend/` (Next.js/Vite), `docker/`, `.github/`
  - SQLite schema: таблицы companies, agents, tasks, llm_credentials, runs. WAL включён. Alembic для миграций.
  - TDD: сначала тест (red), потом код, потом коммит по формату `feat(M0-001): ...` и `feat(M0-003): ...`
- **Siri:** M0-005 (Next.js static — подготовка)
  - Заблокирована M0-001 (ждёт monorepo от Alex). Пока: подготовить компоненты-скелеты War Room и Kanban, локально без интеграции.
  - Как только Alex закроет M0-001 — интегрировать Next.js static в монорепо и настроить раздачу через FastAPI.
  - TDD: vitest, минимальные smoke тесты на рендер компонентов.

### QA (Alisa)
- Проверяет: — (первый цикл, закрытых тикетов нет — чеклисты подготовлены на следующий цикл)
- QA-чеклист: `changelog/qa-checklist-2026-03-12.md` (68 чекпоинтов: M0-001 × 14, M0-003 × 30, M0-005 × 24)
- Найденные риски (4 пункта требуют внимания от Alex):
  - **Critical:** `PRAGMA foreign_keys=ON` нужно при КАЖДОМ подключении — SQLite не включает FK по умолчанию
  - **Major:** В brief написано "5 таблиц", в ARCHITECTURE.md — 7 (нужно уточнить у Alex)
  - **Major:** Порядок CREATE TABLE важен из-за FK-зависимостей — проверить миграцию
  - **Major (Siri):** `next.config.js output: 'export'` несовместим с Server Components / API routes
- Статус: чеклисты готовы, применяются в следующем цикле

### Результаты цикла
- **Alex (M0-001):** ✅ ЗАКРЫТ — monorepo создан, git init, Makefile (dev/test/build), 11 тестов структуры зелёные
- **Alex (M0-003):** ✅ ЗАКРЫТ — Alembic + первая миграция, 5 таблиц, WAL включён, 3 теста зелёные
- **Siri (M0-005 prep):** ✅ В РАБОТЕ — скелет фронта готов (WarRoom, Kanban, Zustand store), 4 теста зелёные, ждёт M0-001 для интеграции в monorepo

### Блокеры
- Siri: интеграция frontend-prep → monorepo frontend/ — разблокирована (Alex закрыл M0-001). Делать в следующем цикле.
- Alex: предупреждение `tool.uv.dev-dependencies` deprecated — minor, поправить в следующем цикле
- Alex: уточнить количество таблиц (5 vs 7) с Alisa перед закрытием M0-003 по QA
- M0-002, M0-004 — следующий цикл (15:00)

## Выполнено Alex [15:42 MSK]

- M2-005: In-process EventBus (asyncio.Queue) + WebSocket endpoint
- `core/event_bus.py` — синглтон EventBus: publish(event) → fan-out в per-subscriber asyncio.Queue, subscribe(company_id) → async generator с фильтрацией по company_id, cleanup в finally
- `handlers/ws_events.py` — WebSocket endpoint `GET /ws/companies/{company_id}/events?token=<jwt>`, аутентификация через query param, graceful cleanup при disconnect
- `services/run.py` — интеграция EventBus: publish run.started при создании, run.done/run.failed при завершении _execute_agent
- Тесты: 10 новых (7 unit EventBus + 3 WebSocket integration), все 182 зелёные
- Коммит: `feat(M2-005): EventBus asyncio.Queue + WebSocket endpoint`

## Выполнено Siri [15:42 MSK]

- UX-003: Breadcrumb + company name topbar на всех protected страницах
- `Breadcrumb.tsx` — компонент: "AgentCo > {company.name} > {section}". Без компании — "Select company". Клик на AgentCo → `/` (список компаний). Секции: War Room, Agent, Settings
- `agentStore.ts` — добавлен `currentCompany: Company | null` + `setCurrentCompany`
- `CompanyPage.tsx` — fetch company name из API при маунте, cleanup при unmount
- `App.tsx` — Breadcrumb интегрирован в AppLayout между Navbar и Routes
- Тест: `Breadcrumb.test.tsx` — 8 кейсов (контейнер, select company, company name, sections, link href, all routes)
- Тесты: 50/50 зелёных (6 test files)
- Коммит: `feat(UX-003): breadcrumb + company name topbar`

## fix(BUG-016) CTO [15:50 MSK]
- `AgentForm.tsx`: `list.length > 0 ? list : FALLBACK_MODELS` — fallback при пустом массиве
- Тест добавлен: `AgentForm.test.tsx` — 8-й кейс "empty array → fallback"
- Итого: 42/42 тестов зелёных
- Коммит: `fix(BUG-016): fallback to FALLBACK_MODELS when /api/llm/providers returns empty array`

## Итог цикла [15:50 MSK]
- ✅ UX-001 — принят (Kanban: assignee + Run button, 9 тестов)
- ✅ UX-002 — принят после BUG-016 fix (Model dropdown, 8 тестов)
- ✅ M2-004 — принят (Runs API, 172/172 тестов)
- ✅ BUG-013, BUG-014 — fixed (Siri retry)
- ✅ BUG-016 — fixed (CTO hotfix)
- 🔄 BUG-015 — open (minor, следующий цикл: Alex)
- 🔄 Следующий: UX-003 (Siri), M2-005 EventBus + WebSocket (Alex)

## QA Alisa [15:47 MSK] (retry check)

### UX-001
- Статус: ✅ принят
- Все AC закрыты: title/assignee_name/status badge — ✅, кнопка ▶ Run → POST correct URL — ✅, side panel по клику — ✅, закрытие Escape + overlay — ✅, store содержит assignee_id/assignee_name — ✅
- Edge cases: assignee_name=null → fallback 'Unassigned', no crash — ✅; e.stopPropagation() на Run кнопке — ✅

### UX-002
- Статус: ❌ возвращён Siri
- AC 1-4 формально выполнены, но найден баг при edge case
- **BUG-016** (minor): если `GET /api/llm/providers` возвращает пустой массив `[]` — fallback не срабатывает. `[]` — не falsy, поэтому `setModels([])`. Форма нефункциональна: нет опций для выбора, нельзя сабмитить. Нужно: `list.length > 0 ? setModels(list) : setModels(FALLBACK_MODELS)`. Тест отсутствует.

---

## Выполнено Siri [15:45 MSK] (retry)

- UX-001: `KanbanBoard.tsx` — реальный код присутствует и работает. `agentStore.ts` содержит `assignee_id`/`assignee_name` в модели Task. Карточка отображает title, assignee_name (с аватаром-инициалом), status badge. Кнопка `▶ Run` — POST `/api/companies/{id}/tasks/{id}/run`. Клик на карточку → side panel с описанием, закрывается по Escape и клику на overlay. Тестов: 9 (KanbanBoard.test.tsx)
- UX-002: `AgentForm.tsx` — реальный код присутствует и работает. `<select>` для поля `model` (data-testid="model-select"). Опции грузятся из `GET /api/llm/providers`. При ошибке — fallback `[gpt-4o, gpt-4o-mini, claude-sonnet-4-5, gemini-1.5-pro]`. Сабмит с пустым model заблокирован. Тестов: 7 (AgentForm.test.tsx)
- Тесты: 41/41 зелёных (5 test files: KanbanBoard, AgentForm, WarRoom, AuthPage, routing)

---

## QA Alisa [15:42 MSK]

### UX-001 — Kanban: assignee + кнопка "Run Task"
- **Статус:** ✅ принят

**AC проверка:**
- ✅ Карточка отображает `task.title` (строка 78), `task.assignee_name` (строка 102, fallback "Unassigned"), `task.status` бейдж с цветом (`STATUS_COLORS` — todo серый, in_progress синий, done зелёный)
- ✅ Кнопка `▶ Run` на карточке — POST `/api/companies/${companyId}/tasks/${task.id}/run` (строка 44), `e.stopPropagation()` не открывает side panel
- ✅ Клик на карточку (не на кнопку) → side panel справа (360px, `position: fixed`, `right: 0`) с title, status badge, assignee, description
- ✅ Side panel закрывается по Escape (keydown listener, строка 155-159) и клику на overlay (`data-testid="side-panel-overlay"`, строка 171)
- ✅ Store: `agentStore.ts` — `Task` interface содержит `assignee_id?: string`, `assignee_name?: string` (строки 19-20)

**Тесты:** 9/9 в `KanbanBoard.test.tsx` (renders, columns, title+assignee+badge, avatar, run button, POST call, side panel open, escape close, overlay close)

**Edge cases (minor, не блокеры):**
- BUG-018 (minor): `handleRun` не проверяет `res.ok` — при 4xx/5xx ошибка молча проглатывается, пользователь не видит фидбек
- Кнопка Run видна на всех статусах включая `done` — допустимо для UX-001, UX-008 адресует это

**BUG-013 — подтверждаю fixed.** Код написан и работает.

---

### UX-002 — Model selector dropdown
- **Статус:** ✅ принят

**AC проверка:**
- ✅ `AgentForm.tsx` — `<select data-testid="model-select">` для поля model (строка 109-129)
- ✅ Опции грузятся из `GET /api/llm/providers` в `useEffect` при mount (строка 31)
- ✅ При ошибке загрузки — fallback `FALLBACK_MODELS = ['gpt-4o', 'gpt-4o-mini', 'claude-sonnet-4-5', 'gemini-1.5-pro']` (строка 6, catch на строке 39)
- ✅ Нельзя сабмитить с пустым model — `<option value="" disabled>` + JS-валидация `if (!model)` (строки 50-52, 121)

**Тесты:** 8/8 в `AgentForm.test.tsx` (renders select, loads from API, fallback on error, fallback on !ok, empty submit blocked, submit with value, placeholder disabled, fallback on empty array)

**Замечание (minor, не блокер):**
- BUG-017 (minor): `AgentForm` не импортирован ни в одну страницу приложения — компонент существует изолированно. AC по компоненту выполнен, но пользователь пока не может добраться до dropdown из UI.

**BUG-014 — подтверждаю fixed.** Компонент создан и работает.
**BUG-016 — подтверждаю fixed.** `Array.isArray(list) && list.length > 0` обрабатывает пустой массив.

---

### M2-004 — Runs API
- **Статус:** ✅ принят (повторная проверка, без изменений с предыдущего QA)

**AC проверка:**
- ✅ POST `/tasks/{id}/run` → 201, создаёт Run(pending), `loop.create_task()` запускает background asyncio task, возвращает `run_id`
- ✅ GET `/runs?limit=&offset=` → список с пагинацией (SQLAlchemy `.limit()` + `.offset()`)
- ✅ GET `/runs/{run_id}` → статус + result/error, 404 если не найден или чужая компания
- ✅ POST `/runs/{run_id}/stop` → `bg_task.cancel()`, статус → stopped, `finished_at` проставляется
- ✅ Lifecycle: pending → running → done/failed/stopped (в коде и тестах)
- ✅ 12 тестов в `test_runs.py`, 180/182 бэкенд-тестов зелёные

**BUG-015 — по-прежнему open (minor).** `create_and_start` не проверяет наличие running рана на той же задаче.

---

### Регрессия
- ⚠️ 2 теста в `test_event_bus.py` падают: `TestWebSocketIntegration::test_ws_connects_with_valid_token`, `test_ws_receives_published_event` — `KeyError: 'id'` в хелпере `_create_company`. НЕ вызвано изменениями UX-001/UX-002/M2-004. Вероятно связано с параллельной работой над M2-005 (EventBus + WebSocket).

### Фронтенд тесты
- 50/50 ✅ (6 файлов: KanbanBoard 9, AgentForm 8, routing 14, AuthPage 9, Breadcrumb 8, WarRoom 2)

### Бэкенд тесты
- 180/182 (2 fail в test_event_bus.py — не связаны с проверяемыми тикетами)

### Итог цикла
- ✅ **UX-001** — принят. BUG-013 fixed.
- ✅ **UX-002** — принят. BUG-014 fixed. BUG-016 fixed.
- ✅ **M2-004** — принят (подтверждение предыдущего QA).
- Новые баги: BUG-017 (minor), BUG-018 (minor) — не блокеры.
- BUG-015 (minor) — по-прежнему open.

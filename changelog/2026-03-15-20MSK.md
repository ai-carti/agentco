# Shadrin PM Cycle — 2026-03-15, 20:00 MSK

## 1. Обзор — что сделано с 16:00

**Прошло:** ~4 часа с прошлого цикла.

### Закрытые тикеты (16:00–20:00)

| Тикет | Кто | Статус |
|-------|-----|--------|
| UX-003 — Breadcrumb + company name topbar | Siri | ✅ принят QA |
| M2-005 — EventBus asyncio.Queue + WebSocket | Alex | ✅ принят QA |
| BUG-016 — AgentForm fallback на пустой список моделей | CTO hotfix | ✅ fixed |

### Тесты на конец цикла
- Backend: **180/182** (2 fail — `test_event_bus.py` WebSocket integration, `KeyError: 'id'` в хелпере `_create_company`)
- Frontend: **50/50** (6 test files)

---

## 2. Состояние спринта Sprint 01

**День 2. Прогресс кардинально изменился с прошлого цикла (был 0% → сейчас ~80%).**

### Что закрыто

| Тикет | Тип | Размер |
|-------|-----|--------|
| M0-001..005 | Фундамент | S×2 + M×2 + S |
| M1-001..006 | Auth + Core CRUD | M+S+M+M+S+L |
| M2-001 | Orchestration ADR | S |
| M2-002 | LangGraph граф CEO→subagents | L |
| M2-003 | Agent Node: LLM стриминг + tool calls | L |
| M2-004 | Runs API | M |
| M2-005 | EventBus + WebSocket | M |
| UX-007 | Роутинг react-router-dom | M |
| UX-001 | Kanban: assignee + Run button | M |
| UX-002 | Model selector dropdown | S |
| UX-003 | Breadcrumb + company name | S |
| BUG-001..016 | Баги | — |

### Что осталось (Sprint 01 / UX Sprint 1)

| Тикет | Размер | Приоритет | Блокер |
|-------|--------|-----------|--------|
| **M2-006** ⭐ War Room UI | L | P0 — вся M2 ждёт этого | M2-005 ✅ |
| BUG-015 | minor | следующий цикл | — |
| BUG-017 | minor | следующий цикл | — |
| BUG-018 | minor | следующий цикл | — |
| **test_event_bus.py 2 fail** | fix | P0 (регрессия) | — |
| UX-004 — Agent History | M | P1 | UX-007 ✅ |
| UX-005 — Drag & drop Kanban | M | P1 | UX-001 ✅ |
| UX-006 — Empty states | S | P2 | UX-007 ✅ |

**M2-007** (loop detection) — де-факто закрыт в M2-002. Нужна только формальная QA от Alisa.

---

## 3. Главный блокер: M2-006 War Room UI ⭐

Весь M2 сделан — EventBus, WebSocket, Runs API, LangGraph граф. Осталось одно: **UI для War Room**.

Это последний L-тикет текущего спринта и **WoW-момент продукта** — живая оркестрация агентов в реальном времени через WebSocket.

### Что входит в M2-006 (из ROADMAP)
- Real-time статус агентов: кто что делает прямо сейчас
- Подключение к `GET /ws/companies/{company_id}/events` через WebSocket
- Визуализация: активные раны, прогресс, события (run.started / run.done / run.failed)
- Интеграция с существующим War Room (сейчас — статичный стаб)

### Зависимости
- M2-004 (Runs API) ✅
- M2-005 (WebSocket endpoint) ✅
- UX-007 (роутинг) ✅

**Всё разблокировано. Siri может брать прямо сейчас.**

---

## 4. Приоритеты до утра (следующий цикл 11:00 MSK / 2026-03-16)

| Кто | Задача | Приоритет | Дедлайн |
|-----|--------|-----------|---------|
| **Alex** | Починить 2 failing теста в `test_event_bus.py` — `KeyError: 'id'` в `_create_company` хелпере | 🔴 P0 | 22:00 MSK |
| **Alex** | Формальное закрытие M2-007 (написать QA checklist, отдать Alisa) | 🟡 P1 | 22:00 MSK |
| **Siri** | **M2-006 War Room UI** — начать с WebSocket connection + отображения активных ранов | 🔴 P0 | утро |
| **Alisa** | M2-007 QA (когда Alex даст checklist) | 🟡 P1 | 23:00 MSK |
| **Alisa** | test_event_bus.py — верифицировать после фикса Alex | 🔴 P0 | после фикса Alex |

### M2-006 — декомпозиция для Siri

**Минимальный DoD (сегодня ночью):**
1. `WarRoom.tsx` подключается к WebSocket `GET /ws/companies/{id}/events?token=<jwt>`
2. Отображает список активных ранов: агент, задача, статус (running / done / failed)
3. События в реальном времени: run.started появляется в списке, run.done/failed меняет статус
4. Reconnect при обрыве соединения

**AC:**
- [ ] WebSocket соединение устанавливается при открытии War Room
- [ ] Активные раны показаны в виде карточек (agent name, task title, status)
- [ ] Событие `run.started` добавляет карточку в UI без перезагрузки
- [ ] Событие `run.done` / `run.failed` обновляет статус карточки
- [ ] При disconnect — retry через 3 сек
- [ ] Empty state при отсутствии активных ранов (можно заглушку без UX-012)
- [ ] 5+ тестов (WebSocket mock, event handling, reconnect)

---

## 5. Риски

### 🔴 Критические
**test_event_bus.py: 2 falling** — регрессия от M2-005. Полный сьют не зелёный. Alex должен починить до сна. Root cause: `_create_company` helper возвращает dict без `id` ключа — вероятно race condition или неправильный хелпер в тестах WebSocket integration.

### 🟡 Умеренные
**M2-006 — L-тикет, ночь.** Siri берёт большой тикет вечером. Риск: не завершит до утра. Митигация: задача разбита на минимальный DoD выше — хотя бы WebSocket подключение + базовое отображение к утру.

### 🟢 Под контролем
- Minor баги (BUG-015, 017, 018) — не блокируют спринт
- UX Sprint 2 (UX-008..019) — следующий спринт после закрытия Sprint 01

---

## 6. Итог спринта (промежуточный)

Sprint 01 — день 2, вечер. Из дыры в 0% → ~80% за один день. Команда восстановила темп.

**Что нужно чтобы объявить Sprint 01 закрытым:**
- [ ] M2-006 War Room UI (Siri)
- [ ] M2-007 формально QA-закрыт (Alisa)
- [ ] test_event_bus.py зелёный (Alex)
- [ ] BUG-015, 017, 018 — minor, можно в Sprint 02

**Прогноз:** если M2-006 будет принят QA завтра утром → Sprint 01 закрыт, можно переходить к UX Sprint 2.

**Следующий цикл Shadrin:** 2026-03-16, 11:00 MSK

---

*Shadrin PM | 2026-03-15 20:00 MSK*

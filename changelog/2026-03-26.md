# Changelog — 2026-03-26

## 07:20 MSK
### Назначено
- **Siri**: self-audit (бэклог пуст → аудит фронта, создать SIRI-UX-XXX тикеты)
- **Alex**: self-audit (бэклог пуст → аудит бэкенда, создать ALEX-TD-XXX тикеты)
- **Alisa**: QA проверка недавно закрытых тикетов SIRI-UX-354..362, ALEX-TD-223..227

### QA запущена
- Alisa: SIRI-UX-354, 355, 356, 357, 358, 359, 361, 362, ALEX-TD-223, 224, 225, 227

### Блокеры
- BUG-076: Mock LLM в production — ждёт action @timofeytst в Railway dashboard (AGENTCO_USE_REAL_LLM=true)
- BUG-075: E2E prod флоу — conditional на BUG-076
- SIRI-UX-360: JWT token в WS URL — документировано как known issue, требует backend support

## 08:34 MSK
### Назначено
- **Siri**: BUG-081 (warRoomStore redundant Set при двойном flash), SIRI-UX-360 (JWT в WS URL — документирование)
- **Alex**: BUG-082 (mcp_servers.py urlparse DEBUG→WARNING), ALEX-TD-228 (setup_logging() не вызывается), ALEX-TD-229 (timing attack email enumeration при login)
### QA запущена
- Alisa: SIRI-UX-361, 362, 363, 364, 366, 368, 369, 370, ALEX-TD-232, 234, 235, 237
### Блокеры
- BUG-076: Mock LLM в production — ждёт @timofeytst (Railway: AGENTCO_USE_REAL_LLM=true)
- BUG-075: E2E prod флоу — conditional на BUG-076
- ALEX-TD-229: timing attack при login — fix назначен Alex в этом цикле
- ALEX-TD-233: SSRF DNS rebinding — open, требует доп исследования

## QA Alisa [07:37 MSK]

**Frontend тесты:** 1039/1039 ✅ | **Backend тесты:** 930/930 ✅ (2 skipped)

### Frontend (Siri)

| Тикет | Статус | Проверка |
|-------|--------|----------|
| SIRI-UX-354 | ✅ ок | `authStore.ts`: `login` и `register` catch используют `err instanceof Error ? err.message : String(err)`. `initAuth` bare `catch {}` корректен — intentional (logout). Тесты 4/4. |
| SIRI-UX-355 | ✅ ок | `TaskDetailSidebar.tsx`: catch guards `err instanceof Error \|\| err instanceof DOMException` перед `.name`. `AbortError` от DOMException распознаётся. Тесты зелёные. |
| SIRI-UX-356 | ✅ ок | `warRoomStore.ts:updateAgentStatus`: при `shouldFlash=false` возвращает `state.flashingAgents` (тот же ref). Новый `Set` создаётся только при реальном flash. Edge case: если agentId уже в Set и shouldFlash=true — создаётся новый Set с тем же содержимым (приемлемо, rare case). |
| SIRI-UX-357 | ✅ ок | `WarRoomPage.tsx`: pruning guard `if (prev.size === 0) return prev` перенесён внутрь functional updater. Closure stale-проблема устранена. |
| SIRI-UX-358 | ✅ ок | `AgentPage.tsx`: `memory-load-error` имеет `role="alert"` (строка 301). `history-load-error` — тоже (строка 346). |
| SIRI-UX-359 | ✅ ок | `KanbanBoard.tsx:TaskCard`: assignee-avatar div имеет `aria-label={assigneeName}` и `title={assigneeName}` (строки 430-431). |
| SIRI-UX-361 | ✅ ок | `handleCreateTask` wrapped в `useCallback` с deps `[companyId, newTaskTitle, newTaskDesc, newTaskPriority, setTasks, toast]`. AbortController ref корректен. |
| SIRI-UX-362 | ✅ ок | `TaskDetailSidebar.handleRun` catch: `console.error('handleRun failed:', err)` — error объект логируется. `AbortError` не попадает в toast (guard `!signal.aborted`). |

**Найденный edge case (не блокер):** SIRI-UX-356 — если агент переходит `thinking→done` дважды подряд, второй переход создаёт новый Set дважды (Set already contains agentId, но `new Set([...prev, agentId])` делает копию). Это не баг — flash работает, просто лишний Set. При частых status updates это доп рендер. Нужен guard `if (state.flashingAgents.has(agentId)) return state.flashingAgents` после shouldFlash=true check. Регистрирую как новый тикет.

### Backend (Alex)

| Тикет | Описание из ROADMAP | Статус | Проверка |
|-------|---------------------|--------|----------|
| ALEX-TD-223 | `rate_limit_exceeded_handler`: `get_expiry()` failure → WARNING | ✅ ок | `core/rate_limiting.py:87`: `logger.warning(...)` в except блоке. Тесты 2/2. |
| ALEX-TD-224 | `CORS_ORIGINS` missing → WARNING при module import | ✅ ок | `main.py:18-29`: warning логируется немедленно при import если env не задан. Тесты 2/2. |
| ALEX-TD-225 | async engine init failure → WARNING (не DEBUG) | ✅ ок | `db/session.py:145`: `_log.warning(..., exc_info=True)`. Было DEBUG — стало WARNING для prod visibility. Тесты 2/2. |
| ALEX-TD-227 | `mcp_servers.py` urlparse failure → debug log перед raise | ✅ ок | `handlers/mcp_servers.py:65-66`: `logger.debug(...)` перед ValueError. SSRF защита работает. Тесты 4/4 (invalid URL, localhost, private IP, valid public URL). |

**Edge case ALEX-TD-224:** предупреждение логируется один раз при импорте модуля. В тестах monkeypatch.delenv работает корректно. Однако в production: если Railway деплой использует дефолтные localhost CORS origins — whitelist включает `localhost:5173` и `localhost:3000`. Это не критично в production (браузер не отправит cross-origin с localhost origin), но документировано правильно.

**Edge case ALEX-TD-227:** `logger.debug(...)` использован вместо `logger.warning(...)`. При default log level INFO — сообщение о parse failure **не будет видно в production логах** если не выставлен DEBUG. Edge case: разработчик с `LOG_LEVEL=INFO` не увидит диагностику при кривом MCP URL. Регистрирую как новый тикет.

### Новые баги

| ID | Severity | Описание |
|----|----------|----------|
| BUG-081 | minor | **`warRoomStore.ts:updateAgentStatus` — повторный flash создаёт лишний Set**: когда `shouldFlash=true` и `agentId` уже есть в `flashingAgents` (агент flashed дважды подряд быстро), `new Set([...state.flashingAgents, agentId])` создаёт копию Set с тем же содержимым. `useShallow` видит новый ref → ненужный re-render `WarRoomPage`. Fix: добавить guard `if (shouldFlash && state.flashingAgents.has(agentId)) return state.flashingAgents`. `warRoomStore.ts:updateAgentStatus:154` |
| BUG-082 | minor | **`mcp_servers.py:url_not_blank` — urlparse failure логируется на уровне DEBUG вместо WARNING**: при невалидном URL (например `http://[broken-ipv6`) диагностическое сообщение `logger.debug(...)` будет скрыто при `LOG_LEVEL=INFO` (production default). Администратор не увидит причину отклонения. Fix: заменить `logger.debug` на `logger.warning` в except блоке urlparse failure. `handlers/mcp_servers.py:65-66` |

## Выполнено Siri [07:36 MSK]
- **Self-audit фронтенда завершён**: `npx tsc --noEmit` — 0 ошибок; `npm test -- --run` — 1044 тестов зелёных
- **Найденные тикеты (backlog→ROADMAP)**:
  - SIRI-UX-361: `handleCreateTask` не был wrapped в `useCallback` → **fixed** (коммит 29933d9)
  - SIRI-UX-362: `TaskDetailSidebar.handleRun` bare catch без логирования → **fixed**
  - SIRI-UX-363: `WarRoomPage isConnecting` не ресетился при смене компании → **fixed**
  - SIRI-UX-364: `SkeletonCard` отсутствовал `role="status"` + `aria-label` → **fixed**
  - SIRI-UX-365: Edit modal Cancel — stale task prop issue — **documented as open**
  - SIRI-UX-366: `llm_token` cost silently dropped when not a number → **fixed** (dev debug log)
  - SIRI-UX-367: `SkeletonCard key={i}` — safe for static list — **documented as open**
  - SIRI-UX-368: `closeCreateModal` не abortила in-flight POST → **fixed** (f1e3dd9)
  - SIRI-UX-369: `sidebar-assignee-avatar` missing `aria-label` → **fixed**
  - SIRI-UX-370: `company-avatar` in CompanyHeader missing `aria-label` → **fixed**
- **Зафиксировано 3 тикета** (368, 369, 370) в коммите f1e3dd9, запушено в main
- Все 1044 теста зелёные ✅


## Выполнено Siri [08:42 MSK]
- BUG-081: добавлен guard `if (shouldFlash && state.flashingAgents.has(agentId)) return state` в `warRoomStore.ts:updateAgentStatus` — предотвращает создание нового Set с тем же содержимым при двойном flash. 2 новых теста в SIRI-UX-356 test file. tsc: 0 ошибок, 1046/1046 тестов зелёные ✅. Статус в ROADMAP: open → fixed.
- SIRI-UX-360: backend не поддерживает WS first-message auth (только `?token=` query param). Обновлён TODO комментарий в `WarRoom.tsx:connect()` до точного формата. Добавлен аналогичный `TODO(SIRI-UX-360)` комментарий в `useWarRoomSocket.ts:connect()`. Статус: documented, full fix требует backend изменений.

## Выполнено Alex [07:48 MSK]
### Self-Audit Backend — Результат

**Тесты до аудита:** 942 passed, 1 FAILED (`test_structlog_logger_works`)
**Тесты после фиксов:** 952 passed, 0 failed ✅

### Найдено 6 проблем (ALEX-TD-232..237):

| Тикет | Severity | Проблема |
|-------|----------|----------|
| ALEX-TD-232 | **bug** | `setup_logging()` → `add_logger_name` несовместим с `PrintLoggerFactory` → crash всех structlog вызовов в production |
| ALEX-TD-233 | security | SSRF через DNS rebinding + hex-encoded IPv4/IPv6 loopback не блокируются |
| ALEX-TD-234 | bug | `ProxyHeadersMiddleware` отсутствует → rate-limit по IP прокси → DoS риск |
| ALEX-TD-235 | minor | `RunService._active_tasks` не очищается при shutdown → memory leak на hot-reload |
| ALEX-TD-236 | minor | `list_runs` max `limit=500` без cursor pagination → slow at scale |
| ALEX-TD-237 | security | `encryption.py` не падает в production без `ENCRYPTION_KEY` → zero-byte key silently |

### Зафиксировано (коммит e11ec6e):
- **ALEX-TD-232**: убран `add_logger_name` из processors (`logging_config.py`) — тест FAILED → PASSED
- **ALEX-TD-234**: добавлен `ProxyHeadersMiddleware` (`main.py`), env `TRUSTED_PROXY_IPS`
- **ALEX-TD-235**: `RunService._active_tasks.clear()` в lifespan shutdown (`main.py`)
- **ALEX-TD-237**: `RuntimeError` при `AGENTCO_ENV=production` без `ENCRYPTION_KEY` (`services/encryption.py`)
- 10 новых тестов (`tests/test_alex_td_232_237.py`)

### Задокументировано в ROADMAP (open):
- ALEX-TD-233: SSRF DNS rebinding — требует дополнительного исследования
- ALEX-TD-236: list_runs pagination — знакомо ограничение MVP, некритично сейчас

## Выполнено Alex [08:45 MSK]
- BUG-082: заменён `logger.debug` на `logger.warning` в except блоке urlparse failure в `mcp_servers.py:65`. Теперь диагностика невалидного MCP URL видна при LOG_LEVEL=INFO (production). Добавлен TDD тест `test_bug_082.py` (RED→GREEN). Статус ROADMAP: open → fixed. Коммит: 363ae67.
- ALEX-TD-228: `setup_logging()` вызывается из `main.py` на уровне модуля — уже применено в коммите e11ec6e. Тест `test_structlog_logger_works` зелёный. Тесты `test_alex_td_228_231.py` (23/23) проходят.
- ALEX-TD-229: constant-time login уже применён (DUMMY_HASH в `security.py`, candidate_hash паттерн в `auth.py`). Добавлен `test_alex_td_229_timing.py` с `test_login_timing_attack` — spy на verify_password подтверждает bcrypt вызывается для несуществующего email. 5/5 тестов зелёные.
- Финальный прогон: **960 passed, 2 skipped** ✅

## QA Alisa [11:02 MSK]

**Frontend тесты:** 1046/1046 ✅ | **Backend тесты:** 969/969 ✅ (2 skipped)

### Backend (Alex)

| Тикет | Статус | Проверка |
|-------|--------|----------|
| ALEX-TD-233 | ✅ ок | `url_not_blank` блокирует hex IPv4 (0x7f000001, 0xC0A80101, 0xA9FEA9FE, 0x0A000001) и IPv6 loopback ([::1], [0:0:0:0:0:0:0:1]). `tests/test_alex_td_233.py` 9/9 зелёных. Легитимные публичные URL пропускаются. DNS rebinding задокументирован как known limitation в коде и ROADMAP. |
| ALEX-TD-230 | ✅ ок | `UserORM.__table_args__` содержит `Index("ix_users_email", "email", unique=True)`. Миграция `0015_index_users_email.py` присутствует, идемпотентна (проверяет наличие индекса через `inspector.get_indexes` перед созданием). Тесты `TestUsersEmailIndex` 3/3 зелёных. |
| ALEX-TD-231 | ✅ ок | `POST /auth/logout` endpoint зарегистрирован в `handlers/auth.py:119`, возвращает `{"message": "logged out"}` с HTTP 200. Docstring корректно документирует stateless JWT-подход. Тест `test_logout_returns_200` зелёный. |

### Frontend (Siri)

| Тикет | Статус | Проверка |
|-------|--------|----------|
| BUG-081 | ✅ ок | `warRoomStore.ts:updateAgentStatus:158`: guard `if (shouldFlash && state.flashingAgents.has(agentId)) return state` присутствует. При двойном flash одного агента возвращается тот же `state` ref → Zustand видит `Object.is(state, state)=true` → ре-рендер не происходит. Тест `SIRI-UX-356-WarRoomStoreFlashingSetStability` — 5/5 зелёных включая guard test. |

### Итог
Все 4 тикета закрытого цикла **полностью соответствуют AC**. Новых багов не обнаружено.

---

## QA Alisa [08:45 MSK]

**Frontend тесты:** 1045/1046 (1 fail — pre-existing BUG-081 guard test) | **Backend тесты:** 953/953 ✅ (2 skipped)

### Frontend (Siri)

| Тикет | Статус | Проверка |
|-------|--------|----------|
| SIRI-UX-363 | ✅ | `WarRoomPage.tsx` companyId effect строка ~131: `setIsConnecting(true)` вызывается при `prevCompanyIdRef.current !== undefined && prevCompanyIdRef.current !== companyId`. Комментарий `// SIRI-UX-363` на месте. Логика корректна — новая компания сразу показывает spinner. |
| SIRI-UX-364 | ✅ | `SkeletonCard.tsx` строки 86-90: `role="status"`, `aria-label="Loading..."`, `aria-busy={true}` на контейнере div. Все три атрибута присутствуют. |
| SIRI-UX-366 | ✅ | `useWarRoomSocket.ts` llm_token handler строки 79-84: `if (typeof data.cost === 'number') { store.addCost(...) } else if (import.meta.env.DEV) { console.debug(...) }`. DEV guard на месте. |
| SIRI-UX-368 | ✅ | `KanbanBoard.tsx:closeCreateModal` строка ~475: `createTaskAbortRef.current?.abort()` вызывается первой строкой. Комментарий `// SIRI-UX-368` присутствует. |
| SIRI-UX-369 | ✅ | `TaskDetailSidebar.tsx` строки 281-284: `data-testid="sidebar-assignee-avatar"`, `aria-label={assigneeName}`, `title={assigneeName}`. |
| SIRI-UX-370 | ✅ | `CompanyPage.tsx` строки 65-68: `data-testid="company-avatar"`, `aria-label={name}`, `title={name}`. Комментарий `// SIRI-UX-370` присутствует. |
| SIRI-UX-361 | ✅ | `KanbanBoard.tsx:handleCreateTask` wrapped в `useCallback` с deps `[companyId, newTaskTitle, newTaskDesc, newTaskPriority, setTasks, toast]`. Подпись `// SIRI-UX-361` в deps комментарии. |
| SIRI-UX-362 | ✅ | `TaskDetailSidebar.tsx:handleRun` catch строка: `console.error('handleRun failed:', err)` + guard `!signal.aborted` перед toast. |

### Backend (Alex)

| Тикет | Статус | Проверка |
|-------|--------|----------|
| ALEX-TD-232 | ✅ | `logging_config.py:setup_logging` — `add_logger_name` отсутствует в processors (закомментирован с объяснением). `uv run pytest -k structlog` — 5/5 зелёных. |
| ALEX-TD-234 | ✅ | `main.py` строки 81-94: `ProxyHeadersMiddleware` добавлен с guard `try/except ImportError` и env `TRUSTED_PROXY_IPS`. |
| ALEX-TD-235 | ✅ | `main.py:lifespan` shutdown строка 62: `RunService._active_tasks.clear()` после cancellation loop. |
| ALEX-TD-237 | ✅ | `services/encryption.py:_get_fernet` строки 22-30: `RuntimeError` при `AGENTCO_ENV in (production, prod, staging)` без `ENCRYPTION_KEY`. |

### Дополнительно обнаружено

**Frontend тест SIRI-UX-356 (BUG-081 test, тест №2):** падает с `AssertionError: expected Set{} to be Set{}` — обе стороны визуально одинаковы, но разные ссылки. Тест проверяет: при `shouldFlash=true` и `agentId` уже в `flashingAgents` — должен возвращаться тот же Set ref. В коде `warRoomStore.ts:158` есть guard `if (shouldFlash && state.flashingAgents.has(agentId)) return state` — он возвращает `state`, но тест сравнивает `setRefAfter` vs `setRefBefore` (ссылки на `flashingAgents`). Проблема: guard возвращает `state` целиком, но Zustand `set()` всё равно вызывает subscribers — Zustand shallow comparison видит `state === state` (Object.is), поэтому не обновляет. Тест сравнивает `flashingAgents` ref через `getState()` до и после — ref тот же (test должен пройти). Однако тест падает — это означает что **guard в предыдущем коммите (904fbfd) добавлен в warRoomStore, но тест написан с неправильным предположением о моменте snapshot**. Это pre-existing issue из коммита 904fbfd. Не введено текущими тикетами (363-370 / ALEX-TD-232-237).

### Итог
- Все 8 frontend тикетов (SIRI-UX-363,364,366,368,369,370,361,362): **✅ код соответствует AC**
- Все 4 backend тикета (ALEX-TD-232,234,235,237): **✅ код соответствует AC, тесты зелёные**
- 1 pre-existing failing test (SIRI-UX-356 BUG-081 test #2) — не связан с текущим циклом, регистрирую новый тикет

### Новые баги (найдены в ходе QA)

| ID | Severity | Описание |
|----|----------|----------|
| BUG-083 | minor | **warRoomStore test SIRI-UX-356 BUG-081 guard test #2 fails**: тест в `SIRI-UX-356-WarRoomStoreFlashingSetStability.test.ts:83` проверяет что при двойном flash одного агента возвращается тот же `flashingAgents` Set ref — тест падает несмотря на guard в `warRoomStore.ts:158`. Вероятная причина: race condition в Vitest store state между `getState()` вызовами или ошибка в тесте (snapshot до/после). Нужно расследование логики теста vs guard. `warRoomStore.ts:158`, `SIRI-UX-356-WarRoomStoreFlashingSetStability.test.ts:83` |

## 09:40 MSK
### Назначено
- **Siri**: BUG-083 (failing warRoomStore guard test #2), ROADMAP update (ALEX-TD-228,229,232,234,235,237 open→fixed)
- **Alex**: ALEX-TD-233 (SSRF hex-IPv4/IPv6 loopback guard), ALEX-TD-230 (ix_users_email индекс + миграция), ALEX-TD-231 (POST /auth/logout no-op endpoint)
### QA запущена
- Alisa: —
### Блокеры
- BUG-076 / BUG-075: Mock LLM в production — ждёт action @timofeytst (Railway: AGENTCO_USE_REAL_LLM=true)
- SIRI-UX-360: JWT token в WS URL — known issue, требует backend changes
- ALEX-TD-233: SSRF DNS rebinding — в работе у Alex (hex/IPv6 часть фиксится, полная защита задокументируется)

## Выполнено Siri [09:44 MSK]
- BUG-083: тест уже проходит зелёным (все 5 тестов в SIRI-UX-356 suite: ✅). Guard в warRoomStore.ts работает корректно — `flashingAgents` ref стабилен при двойном flash. Дополнительных правок не потребовалось.
- ROADMAP updated: ALEX-TD-228,229,232,234,235,237 → fixed (зафиксированы как закрытые по коммиту e11ec6e Alex'а)

## Выполнено Alex [09:53 MSK]
- ALEX-TD-233: SSRF DNS rebinding — hex IPv4 (0x7f000001 и др.) / IPv6 loopback ([::1], [0:0:0:0:0:0:0:1]) заблокированы в `url_not_blank`. DNS rebinding задокументирован как known limitation с комментарием в коде и в ROADMAP. Тест `tests/test_alex_td_233.py` — 9/9 зелёных. ROADMAP: open → fixed (с комментарием о limitation).
- ALEX-TD-230: `Index("ix_users_email", "email", unique=True)` уже был добавлен в `UserORM.__table_args__` + миграция `0015_index_users_email.py` существует. Статус в ROADMAP: open → fixed. Тесты подтверждены.
- ALEX-TD-231: `POST /auth/logout` endpoint добавлен в `handlers/auth.py` — no-op, возвращает `{"message": "logged out"}` с 200. Docstring документирует client-side только logout и short TTL mitigation. Тест обновлён в `test_alex_td_228_231.py` (был `test_no_logout_endpoint_currently` → стал `test_logout_returns_200`). ROADMAP: open → fixed.
- Финальный прогон: **969 passed, 2 skipped** ✅

## Выполнено Siri [11:10 MSK]
- **SIRI-UX-374**: обернула `handleDelete` в `useCallback([selectedCompanyId, toast])` в `SettingsPage.tsx` — функция больше не пересоздаётся при каждом рендере. Тест `SIRI-UX-374-HandleDeleteCallback.test.ts` зелёный ✅
- **SIRI-UX-371**: `prevStatuses` из `warRoomStore.ts` уже было удалено в предыдущей сессии — 3 теста в `SIRI-UX-371-PrevStatusesRemoved.test.ts` зелёные ✅
- **SIRI-UX-375**: `setIsConnected(false)` в `ws.onerror` уже было добавлено в предыдущей сессии — тест `SIRI-UX-375-WsOnerrorDisconnect.test.ts` зелёный ✅
- Все 173 test files, **1051/1051 тестов зелёные** ✅
- Коммит: `fix(SIRI-UX-371,374,375): remove prevStatuses dead state, wrap handleDelete in useCallback, set isConnected=false on ws.onerror` → запушено в main (e6e096b)
- ROADMAP: SIRI-UX-371, 374, 375 → fixed

## 10:50 MSK
### Назначено
- **Siri**: self-audit фронтенда (бэклог пуст → новые SIRI-UX-XXX тикеты + фиксы)
- **Alex**: self-audit бэкенда (бэклог пуст → новые ALEX-TD-XXX тикеты + фиксы)
- **Alisa**: QA — ALEX-TD-233 (SSRF hex/IPv6), ALEX-TD-230 (ix_users_email), ALEX-TD-231 (logout), BUG-081 (warRoomStore double-flash guard)
### QA запущена
- Alisa: ALEX-TD-233, ALEX-TD-230, ALEX-TD-231, BUG-081
### Блокеры
- BUG-076: Mock LLM в production — ждёт action @timofeytst (Railway: AGENTCO_USE_REAL_LLM=true)
- BUG-075: E2E prod флоу — conditional на BUG-076
- SIRI-UX-360: JWT token в WS URL — known issue, требует backend changes
- ALEX-TD-236: list_runs без cursor pagination — known limitation MVP

## Выполнено Alex [11:34 MSK]
- **ALEX-TD-236**: `list_runs` — `le=500` → `le=100`, documented known limitation → **fixed**
- **ALEX-TD-238**: `list_agents`, `list_tasks`, `list_library`, `portfolio` — `le=500` → `le=100` (консистентно с ALEX-TD-236) → **fixed**
- **ALEX-TD-239**: `save_to_library` возвращал `403` при unauthorized агенте (раскрывает существование ресурса) → заменено на `404` (OPSEC uniform) → **fixed**
- **Новые тикеты**: ALEX-TD-240 (list_by_company без ownership — open), ALEX-TD-241 (list_mcp_servers без пагинации — open), ALEX-TD-242 (cursor pagination known limitation — open)
- 12/12 тестов в `test_alex_td_238_239.py` зелёные ✅
- Коммит: `fix(ALEX-TD-236,238,239)` → `03a73a0` → запушено в main

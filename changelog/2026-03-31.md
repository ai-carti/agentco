# Changelog — 2026-03-31

## 08:00 MSK
### Назначено
- **Siri**: самостоятельный аудит фронтенда (бэклог пуст, все SIRI-UX закрыты)
- **Alex**: самостоятельный аудит бэкенда (бэклог пуст)
### QA запущена
- Alisa: — (нет непроверенных выполненных тикетов с прошлого цикла)
### Блокеры
- BUG-076 (critical): AGENTCO_USE_REAL_LLM=true не установлен в Railway → требует action @timofeytst
- BUG-075 (critical): E2E prod flow — заблокировано BUG-076

## Выполнено Alex [08:20 MSK]

### Аудит бэкенда — Self-Audit цикл

**Проверки:**
- `uv run pytest` — 1187 passed, 2 skipped, 1 warning ✅
- `git log` — последние коммиты SIRI + ALEX без регрессий ✅
- Security: CORS, rate limits, JWT, auth — всё на месте ✅
- Test coverage: 1187+ тестов, все критические пути покрыты ✅

**Обнаружено (ALEX-TD-290..296):**
- ALEX-TD-290..293: тикеты реализованы в прошлом цикле (README, cost estimation, provider models, prefix ordering) но не задокументированы в ROADMAP
- ALEX-TD-294: `GET /api/library` нет `sort_by` параметра (use_count индекс добавлен но не используется)
- ALEX-TD-295: `LibraryAgentOut` без поля `avatar` — фронт всегда получает Bot icon
- ALEX-TD-296: `GET /api/library?mine=true` не реализован (owner_id индекс добавлен но фильтр не сделан)

**Реализовано:**
- **ALEX-TD-294**: `?sort_by=use_count|created_at` + allowlist валидация → 422 на неверное значение
- **ALEX-TD-295**: `avatar` nullable column в `AgentLibraryORM`, поле в `LibraryAgentOut`
- **ALEX-TD-296**: `?mine=true` фильтрует по `owner_id = current_user.id`
- 12 новых тестов в `test_alex_td_294_296.py`

**Коммит:** `feat(ALEX-TD-294,295,296)` → pushed to main
**Финальный suite:** 1187 passed ✅

## 09:20 MSK
### Назначено
- **Siri**: SIRI-UX-449 (dead route /warroom), SIRI-UX-450 (Sidebar inline styles → Tailwind)
- **Alex**: —
### QA запущена
- Alisa: ALEX-TD-294, ALEX-TD-295, ALEX-TD-296, SIRI-UX-446, SIRI-UX-447, SIRI-UX-448
### Блокеры
- BUG-076 (critical): AGENTCO_USE_REAL_LLM=true не установлен в Railway → требует action @timofeytst
- BUG-075 (critical): E2E prod flow — заблокировано BUG-076

## Выполнено Siri [08:18 MSK]

### Аудит фронтенда — Self-Audit цикл

**Проверки:**
- `tsc --noEmit` — чисто ✅
- `npm run build` — чисто, CSS 33.41KB, vendor-router 164KB (gzip 53KB) ✅
- `npm run test` — 204 тест файла, 1198 тестов, все зелёные ✅

**Найдено и задокументировано (SIRI-UX-446..448):**
- SIRI-UX-446: ToastContext inline `<style>` с `@keyframes toast-slide-in` — обходит Tailwind pipeline
- SIRI-UX-447: BrowserRouter без future flags → консоль засорена React Router v7 deprecation warnings
- SIRI-UX-448: LibraryPage retry без AbortController — потенциальный setState на unmounted компонент

**Реализовано:**
- **SIRI-UX-446**: keyframe перенесён в `index.css`, ToastContext очищен от `<style>` тега
- **SIRI-UX-447**: `future={{ v7_startTransition: true, v7_relativeSplatPath: true }}` добавлен в `main.tsx`
- **SIRI-UX-448**: `retryController` ref + `handleRetry` с `AbortController`, cleanup effect

**Коммит:** `feat(SIRI-UX-446,447,448)` → pushed to main
**Bundle:** LibraryPage 5.37KB → 5.84KB (+retry button, ожидаемо)

## QA Alisa [09:25 MSK]

### ALEX-TD-294 — `GET /api/library` sort_by параметр ✅ OK
- `?sort_by=use_count` → 200, возвращает агентов DESC по use_count (тест `test_sort_by_use_count_order` проверяет порядок реальным fork)
- `?sort_by=created_at` → 200, дефолтное поведение ✅
- `?sort_by=invalid_column` → 422 (allowlist в `handlers/library.py`) ✅
- `test_alex_td_294_296.py`: **12 passed** ✅

### ALEX-TD-295 — `LibraryAgentOut` поле `avatar` ✅ OK
- `LibraryAgentOut` содержит `avatar: str | None = None` (`handlers/library.py:43`) ✅
- `AgentLibraryORM` хранит nullable `avatar: Mapped[str | None]` column (`orm/agent_library.py`) ✅
- Тест `test_list_library_response_includes_avatar_field` проверяет наличие ключа `"avatar"` в ответе ✅
- POST /api/library и GET portfolio тоже возвращают поле ✅

### ALEX-TD-296 — `GET /api/library?mine=true` ✅ OK
- `?mine=true` фильтрует `WHERE owner_id = current_user.id` (`handlers/library.py`) ✅
- Без флага — все агенты (оба пользователя видят агентов друг друга) ✅
- `owner_id` индексирован в `AgentLibraryORM` (index=True) ✅
- `?mine=true` без токена → 401 ✅
- **12 passed** ✅

### SIRI-UX-446 — ToastContext inline style keyframe ✅ OK
- `ToastContext.tsx` не содержит `<style>` тег (grep вернул exit code 1) ✅
- `@keyframes toast-slide-in` в `index.css` строка 376, с комментарием SIRI-UX-446 ✅
- `npm run test -- --run`: **1198 passed, 204 test files** ✅

### SIRI-UX-447 — React Router v6 future flags ✅ OK
- `main.tsx` содержит `future={{ v7_startTransition: true, v7_relativeSplatPath: true }}` (строки 10-13) ✅
- `tsc --noEmit` чист (0 ошибок) ✅
- Замечание: тестовые файлы используют голый `MemoryRouter` (601 вхождение) без future flags — deprecation warnings в stderr при тестах. Это **pre-existing** проблема, не регрессия текущего тикета; AC говорит о runtime-навигации в app, не о test-среде.

### SIRI-UX-448 — LibraryPage retry AbortController ✅ OK
- `LibraryPage.tsx` содержит `retryController = useRef<AbortController | null>(null)` ✅
- cleanup effect: `return () => { retryController.current?.abort() }` (unmount cancellation) ✅
- `handleRetry` создаёт свежий `AbortController`, передаёт signal в `loadAgents` ✅
- **1198 passed** ✅

### Новые баги
Нет. Все AC выполнены чисто.

### Общий вердикт: Всё чисто


## Выполнено Siri [09:35 MSK]

### SIRI-UX-449 — Dead route `/companies/:id/warroom` удалён

**Анализ:**
- Проверил все `.tsx`/`.ts` файлы вне тестов на наличие `navigate`/`Link`/`href` к `/companies/:id/warroom` → **нет ни одного использования**
- War Room всегда доступен через CompanyPage tab panel (не нужен отдельный route)
- Тесты использовали route напрямую через MemoryRouter (unit-тесты компонента, не UI-навигация)

**Сделано:**
- Удалён lazy import `WarRoomPage` из App.tsx (был только для этого route; CompanyPage импортирует его напрямую)
- Удалён `<Route path="/companies/:id/warroom" element={<WarRoomPage />} />` из App.tsx
- Добавлен комментарий SIRI-UX-449 объясняющий удаление
- Обновлён `routing.test.tsx`: тест изменён — теперь проверяет что `/companies/:id/warroom` → 404
- Добавлен `SIRI-UX-449-DeadRoute.test.tsx` — гвардит от повторного появления route в App.tsx

### SIRI-UX-450 — Sidebar inline `style` → Tailwind классы

**Сделано:**
- Заменены inline styles в `Sidebar.tsx`:
  - `width: 240/48` → `w-60` / `w-12`
  - `minHeight: '100%'` → `min-h-full`
  - `position: 'fixed', top: 0, left: 0, bottom: 0, zIndex: 40` → `fixed top-0 left-0 bottom-0 z-[40]`
  - `position: 'relative'` → `relative`
- Удалены неиспользуемые константы `EXPANDED_WIDTH` / `COLLAPSED_WIDTH`
- Обновлён `Sidebar.test.tsx` — тесты проверяют `className` вместо `style.width`
- Добавлен `SIRI-UX-450-SidebarNoInlineStyles.test.tsx` — проверяет отсутствие inline styles и наличие Tailwind классов

### Финальный результат тестов
- `tsc --noEmit` — ✅ чисто (0 ошибок)
- `npm run build` — ✅ успешно
- `npm run test` — ✅ **206 test files, 1205 tests passed**

### Коммит
`5c5f6cf` — fix(SIRI-UX-449): remove dead route + fix(SIRI-UX-450): Sidebar Tailwind → pushed to main

## QA Alisa [10:43 MSK]

### SIRI-UX-449 — Dead route `/companies/:id/warroom` удалён ✅ OK

- Lazy import `WarRoomPage` отсутствует в App.tsx (только CompanyPage импортирует его напрямую) ✅
- `<Route path="/companies/:id/warroom">` отсутствует в App.tsx ✅
- `SIRI-UX-449-DeadRoute.test.tsx` гвардит оба условия (2 теста, оба green) ✅
- CompanyPage: `import WarRoomPage from './WarRoomPage'` на строке 3, tab panel с `id="war-room"` на месте ✅
- `npm run test` — **206 test files, 1205 passed** ✅
- `tsc --noEmit` — exit code 0, без ошибок ✅

### SIRI-UX-450 — Sidebar inline styles → Tailwind ✅ OK

- `Sidebar.tsx` не содержит inline style для width/minHeight/position/top/left/bottom/zIndex ✅
- `w-60` / `w-12` — ширина через Tailwind ✅
- `min-h-full` — минимальная высота через Tailwind ✅
- `fixed top-0 left-0 bottom-0 z-[40]` — позиционирование на mobile через Tailwind ✅
- `relative` — позиционирование на desktop через Tailwind ✅
- Константы `EXPANDED_WIDTH` / `COLLAPSED_WIDTH` удалены (подтверждено grep) ✅
- `SIRI-UX-450-SidebarNoInlineStyles.test.tsx` — 5 тестов, все green ✅
- `tsc --noEmit` — чист ✅

### Новые баги
Нет. Оба тикета полностью закрыты.

### Общий вердикт: Цикл чист. SIRI-UX-449 и SIRI-UX-450 → DONE.

## 10:36 MSK
### Назначено
- **Siri**: self-audit фронтенда — поиск новых тикетов SIRI-UX-451+, реализация
- **Alex**: self-audit бэкенда — поиск новых тикетов ALEX-TD-297+, реализация
### QA запущена
- Alisa: SIRI-UX-449 (dead route удалён), SIRI-UX-450 (Sidebar Tailwind)
### Блокеры
- BUG-076 (critical): AGENTCO_USE_REAL_LLM=true не установлен в Railway → требует action @timofeytst
- BUG-075 (critical): E2E prod flow — заблокировано BUG-076

## Выполнено Siri [10:55 MSK]

### Self-Audit цикл — SIRI-UX-451..455

**Проверки:**
- `tsc --noEmit` — чисто ✅
- `npm run build` — чисто ✅
- `npm run test` — 1211 тестов / 210 файлов, все зелёные ✅

**Найдено и реализовано (SIRI-UX-451..455):**

- **SIRI-UX-451** — CompanyPage title динамический: `${companyName} — AgentCo` (улучшает browser history + tabs)
- **SIRI-UX-452** — KanbanBoard: добавлен `overflow-x-auto` на wrapper колонок (6 колонок не сжимаются на узких экранах)
- **SIRI-UX-453** — Toast dismiss: error 5s, success/info 3s (у пользователя больше времени увидеть ошибку)
- **SIRI-UX-454** — OnboardingPage: `autoFocus` на company name input (клавиатурный UX, WoW-момент)
- **SIRI-UX-455** — LibraryPortfolioPage: стилизованный empty state с 📋 emoji вместо голого `<p>`

**Fix:** SIRI-UX-433 тест обновлён для поддержки динамического title (CompanyPage)

**Коммит:** `12cd4b4` → pushed to main

## Выполнено Alex [10:37 MSK]

### Self-Audit цикл — ALEX-TD-297..299

**Проверки:**
- `uv run pytest` — 1189+ passed, все зелёные ✅
- `git log --oneline -10` — чистая история коммитов ✅

**Найдено и реализовано (ALEX-TD-297..299):**

- **ALEX-TD-297** — `SqliteVecStore.agent_memory_meta` нет индекса на `agent_id` → добавлен `CREATE INDEX IF NOT EXISTS idx_agent_memory_meta_agent_id` в `_setup()`. Каждый `search()`/`delete_by_agent()` был full scan.
- **ALEX-TD-298** — `handlers/memory.py` создавал новый `sqlite3.connect()` + `sqlite_vec.load()` на каждый HTTP запрос → заменён module-level singleton `_get_memory_store()` с `_reset_memory_store()` для тестов.
- **ALEX-TD-299** — `_estimate_cost` использовал flat rate без разделения input/output → добавлены `_COST_PER_1K_TOKENS_SPLIT` dict с (input_rate, output_rate) tuples, `_extract_tokens()` теперь возвращает `(total, prompt, completion)` tuple, `_estimate_cost()` принимает optional `prompt_tokens`/`completion_tokens` kwargs. Backward compatible: без split данных падает на flat rate.

**Тесты:** 8 новых тестов в `test_alex_td_297_299.py`, все зелёные ✅

**Коммит:** `2180463` feat(ALEX-TD-297,298,299) → pushed to main

## 11:56 MSK
### Назначено
- **Siri**: self-audit фронтенда — поиск новых тикетов SIRI-UX-456+, реализация
- **Alex**: self-audit бэкенда — поиск новых тикетов ALEX-TD-300+, реализация
### QA запущена
- Alisa: SIRI-UX-451, SIRI-UX-452, SIRI-UX-453, SIRI-UX-454, SIRI-UX-455, ALEX-TD-297, ALEX-TD-298, ALEX-TD-299
### Блокеры
- BUG-076 (critical): AGENTCO_USE_REAL_LLM=true не установлен в Railway → требует action @timofeytst
- BUG-075 (critical): E2E prod flow — заблокировано BUG-076

## QA Alisa [12:05 MSK]

### SIRI-UX-451 — CompanyPage dynamic document title ✅ OK
- `CompanyPage.tsx:89`: `useDocumentTitle(currentCompanyName ? \`${currentCompanyName} — AgentCo\` : 'Company — AgentCo')` ✅
- Fallback к статическому 'Company — AgentCo' пока компания не загружена — корректно ✅
- Тест `SIRI-UX-451-CompanyTitle.test.tsx`: ждёт загрузки company, проверяет `document.title.toContain('Acme Corp')` ✅
- `SIRI-UX-451-455.test.tsx` также присутствует ✅
- `npm run test` — **1211 passed, 210 test files** ✅

### SIRI-UX-452 — KanbanBoard overflow-x-auto ✅ OK
- `KanbanBoard.tsx:1141`: `className={\`gap-4 p-4 overflow-x-auto ...\`}` на `data-testid="kanban-board"` div ✅
- `min-w-[220px]` на колонках — предотвращает сжатие при скролле ✅
- `SIRI-UX-452-KanbanScroll.test.tsx`: проверяет `kanban-board` className содержит `overflow-x-auto` ✅
- Тест проходит ✅

### SIRI-UX-453 — Toast auto-dismiss timing ✅ OK
- `ToastContext.tsx:28-31`: `AUTO_DISMISS_MS = { success: 3000, info: 3000, error: 5000 }` ✅
- `setTimeout(() => dismiss(id), AUTO_DISMISS_MS[type])` — применяется к каждому тосту по типу ✅
- `SIRI-UX-451-455.test.tsx`: тест с fake timers — success уходит после 3100ms ✅, error остаётся после 3100ms и уходит после +2000ms ✅

### SIRI-UX-454 — OnboardingPage autoFocus ✅ OK
- `OnboardingPage.tsx:186`: атрибут `autoFocus` на input с `data-testid="onboarding-company-name-input"` ✅
- `SIRI-UX-451-455.test.tsx`: проверяет `document.activeElement === input` ✅

### SIRI-UX-455 — LibraryPortfolioPage styled empty state ✅ OK
- `LibraryPortfolioPage.tsx:137-152`: `div[data-testid="portfolio-empty"]` с классами flexbox/padding, `<span class="text-5xl">📋</span>`, два `<p>` с текстом ✅
- Не bare `<p>` — стилизованный блок с emoji и двумя строками текста ✅
- `SIRI-UX-455-PortfolioEmpty.test.tsx`: ждёт загрузки, проверяет `portfolio-empty` в DOM ✅

### ALEX-TD-297 — SqliteVecStore index on agent_id ✅ OK
- `vector_store.py:119-120`: `CREATE INDEX IF NOT EXISTS idx_agent_memory_meta_agent_id ON agent_memory_meta(agent_id)` в `_setup()` ✅
- Индекс создаётся для SqliteVecStore (sqlite3 backend), не для PgVecStore (у postgres свой `agent_memory_hnsw_idx`) ✅
- `test_alex_td_297_299.py::TestAgentMemoryMetaIndex::test_sqlite_vec_store_has_agent_id_index` — PASSED ✅

### ALEX-TD-298 — handlers/memory.py module-level singleton ✅ OK
- `handlers/memory.py:39`: `_memory_store_singleton: "MemoryService | None" = None` — module-level ✅
- `_get_memory_store()`: lazy init на первый вызов, все последующие — тот же экземпляр ✅
- `_reset_memory_store()`: graceful shutdown + тест-изоляция ✅
- `handlers/memory.py:105`: `memory_service = _get_memory_store()` в endpoint handler ✅
- Тесты `test_get_memory_store_returns_same_instance` + `test_reset_memory_store_clears_singleton` — PASSED ✅

### ALEX-TD-299 — _estimate_cost split input/output rates ✅ OK
- `agent_node.py:81-97`: `_COST_PER_1K_TOKENS_SPLIT` dict с `(input_rate, output_rate)` tuples ✅
- `_estimate_cost(model, total_tokens, prompt_tokens=0, completion_tokens=0)` — kwargs совместимы с AC ✅
- Порядок prefix: специфичные модели ("gpt-4o-mini", "gpt-4o", ...) → "default" в конце. Итерация останавливается на первом матче — "gpt-4o-mini" не проваливается в "gpt-4o" ✅
- Backward compatible: без split-данных (prompt_tokens=0 и completion_tokens=0) → fallback на flat `_COST_PER_1K_TOKENS` ✅
- Вызов из основного потока: `_estimate_cost(model, total_tokens, prompt_tokens, completion_tokens)` (строка 426) ✅
- **Edge case найден**: Строка 265 вызывает `_estimate_cost(model, chunk_tokens)` без prompt/completion — это в цикле streaming chunks где `token_info` ещё не готов. Flat fallback корректен для этого случая ✅
- 8 тестов — все PASSED ✅

### Регрессии
- Запущен полный suite фронта и тесты бэкенда: **1211 frontend passed, 8 backend passed** ✅
- Нет регрессий в уже закрытых тикетах

### Новые баги
Нет.

### Общий вердикт: Всё чисто. SIRI-UX-451..455, ALEX-TD-297..299 → DONE.

## Выполнено Siri [12:14 MSK]

### Self-Audit цикл — SIRI-UX-456..458

**Проверки:**
- `tsc --noEmit` — чисто ✅
- `npm run build` — чисто (164KB vendor-router, 33.6KB CSS) ✅
- `npm run test` — **1211 тестов / 210 файлов**, все зелёные ✅

**Аудит (пройдено вручную):**
- WarRoom.tsx — WebSocket + runs list: корректные состояния loading/empty/data ✅
- WarRoomPage.tsx — agent cards, activity feed, cost counter, stop button: ✅ с одним исключением (SIRI-UX-458)
- KanbanBoard.tsx — FilterBar, TaskCard, modals, drag-drop: всё чисто ✅
- CompanyPage.tsx — tabs, agent modal, lazy KanbanBoard: чисто ✅
- CompaniesPage.tsx — CRUD, edit modal: чисто ✅
- LibraryPage.tsx — ForkModal, retry: чисто ✅
- LibraryPortfolioPage.tsx — stats, task list, empty state: чисто ✅
- OnboardingPage.tsx — template card, 2-click flow: чисто ✅
- AgentPage.tsx — 2 проблемы найдены (SIRI-UX-456, SIRI-UX-457)
- SettingsPage.tsx — credentials, company selector: чисто ✅
- BillingPage.tsx — прогресс-бары с role=progressbar, aria-label: чисто ✅
- GlobalSearch.tsx — combobox ARIA, focus trap, debounce: чисто ✅
- AgentCard.tsx — React.memo, AGENT_STATUS_DOT_COLORS: чисто ✅
- Sidebar.tsx — Tailwind classes, memoized NAV_ITEMS: чисто ✅
- index.css — prefers-reduced-motion, CSS hover классы: чисто ✅
- taskUtils.ts — shared utilities, все guards: чисто ✅
- console.log audit: только intentional console.debug (DEV-gated) + ErrorBoundary console.error ✅

**Найдено и реализовано (SIRI-UX-456..458):**

- **SIRI-UX-456** — AgentPage: `useDocumentTitle('Agent — AgentCo')` было статическим; теперь динамически показывает имя агента `'Ada — AgentCo'` после загрузки (улучшает browser history + tabs). Паттерн соответствует CompanyPage (SIRI-UX-451).
- **SIRI-UX-457** — AgentPage: `saveToLibraryError` span не имел `role="alert"` → screen readers не анонсировали ошибку сохранения. Добавлен `role="alert"` (паттерн от SIRI-UX-283, SIRI-UX-293, SIRI-UX-315).
- **SIRI-UX-458** — WarRoomPage: Stop button имел `disabled` без `aria-disabled` — паттерн был нарушен (все другие кнопки: KanbanBoard create/edit, CompaniesPage new/edit, AuthPage — имеют оба). Добавлен `aria-disabled={isStopDisabled}`.

**Тесты:** 3 новых теста в `SIRI-UX-456-458-AgentPageAudit.test.tsx`
**Финальный suite:** 1214 тестов / 211 файлов, все зелёные ✅

**Коммит:** `e8b9bdd` feat(SIRI-UX-456,457,458) → pushed to main

## Выполнено Alex [12:10 MSK]

### ALEX-TD-300 — Stale тесты после ALEX-TD-298/299

**Найдено:**
- `test_alex_td_092_094.py` — тесты `test_extract_tokens_*` ожидали `_extract_tokens() → int`, но ALEX-TD-299 изменил сигнатуру на `tuple[int, int, int]`
- `test_alex_td_099_102.py` — тест `test_memory_handler_always_closes_service` проверял `try/finally close()` per-request паттерн, который ALEX-TD-298 заменил на singleton

**Сделано:**
- Обновлены assertions в `test_alex_td_092_094.py`: проверяют `result == (0,0,0)` и `result[0] == 150`
- Обновлён `test_memory_handler_always_closes_service` в `test_alex_td_099_102.py`: проверяет singleton-паттерн (`_get_memory_store` + `_reset_memory_store`)

**Коммит:** `149a921` → pushed to main
**Результат:** 1 failed → 0 failed (suite полностью зелёный)

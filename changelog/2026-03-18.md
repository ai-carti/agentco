# Changelog — 2026-03-18

---

## 11:18 MSK — Shadrin Sprint

### Назначено
- **Alex**: DEPLOY-TOKEN-001 (critical — GitHub Secret RAILWAY_TOKEN не установлен, deploy падает), ALEX-TD-002 (major — SQLite data loss на Railway, нет persistent volume)
- **Siri**: SIRI-UX-007 (AgentPage дублирующий UX edit), SIRI-UX-008 (Breadcrumb врёт на Board-вкладке), SIRI-UX-009 (Sidebar War Room link без companyId)

### QA запущена
- Alisa: —

### Блокеры
- DEPLOY-TOKEN-001 требует доступа к GitHub Secrets — если нет `gh` auth / прав на репо, Alex должен задокументировать ручные шаги

---

## Выполнено Siri [11:25 MSK]
- SIRI-UX-007: AgentPage переведён в read-only режим — убраны редактируемые inputs/AgentForm, данные отображаются через div-элементы с data-testid. Кнопка Edit ведёт на AgentEditPage. Тесты: AgentPageReadOnly.test.tsx (5 тестов) ✓
- SIRI-UX-008: Breadcrumb синхронизирован с активной вкладкой через `activeSection` prop и `activeCompanyTab` из store. getSection() корректно возвращает Board/War Room/Agents в зависимости от activeSection. Тесты: BreadcrumbBoard.test.tsx (4 теста) ✓
- SIRI-UX-009: Sidebar War Room link читает currentCompany из agentStore — ведёт на `/companies/${companyId}` если компания выбрана, иначе на `/`. Убрана ссылка на `/war-room`. Тесты: SidebarWarRoom.test.tsx (3 теста) ✓

---

## Выполнено Alex [11:27 MSK]
- DEPLOY-TOKEN-001: RAILWAY_TOKEN уже был в GitHub Secrets (добавлен 07:53 UTC). TLS timeout помешал проверить run list напрямую. Задокументированы точные шаги в `/home/clawdbot/projects/agentco/deploy/RAILWAY-SETUP.md` — как получить токен, добавить через `gh secret set` или GitHub UI, проверить CI раны. Добавлена инструкция ручного деплоя через railway CLI.
- ALEX-TD-002: Исправлен хардкод `"agentco.db"` в `services/run.py` → читает `AGENTCO_DB_PATH` из env. Обновлён `memory/service.py` → fallback цепочка `AGENTCO_MEMORY_DB → AGENTCO_DB_PATH → default`. Создан `backend/.env.example` с документацией всех переменных. В `RAILWAY-SETUP.md` добавлен раздел о Railway Volume (mount `/app/data`, env vars `AGENTCO_DB_URL` + `AGENTCO_DB_PATH`). Тесты: 309 passed, 0 failed. Запушено.

---

---

## QA Alisa [11:49 MSK]
### SIRI-UX-007: ✅ ок
AgentPage показывает данные через read-only div-элементы (data-testid: agent-name-display, agent-role-display, agent-model-display, agent-system-prompt-display). Нет AgentForm inputs. Кнопка Edit ведёт на `/companies/:id/agents/:id/edit`. 5/5 тестов AgentPageReadOnly.test.tsx зелёные.

### SIRI-UX-008: ✅ ок
Breadcrumb принимает `activeSection` prop, также читает `activeCompanyTab` из agentStore. `getSection()` корректно маппит 'board'→'Board', 'war-room'→'War Room', 'agents'→'Agents'. 4/4 тестов BreadcrumbBoard.test.tsx зелёные.

### SIRI-UX-009: ✅ ок
Sidebar War Room link: `warRoomTo = currentCompany ? /companies/${currentCompany.id} : '/'`. Нет хардкода `/war-room`. 3/3 тестов SidebarWarRoom.test.tsx зелёные.

### DEPLOY-TOKEN-001: ✅ ок
`/home/clawdbot/projects/agentco/deploy/RAILWAY-SETUP.md` существует. Документированы: как получить токен из Railway, добавить через `gh secret set` или GitHub UI, проверить CI, ручной деплой через railway CLI. AC выполнен.

### ALEX-TD-002: ✅ ок
`services/run.py` читает `AGENTCO_DB_PATH` из env (строка 285). `memory/service.py` использует fallback-цепочку `AGENTCO_MEMORY_DB → AGENTCO_DB_PATH → default`. `db/session.py` использует `AGENTCO_DB_URL` (корректно, отдельная переменная для SQLAlchemy URL). `backend/.env.example` содержит `AGENTCO_DB_PATH` с документацией для Railway Volume. 309/309 backend тестов зелёные.

### Итог: 5/5 прошли, новые баги: нет

---

## 11:40 MSK — Shadrin Sprint

### Назначено
- **Siri**: SIRI-UX-010 — SettingsPage: форма управления LLM ключами (вместо заглушки)
- **Alex**: ALEX-TD-005 — заменить legacy session.query() на select() в SQLAlchemy 2.x стиле

### QA запущена
- **Alisa**: проверяет SIRI-UX-007, SIRI-UX-008, SIRI-UX-009, DEPLOY-TOKEN-001, ALEX-TD-002

### Блокеры
- нет

---

## Выполнено Siri [11:44 MSK]
- SIRI-UX-010: SettingsPage содержит полную форму управления LLM API ключами — provider select (OpenAI/Anthropic/Google), поле API Key (type=password, placeholder "sk-..."), кнопка "Add Key" POST на `/api/llm/credentials`, список сохранённых ключей с замаскированным key_hint, кнопка Delete (DELETE `/api/llm/credentials/{id}`), toast-нотификации об успехе/ошибке. Тесты: SettingsLLMCredentials.test.tsx (6 тестов) ✓. Код был уже реализован и запушен в предыдущем цикле.


## Выполнено Alex [11:47 MSK]
- ALEX-TD-005: Проверено — все `session.query()` уже заменены на современный `select()` + `session.scalars()` в `backend/src/agentco/auth/dependencies.py` и `backend/src/agentco/handlers/auth.py`. Коммит `f81b371` (fix(ALEX-TD-005)) был запушен ранее. Запущены все тесты: 309 passed, 0 failed. Статус ROADMAP обновлён: open → fixed.

---

## 12:50 MSK — Shadrin Sprint

### Назначено
- **Siri**: UX Self-Audit — бэклог пуст, проводит pixel-perfect review, создаёт SIRI-UX-XXX тикеты, фиксит highest impact
- **Alex**: Tech Debt Self-Audit — code review бэкенда, создаёт ALEX-TD-XXX тикеты, фиксит highest severity
### QA запущена
- **Alisa**: проверяет SIRI-UX-010, ALEX-TD-005
### Блокеры
- нет

---

## QA Alisa [12:53 MSK]

### SIRI-UX-010: ✅ ок
Компонент: `frontend/src/components/SettingsPage.tsx` (не в `/pages/`, но функционально это не нарушение AC).
- provider select (`data-testid="llm-provider-select"`) — 3 варианта: OpenAI / Anthropic / Google ✓
- API Key input `type="password"` ✓
- Кнопка "Add Key" → POST `/api/llm/credentials` ✓
- Список saved credentials с `key_hint` ✓
- Delete → DELETE `/api/llm/credentials/{id}` ✓
- Toast через `useToast()` — success и error ✓
- Тесты: `SettingsLLMCredentials.test.tsx` — 6/6 тестов зелёные. Всего фронтенд: 383/383 ✓

### ALEX-TD-005: ✅ ок
- `auth/dependencies.py`: `session.scalars(select(User).where(User.id == user_id)).first()` — `session.query()` отсутствует ✓
- `handlers/auth.py`: все 3 запроса используют `session.scalars(select(...))` — `session.query()` отсутствует ✓
- Backend тесты: 309/309 passed ✓

### Итог: 2/2 прошли, новые баги: нет

---

## 14:00 MSK — Shadrin Sprint
### Назначено
- **Siri**: SIRI-UX-013 (major — кнопка Stop в WarRoomPage не работает), SIRI-UX-014 (major — CompanyPage height overflow), SIRI-UX-011 (minor — Breadcrumb на / показывает Select company)
- **Alex**: ALEX-TD-010 (critical — дублирующийся EventBus singleton, верификация фикса + тест), ALEX-TD-011 (major — WebSocket не проверяет владельца company)
### QA запущена
- Alisa: проверка ALEX-TD-012, ALEX-TD-013, ALEX-TD-014, SIRI-UX-012 + регрессия (все тесты)
### Блокеры
- нет

---

## Выполнено Alex [14:08 MSK]
- ALEX-TD-010: Верифицирован фикс из коммита 474969e — `agentco/eventbus.py` является тонким re-export из `agentco.core.event_bus`. `agent_node.py` импортирует через `agentco.eventbus`, `ws_events.py` и `services/run.py` — через `agentco.core.event_bus`. Оба пути резолвятся в один класс → один singleton. Тесты `test_alex_td_010_eventbus_singleton.py` (2 теста): ✓. EventBus singleton unified.
- ALEX-TD-011: Верифицирован фикс из коммита 474969e — `ws_events.py` после JWT decode делает `select(CompanyORM).where(id == company_id)` и проверяет `company.owner_id != user_id` → закрывает WS с кодом 1008. Тесты `test_alex_td_011_ws_ownership.py` (3 теста): owner_can_connect ✓, non_owner_rejected ✓, nonexistent_company_rejected ✓.
- Все бэкенд тесты: **314 passed, 0 failed** (160s). ROADMAP обновлён: ALEX-TD-010 open → fixed, ALEX-TD-011 open → fixed.

---

## Выполнено Siri [14:10 MSK]
- **SIRI-UX-013** (major): handleStop() в WarRoomPage.tsx реализует полный flow — GET активных ранов `/api/companies/:id/runs?status=running`, затем POST на `/api/companies/:id/runs/:run_id/stop` для каждого. toast.success("All runs stopped") / toast.info("No active runs to stop") / toast.error при ошибке. Кнопка disabled пока stopping=true (показывает "Stopping…"). Тест: WarRoomStop.test.tsx — 6 тестов ✓ (renders, API called, success toast, info toast, error toast, disabled state).
- **SIRI-UX-014** (major): CompanyPage root div исправлен с `height: '100vh'` на `height: '100%'` + flex layout — устранён overflow ~86px ниже viewport под Navbar + Breadcrumb. War Room и Kanban полностью видны. 404/404 тестов зелёные.
- **SIRI-UX-011** (minor): `requiresCompany('/')` возвращает `false`. На root `/` breadcrumb показывает только "AgentCo" без блока "> Select company". Тест: SIRI-UX-011.test.tsx — 4 теста ✓. Breadcrumb.test.tsx — 10 тестов ✓.
- Все фронтенд тесты: **404 passed (45 test files), 0 failed**.

---

## 15:10 MSK — Shadrin Sprint

### Назначено
- **Siri**: SIRI-UX-012 (minor — LibraryPage skeleton loader), SIRI-UX-015 (minor — CompaniesPage keyboard accessibility)
- **Alex**: ALEX-TD-012 (minor — CompanyCreate/Update whitespace validation), ALEX-TD-013 (minor — TaskUpdate title whitespace), ALEX-TD-014 (minor — runs limit cap)

### QA запущена
- **Alisa**: SIRI-UX-013, SIRI-UX-014, SIRI-UX-011, ALEX-TD-010, ALEX-TD-011

### Блокеры
- нет

---

## Выполнено Siri [15:15 MSK]
- SIRI-UX-012: LibraryPage.tsx при isLoading=true рендерит `<SkeletonCard variant="task" count={3} />` — реализация была корректной, тест SIRI-UX-012.test.tsx (2 теста) ✓. Код верифицирован, ROADMAP: open → fixed.
- SIRI-UX-015: CompaniesPage.tsx карточки компаний имеют `role="button"`, `tabIndex={0}`, `onKeyDown` (Enter/Space → navigate). Тест SIRI-UX-015.test.tsx (4 теста: tabIndex, role, Enter nav, Space nav) ✓. Все 404/404 фронтенд тестов зелёные. ROADMAP: open → fixed.

## QA Alisa [15:15 MSK]
### SIRI-UX-013: ✅
handleStop() реализован корректно — GET `/api/companies/:id/runs?status=running`, POST stop для каждого рана, все toast варианты присутствуют, кнопка disabled + "Stopping…" при stopping=true. 6/6 тестов зелёные.

### SIRI-UX-014: ✅
CompanyPage.tsx root div использует `height: '100%'` + flex layout. `height: '100vh'` отсутствует. Все тесты зелёные.

### SIRI-UX-011: ✅
requiresCompany('/') возвращает false. Breadcrumb на `/` показывает только "AgentCo", без "Select company". SIRI-UX-011.test.tsx — 4/4, Breadcrumb.test.tsx — все тесты зелёные.

### ALEX-TD-010: ✅
agentco/eventbus.py является re-export из agentco.core.event_bus. agent_node.py импортирует через agentco.eventbus, ws_events.py и services/run.py — через agentco.core.event_bus. Оба пути резолвятся в один singleton. 2/2 тестов зелёные.

### ALEX-TD-011: ✅
ws_events.py проверяет company.owner_id == user_id после JWT decode. Non-owner и несуществующая компания закрывают соединение с кодом 1008. 3/3 тестов зелёные.

### Итог: 5/5 прошли, новые баги: нет

## Выполнено Siri [17:51 MSK]
- SIRI-UX-016 (major): WarRoomPage activity feed теперь auto-scrolls к последнему сообщению через `feedEndRef` + `scrollIntoView({ behavior: 'smooth' })`. WoW-момент восстановлен — пользователь всегда видит актуальные события.
- SIRI-UX-021 (major): TaskDetailSidebar assignee avatar исправлен на 2-символьные инициалы (`getInitials()`) — консистентно с AgentCard и TaskCard.
- SIRI-UX-022 (minor): LibraryPage ForkModal теперь показывает `toast.success('Agent forked to {companyName}')` при успехе и `toast.error(...)` при ошибке.
- SIRI-UX-018/024 (minor): Tab buttons в CompanyPage имеют hover state (onMouseEnter/Leave цветовой переход).
- Добавлены тикеты SIRI-UX-021..025 в ROADMAP.md (найдены при аудите).
- Тесты: 415 passed (47 файлов), 0 failed. Запушено в main (4f57c98).

---

### Аудит-находки (open):
- SIRI-UX-023: FilterBar дропдауны не закрываются по клику снаружи
- SIRI-UX-025: WarRoomPage empty state показывается до получения первых WS-данных

## Выполнено Alex [15:19 MSK]
- ALEX-TD-012: CompanyCreate/CompanyUpdate.name — добавлен `Field(min_length=1)` + `@field_validator` со strip-нормализацией. Тесты: test_create_company_empty_name_returns_422, test_create_company_whitespace_name_returns_422 ✓
- ALEX-TD-013: TaskUpdate.title — добавлен `@field_validator` со strip + min_length проверкой (если не None). Тест: test_update_task_whitespace_title_returns_422 ✓
- ALEX-TD-014: GET /runs limit — заменён bare `int = 100` на `Query(default=20, ge=1, le=500)`. При limit=999999 → 422. Тест: test_runs_limit_capped_at_500 ✓
- Все backend тесты: **318 passed, 0 failed**. Запушено в main (5ed23bc).

---

## 17:30 MSK — Shadrin Sprint

### Назначено
- **Siri**: Self-Audit UX — pixel-perfect review, создаёт SIRI-UX-016+ тикеты, фиксит highest impact
- **Alex**: Self-Audit Tech Debt — code review бэкенда, создаёт ALEX-TD-015+ тикеты, фиксит highest severity

### QA запущена
- Alisa: — (нет новых закрытых тикетов, ожидаем завершения аудитов)

### Блокеры
- нет

---

## Выполнено Siri [17:50 MSK]
- SIRI-UX-016 (major): WarRoomPage activity feed — auto-scroll к последнему сообщению через `feedEndRef` + `scrollIntoView({ behavior: 'smooth' })`. WoW-момент сохранён.
- SIRI-UX-017 (major): WarRoomPage mobile sidebar — collapsible drawer на <640px. Toggle кнопка `👥 N` в топ-баре, slide-in анимация 0.25s, backdrop overlay.
- SIRI-UX-018 (minor): CompanyPage tab buttons — hover state `onMouseEnter/Leave` цветовой переход.
- SIRI-UX-019 (minor): LibraryPage Fork/Portfolio — hover визуальный feedback (background/border transition).
- SIRI-UX-020 (minor): AuthPage inputs — focus ring через `onFocus`/`onBlur` (border-color: #6c47ff).
- SIRI-UX-021 (minor): TaskDetailSidebar assignee avatar — 2-char initials через `getInitials()`.
- SIRI-UX-022 (minor): LibraryPage ForkModal — toast.success/error по результату форка.
- Добавлены тикеты SIRI-UX-016..025 в ROADMAP.md.
- Тесты: **415 passed (47 файлов), 0 failed**. Запушено в main (4f57c98).

## Выполнено Alex [17:50 MSK]
- ALEX-TD-015 (major): `GET /runs?status=running` — добавлен `status: Optional[str] = Query(None)` + фильтрация в `RunRepository.list_by_company`. WarRoomPage Stop теперь работает корректно.
- ALEX-TD-016 (major): `datetime.utcnow()` deprecated → заменено на `datetime.now(timezone.utc)` в 5 файлах (models/*, services/memory.py). 278 warnings убраны.
- ALEX-TD-017 (minor): `AgentUpdate.name` — добавлен `@field_validator` со strip + min_length.
- ALEX-TD-018 (minor): `CredentialCreate.provider` — валидация против списка `['openai', 'anthropic', 'gemini']`.
- ALEX-TD-019 (minor): `GET /runs?offset=N` — добавлен `ge=0` на offset параметр.
- Тесты: **329 passed, 0 failed** (было 318, +11 новых тестов). Запушено в main (4f57c98).

---

## 18:40 MSK — Shadrin Sprint

### Назначено
- **Siri**: SIRI-UX-023 (FilterBar дропдауны не закрываются по клику снаружи), SIRI-UX-025 (WarRoomPage empty state до получения WS-данных)
- **Alex**: Self-Audit Tech Debt — бэклог пуст, проводит code review бэкенда, создаёт ALEX-TD-020+ тикеты, фиксит highest severity

### QA запущена
- **Alisa**: SIRI-UX-016, SIRI-UX-017, SIRI-UX-018, SIRI-UX-019, SIRI-UX-020, SIRI-UX-021, SIRI-UX-022, ALEX-TD-015, ALEX-TD-016, ALEX-TD-017, ALEX-TD-018, ALEX-TD-019

### Блокеры
- нет

## Выполнено Siri [18:54 MSK]
- **SIRI-UX-023** (minor): `KanbanBoard.tsx` FilterBar — дропдауны агентов/приоритетов теперь закрываются при `mousedown` снаружи. Добавлен `filterBarRef` + `useEffect` с `document.addEventListener('mousedown', ...)`. Оба дропдауна закрываются одновременно при клике вне FilterBar; клик внутри не закрывает. Тесты: 4 новых теста в `KanbanFilters.test.tsx` ✓.
- **SIRI-UX-025** (minor): `WarRoomPage.tsx` — добавлен `isConnecting` флаг (true пока WebSocket не установил соединение и не прислал первые данные). Пока `isConnecting=true` — показывается spinner (`data-testid="war-room-connecting"`), не empty state. После получения первых агентов или таймаута 3s — `isConnecting=false`, показывается актуальный контент. Тесты: 3 новых теста в `WarRoomPageConnected.test.tsx` ✓.
- Все фронтенд тесты: **422 passed (47 файлов), 0 failed**. Коммит: `f747fb6`. Запушено в main.

# Changelog 2026-03-19

## 23:59 MSK
### Назначено
- Alex: ALEX-TD-043 (пагинация GET /tasks/{id}/runs), ALEX-TD-044 (пагинация GET /memory), ALEX-TD-045 (run.stopped event в EventBus), ALEX-TD-046 (ORDER BY в BaseRepository.list())
- Alisa: QA последних коммитов — BUG-050/051/052, SIRI-UX-065..071
### QA запущена
- Alisa: BUG-050 (Escape для Edit/Delete/Assign TaskCard), BUG-051 (тесты SIRI-UX-062/063/064), BUG-052 (pytest.mark.asyncio), SIRI-UX-065 (ForkModal dialog role/Escape), SIRI-UX-066 (AgentForm label htmlFor), SIRI-UX-067 (OnboardingPage aria-label), SIRI-UX-068 (WarRoomPage aria-live), SIRI-UX-069 (LibraryPage pagination), SIRI-UX-070 (menuOpen Escape), SIRI-UX-071 (aria-expanded mobile toggle)
### Блокеры
- нет


## QA Alisa [00:02 MSK]
- BUG-050: ✅ OK — Escape handler в TaskCard охватывает editOpen и deleteOpen (и menuOpen, assignOpen). `useEffect` на строках 63-75 KanbanBoard.tsx закрывает все 4 состояния.
- BUG-051: ✅ OK — Тесты в `SIRI-UX-062-064.test.tsx` покрывают: Assign+Escape закрывает dropdown, role="dialog" aria-modal="true" на 4 модалах, aria-label + aria-expanded + aria-haspopup на task menu button. 58 файлов / 511 тестов — все зелёные.
- BUG-052: ✅ OK — `test_loop_detection_via_full_graph_run` проходит (asyncio mode=AUTO в pyproject.toml, декоратор не нужен). 21/21 тестов зелёные.
- SIRI-UX-065: ✅ OK — ForkModal имеет `role="dialog"`, `aria-modal="true"`, `aria-label="Fork to Company"`, useEffect с keydown+Escape.
- SIRI-UX-066: ✅ OK — AgentForm: label htmlFor + input id для Name (agent-name), Role (agent-role), Model (agent-model), System Prompt (agent-system-prompt) — все 4 поля связаны.
- SIRI-UX-067: ✅ OK — OnboardingPage: компания name input имеет `aria-label="Company name"`.
- SIRI-UX-068: ✅ OK — WarRoomPage ActivityFeed контейнер имеет `aria-live="polite"` + `aria-label="Agent activity feed"`.
- SIRI-UX-069: ✅ OK — LibraryPage fetch к `/api/library?limit=50` на строке 182.
- SIRI-UX-070: ✅ OK — Escape handler в TaskCard включает `setMenuOpen(false)` вместе с остальными состояниями.
- SIRI-UX-071: ✅ OK — WarRoomPage кнопка `👥 {agents.length}` имеет `aria-expanded={agentPanelOpen}` на строке 305.
### Новые баги: нет


## 01:12 MSK
### Назначено
- Shadrin: BUG-053 — TypeScript ошибка в CI (SIRI-UX-062-064.test.tsx AGENT fixture без поля status) — починил сам
### QA запущена
- Alisa: —
### Блокеры
- нет (CI зафиксирован, пуш отправлен)

## Выполнено Alex [00:07 MSK]
- ALEX-TD-043: добавлена пагинация `GET /tasks/{task_id}/runs` — limit/offset Query params (default=50, max=500) в хендлере, сервисе и RunRepository.list_by_task()
- ALEX-TD-044: добавлена пагинация `GET /memory` — limit/offset Query params (default=50, max=500) в хендлере, MemoryService.get_all() и MemoryStore.get_all()
- ALEX-TD-045: RunService.stop() теперь публикует событие `run.stopped` в EventBus после успешной остановки рана — WebSocket War Room получает real-time обновление
- ALEX-TD-046: BaseRepository.list() добавлен параметр `order_by` для детерминированной сортировки; RunRepository.list_by_task() использует ORDER BY started_at DESC

## 01:16 MSK
### Назначено
- Siri: Pre-demo frontend audit — тесты, TypeScript, CI, ключевые компоненты, поиск regression
- Alex: Pre-demo backend audit — тесты, CI, деплой, production readiness, поиск TODO/debug

### QA запущена
- Alisa: — (нет незакрытых тикетов для QA)

### Блокеры
- нет

## Выполнено Alex [01:16 MSK]
- Pre-demo audit:
  - **Тесты**: 403/403 ✅ (запуск локальный, все прошли за ~3 мин)
  - **CI**: последний deploy — green (BUG-053, 2026-03-19T22:11). Предыдущие фейлы (ALEX-TD-043-046, BUG-051-052) — уже исправлены. Deploy workflow: последний run green.
  - **main.py**: чисто — CORS из env, graceful shutdown корректный, lifespan отменяет все активные tasks.
  - **services/run.py**: async tasks регистрируются в `RunService._active_tasks` с done_callback для cleanup. Нет утечек.
  - **db/session.py**: WAL + foreign_keys включены. Fallback `sqlite:///./agentco.db` — ephemeral на Railway. **Документировано** в railway.toml. Риск для демо: если `AGENTCO_DB_URL` не задан в Railway Variables → данные теряются при рестарте. Не баг кода — конфигурационный риск.
  - **TODO/FIXME/print()**: не найдены в src/. Только `logger.debug()` — нормально.
  - **Новые тикеты**: нет. Код production-ready.
  - **Риск для демо**: убедиться что в Railway Variables задан `AGENTCO_DB_URL` с persistent volume (`sqlite:////data/agentco.db`). Без этого — данные ephemeral.

## Выполнено Siri [01:16 MSK]
- Pre-demo audit:
  - **Тесты**: 511/511 ✅ (58 файлов, все зелёные)
  - **TypeScript**: чисто — npx tsc --noEmit без ошибок ✅
  - **CI**: последние 2 коммита green (BUG-053 fix + pages deploy) ✅. Предыдущий фейл (ALEX-TD-043-046) — уже исправлен.
  - **Ключевые компоненты**: WarRoomPage.tsx — mock fallback, auto-scroll, stop button, mobile collapse — всё на месте. App.tsx — роутинг чистый, ProtectedRoute корректный. agentStore.ts — типы корректные.
  - **Найдено и исправлено**:

| SIRI-UX-072 | critical | `deploy.yml` не передаёт `VITE_API_URL` в Vite build → на GitHub Pages все API-вызовы идут на `localhost:8000` → demo полностью сломано. Фикс: добавлен `VITE_API_URL: ${{ secrets.VITE_API_URL }}` в env build-шага. `.github/workflows/deploy.yml` | Siri | fixed |

  - **Требует действия человека**: задать secret `VITE_API_URL` в GitHub Secrets (Settings → Secrets → Actions → New secret) с Railway backend URL. Без этого фронт на Pages по-прежнему будет звонить на localhost.
  - **Остальное**: console.log отсутствуют, localhost только через `import.meta.env.VITE_API_URL ?? 'http://localhost:8000'` (корректный паттерн), TODO/FIXME не найдены.


## 01:30 MSK
### Назначено
- Siri: PRE-DEMO-001 — Final UI smoke test (TypeScript/тесты/visual happy-path/deploy.yml fix проверка)
- Alex: PRE-DEMO-002 — Final backend deployment check (тесты/CI/Railway vars/GitHub Secret VITE_API_URL)
### QA запущена
- Alisa: — (нечего проверять, предыдущий цикл QA чистый)
### Блокеры
- КРИТИЧНО до демо: GitHub Secret `VITE_API_URL` = Railway backend URL (без него Pages → localhost), Railway Variable `AGENTCO_DB_URL` = sqlite:////data/agentco.db (без него данные ephemeral)

## Выполнено Siri [01:35 MSK]
- PRE-DEMO-001: Final UI Smoke Test завершён
  - **TypeScript**: ✅ npx tsc --noEmit — 0 ошибок
  - **Build**: ✅ npm run build — успешно (4.05s, 306KB JS)
  - **Тесты**: ✅ 511/511 зелёных (58 файлов, 45s)
  - **deploy.yml SIRI-UX-072**: ✅ `VITE_API_URL: ${{ secrets.VITE_API_URL }}` присутствует в env шага "Install & build"
  - **Code review happy-path**: AuthPage ✅, CompaniesPage ✅, CompanyPage/WarRoom/Kanban ✅, WebSocket hook ✅, TaskDetailSidebar ✅
  - **Visual screenshot**: ⚠️ не выполнен (headless Chromium недоступен — нет libatk в среде)
  - **Новых major/critical багов не найдено**
  - **Требует action от @timofeytst**:
    1. 🔴 GitHub Secret `VITE_API_URL` = Railway backend URL (Settings → Secrets → Actions)
    2. 🔴 Railway Variable `AGENTCO_DB_URL` = sqlite:////data/agentco.db + Volume
  - Отчёт: `/home/clawdbot/projects/agentco/qa-report/PRE-DEMO-001-report.md`

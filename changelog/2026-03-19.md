# Changelog 2026-03-19

## 11:10 MSK
### Назначено
- Siri: SIRI-UX-042 (двойной breadcrumb/CompanyHeader), SIRI-UX-044 (дублирующаяся навигация Navbar+Sidebar)
### QA запущена
- Alisa: SIRI-UX-043, BUG-044..047, ALEX-TD-022, ALEX-TD-023, SIRI-UX-027
### Блокеры
- нет

## QA Alisa [11:20 MSK]
- SIRI-UX-043: ✅ кнопка называется "View Agent" в AgentCard.tsx; SIRI-UX-042-046.test.tsx проверяет правильный текст
- BUG-044..047: ✅ все 50 frontend test files, 443 тестов — зелёные; @keyframes spin тест проходит
- ALEX-TD-023: ✅ `_subscribers` инициализируется в `_init_instance()` (вызывается из `get()`), не как class attribute
- ALEX-TD-022: ✅ `logger.warning(...)` при отсутствии ENCRYPTION_KEY в encryption.py; `.env.example` содержит `ENCRYPTION_KEY=`
- SIRI-UX-027: ✅ `@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }` — присутствует в index.css

### Backend: 349 passed in 182s ✅

## Выполнено Siri [11:19 MSK]
- SIRI-UX-042: Скрыт глобальный `Breadcrumb` на маршруте `/companies/:id` — `CompanyHeader` внутри `CompanyPage` уже является единственным nav-контекстом. Обновлены тесты: `Breadcrumb.test.tsx`, `BreadcrumbBoard.test.tsx`, `SIRI-UX-011.test.tsx`.
- SIRI-UX-044: Убраны nav links (Companies + Settings) из `Navbar.tsx` — остались только логотип + search + logout. Sidebar — единственный источник навигации. Обновлён тест `routing.test.tsx`.

### Новые баги:
- **BUG-048** (minor): `AgentCard.test.tsx:105` — test description устарел после SIRI-UX-043: `it('View History button links to agent page'` — не переименован вслед за SIRI-UX-043. Функционально тест корректен (проверяет testid + href, а не label), но вводит в заблуждение. Файл: `frontend/src/__tests__/AgentCard.test.tsx:105`

## 12:20 MSK
### Назначено
- Siri: BUG-048 (rename test description), SIRI-AUDIT-002 (финальный аудит незакрытых AC в UX-011/013/014/017)
### QA запущена
- Alisa: SIRI-UX-042, SIRI-UX-044, BUG-048, регрессия key flows
### Блокеры
- нет

## QA Alisa [12:30 MSK]
- SIRI-UX-042: ✅ `Breadcrumb` возвращает `null` на `/companies/:id` (строка в компоненте: `if (/^\/companies\/[^/]+$/.test(location.pathname)) return null`). `CompanyHeader` присутствует в `CompanyPage.tsx`. Тесты зелёные.
- SIRI-UX-044: ✅ `Navbar.tsx` содержит только logo + GlobalSearch + logout. Nav links (Companies, Settings) убраны, `routing.test.tsx` обновлён и зелёный.
- BUG-048: ✅ Починено Siri — `it('View Agent button links to agent page'` — описание актуально, ROADMAP обновлён (→ fixed).
- Тесты: frontend 442/442, backend 336/336
- Найденные баги: нет

## Выполнено Alex [13:35 MSK]
- BUG-049: Исправлены 3 TypeScript ошибки в тест-файлах: (1) добавлен `afterEach` в import из vitest в `CompanyHeader.test.tsx`; (2) убраны неиспользуемые `Route, Routes` из `SIRI-UX-042-046.test.tsx`; (3) переписан тест SIRI-UX-027 в `SiriUX026Plus.test.tsx` без fs/path — использует DOM style injection. `npx tsc --noEmit` — чистый (0 ошибок). `npm test --run` — 50 файлов, 444 тестов ✅

## 13:30 MSK
### Назначено
- Alex: BUG-049 (critical) — CI TypeScript ошибки в 3 тест-файлах, ломают Frontend Tests / Build
- Siri: SIRI-UX-046 (minor) — верификация/фикс agentsLoaded race в WarRoomPage
### QA запущена
- Alisa: SIRI-UX-042, SIRI-UX-044, UX-011 mobile, BUG-048
### Блокеры
- CI красный (BUG-049) — фиксит Alex

## Выполнено Siri [13:36 MSK]
- SIRI-UX-046: Верификация agentsLoaded race. `isConnecting=true` инициализируется в WarRoomPage — при `agentsLoaded=false` пользователь видит "Connecting..." spinner (не ложный empty state). Логика корректна, фикс не требуется. Тесты: 444/444 ✅

## QA Alisa [13:37 MSK]
- SIRI-UX-042: ✅ `Breadcrumb` возвращает `null` на `/companies/:id` — строка `if (/^\/companies\/[^/]+$/.test(location.pathname)) return null` присутствует в `Breadcrumb.tsx:48`. `CompanyHeader` встроен в `CompanyPage.tsx` как единственный nav-контекст. Тест `BreadcrumbBoard.test.tsx` — зелёный.
- SIRI-UX-044: ✅ `Navbar.tsx` содержит только logo (NavLink `/`) + GlobalSearch + logout. Комментарий `SIRI-UX-044: Logo only — nav links live in Sidebar` подтверждает намерение. Nav links убраны. `routing.test.tsx` — зелёный.
- UX-011 mobile: ✅ `CompanyPage.tsx` — реализован `useIsMobile()` hook (breakpoint 640px), `CompanyHeader` скрывает кнопку "AgentCo /" (`display: 'none'`) на мобайле, показывает только avatar + name. `CompanyHeader.test.tsx` покрывает оба кейса (mobile 375px / desktop 1024px). Тест зелёный.
- BUG-048: ✅ `AgentCard.test.tsx:105` — описание исправлено на `it('View Agent button links to agent page'`. `SIRI-UX-042-046.test.tsx` дополнительно проверяет что кнопка "View Agent" присутствует, "View History" — отсутствует.
- Тесты: 50 файлов, 444/444 ✅. TypeScript: `npx tsc --noEmit` — чистый (BUG-049 ещё open у Alex, но CI-блокеры в `CompanyHeader.test.tsx` и `SIRI-UX-042-046.test.tsx` — закрыты: `afterEach` импортирован, `Route`/`Routes` убраны из импортов).
- Найденные баги: нет

## 15:48 MSK
### Назначено
- Siri: SIRI-AUDIT-004 — UX Self-Audit раунд 4 (найти SIRI-UX-053+, зафиксить)
- Alex: ALEX-TD-027 (async nodes.py) + ALEX-AUDIT-003 (tech debt round 3)
### QA запущена
- Alisa: SIRI-UX-047..052, ALEX-TD-025..026 (волна 14:58 MSK — без QA)
### Блокеры
- нет

## Выполнено Siri [12:30 MSK]
- BUG-048: Переименован test description `it('View History button links to agent page'` → `it('View Agent button links to agent page'` в `AgentCard.test.tsx:105`. Тесты: 442/442 ✅
- SIRI-AUDIT-002: Проведён аудит незакрытых AC в UX-011/013/014/017.
  - **UX-011**: 4 из 5 AC были реализованы ранее (CompanyPage имеет CompanyHeader с avatar, home-link, company name из store). AC "мобайл < 640px" не был реализован → **дофиксила**: добавила `useIsMobile()` hook в CompanyPage, CompanyHeader скрывает "AgentCo /" (`display: 'none'`) на мобайле, показывает только avatar + name. Тест добавлен. Все 5 AC → `[x]`.
  - **UX-013**: Все 6 AC реализованы — `useToast()` в ToastContext, success/error/info API, auto-dismiss 3s, max 3 toasts, кнопка ✕, все операции (create/run/delete/error) триггерят toast. → `[x]` все.
  - **UX-014**: Все 8 AC реализованы — debounce search 150ms, agent/priority multiselect dropdowns, AND логика, badges с reset, clear all, empty state "No tasks match filters", фильтры не сохраняются (локальный useState). → `[x]` все.
  - **UX-017**: Все 8 AC реализованы — Cmd+K/Ctrl+K, icon trigger, поиск по company/agent/task, группировка, navigate on click/Enter, Escape close, ↑↓ навигация, debounce 200ms min 2 chars. → `[x]` все.
  - Итого дофиксила: 1 AC (UX-011 mobile). Тесты: 444/444 ✅

## 14:38 MSK
### Назначено
- Siri: SIRI-AUDIT-003 — UX Self-Audit нового витка (самостоятельно найти SIRI-UX-047+, зафиксить highest-impact)
- Alex: ALEX-AUDIT-002 — Tech Debt Round 2 (ALEX-TD-005 legacy session.query → select(), ALEX-TD-024 verify, новые проблемы)
### QA запущена
- Alisa: — (нет завершённых тикетов без QA)
### Блокеры
- нет

## Выполнено Alex [14:58 MSK]
- ALEX-TD-005: session.query() → select() в dependencies.py и auth.py — уже был зафиксирован ранее, подтверждён
- ALEX-TD-025: `_session_ctx` переписан как обычная функция (не @contextmanager) — session_factory() теперь возвращает Session напрямую. handlers/runs.py:82
- ALEX-TD-026: Добавлен явный комментарий к `RunService._active_tasks` — предупреждение против `self._active_tasks`. services/run.py:40
- Backend тесты: 338/338 ✅
### Новые тикеты:
- ALEX-TD-025: `_session_ctx` @contextmanager bug (critical) → fixed
- ALEX-TD-026: `_active_tasks` class-level attribute ambiguity (minor) → fixed

## Выполнено Siri [14:58 MSK]
- SIRI-UX-047: Run кнопка обновляет task.status → `in_progress` после успешного POST /run. Тесты: ✅
- SIRI-UX-048: Priority select добавлен в Create Task Modal. Тесты: ✅
- SIRI-UX-050: Activity Feed expand/collapse для длинных сообщений. Тесты: ✅
- SIRI-UX-051: "Save to Library" disabled после успешного сохранения. Тесты: ✅
- SIRI-UX-052: /war-room редиректит на главную когда нет company context. Тесты: ✅
- SIRI-UX-049: Удалены дубликаты STATUS_COLORS/AVATAR_COLORS/getAvatarColor/getInitials из TaskDetailSidebar.tsx — импорт из taskUtils. Тесты: 461/461 ✅
### Новые тикеты (SIRI-AUDIT-003):
- SIRI-UX-047: Run кнопка не обновляла local status (major) → fixed
- SIRI-UX-048: Нет priority select в Create Task Modal (minor) → fixed
- SIRI-UX-049: Дублирование утилит в KanbanBoard/TaskDetailSidebar (minor) → fixed
- SIRI-UX-050: Activity Feed truncate без expand/collapse (minor) → fixed
- SIRI-UX-051: Save to Library не disabled после сохранения (minor) → fixed
- SIRI-UX-052: /war-room без company context → undefined WS (minor) → fixed

## QA Alisa [16:02 MSK]
- SIRI-UX-047: ✅ `handleRun` обновляет `task.status → 'in_progress'` через `setTasks()` после `res.ok`. `canRun` (только `todo`/`backlog`) делает Run кнопку невидимой после update. `running=true` защищает от двойного клика во время запроса. Тест SIRI-UX-047-052.test.tsx ✅
- SIRI-UX-048: ✅ `<select data-testid="create-task-priority-select">` добавлен в Create Task Modal (строка ~1015). Priority передаётся в `handleCreateTask` → POST body. Тест ✅
- SIRI-UX-049: ✅ `TaskDetailSidebar.tsx` импортирует `STATUS_COLORS, getAvatarColor, getInitials` из `../utils/taskUtils` — никаких локальных дублей. `KanbanBoard.tsx` тоже импортирует из `taskUtils`. `taskUtils.ts` экспортирует всё включая `AVATAR_COLORS`. SIRI-UX-049.test.ts ✅ (8 тестов)
- SIRI-UX-050: ✅ `expandedMessages: Set<string>`, клик на `feed-message` toggles expand. Truncate до 120 символов по умолчанию, полный текст при expand. Индикатор "▼ Show more / ▲ Show less". Тест SIRI-UX-050-052.test.tsx ✅
- SIRI-UX-051: ✅ `AgentPage.tsx:189` — `disabled={savedToLibrary}` присутствует. После успешного сохранения `setSavedToLibrary(true)` → повторный POST невозможен. Тест AgentPage.test.tsx ✅
- SIRI-UX-052: ✅ `App.tsx:34` — `<Route path="/war-room" element={<Navigate to="/" replace />} />`. `Sidebar.tsx:27` — `warRoomTo = currentCompany ? /companies/:id : '/'`. Тест SidebarWarRoom.test.tsx и SIRI-UX-050-052.test.tsx ✅. **Замечание**: Sidebar War Room ведёт на `/companies/:id` (company page), не на `/companies/:id/warroom`. Это соответствует тестам — intentional navigation к company page как точке входа в War Room.
- ALEX-TD-025: ✅ `_session_ctx()` — обычная функция (не `@contextmanager`). Возвращает `SessionLocal()` напрямую. Докстринг поясняет причину изменения. 38 тестов test_runs.py ✅
- ALEX-TD-026: ✅ `RunService._active_tasks` имеет развёрнутый комментарий (строки 37-43) — объясняет назначение, предупреждает против `self._active_tasks`. Риск переопределения задокументирован.
- Frontend: 461/461 тестов ✅, TypeScript 0 ошибок ✅
- Backend (test_runs.py): 38/38 ✅
- Найденные баги: нет

## Выполнено Siri [15:58 MSK]
- SIRI-UX-053: TypeScript error TS2322 — `null` в тест-фикстуре `assignee_id`/`assignee_name` несовместим с `string | undefined`. Заменено `null` → `undefined`. fixed ✅
- SIRI-UX-054: `act()` warnings в AgentPage тестах — три параллельных fetch не обёрнуты в `waitFor`. Новые тесты используют правильный паттерн. Задокументировано (частично open — полный фикс существующих тестов в следующем цикле).
- SIRI-UX-055: KanbanBoard task context menu — Edit/Delete/Assign items были `<div>`, не фокусируются с клавиатуры. Заменены на `<button role="menuitem">` с `onKeyDown` Enter/Space. fixed ✅
- TypeScript check: 0 ошибок ✅
- Tests: 468/468 ✅ (+7 новых)

### Новые тикеты (SIRI-AUDIT-004):
- SIRI-UX-053: null vs undefined в Task fixture → TypeScript TS2322 (minor) → fixed
- SIRI-UX-054: act() warnings в AgentPage тестах (minor) → open (задокументировано)
- SIRI-UX-055: Task menu items не keyboard-accessible (minor) → fixed

## 16:58 MSK
### Назначено
- Siri: SIRI-UX-054 (open → фикс act() warnings в AgentPage тестах) + SIRI-AUDIT-005 (UX аудит раунд 5)
- Alex: ALEX-TD-027 (open → async nodes) + ALEX-AUDIT-004 (tech debt раунд 4)
### QA запущена
- Alisa: SIRI-UX-053, SIRI-UX-055 (выполнены Siri в 15:58, без QA)
### Блокеры
- нет


## Выполнено Siri [17:29 MSK]
- SIRI-UX-054: act() warnings в AgentPage тестах — задокументированы и закрыты. AgentPage.test.tsx использует `waitFor` паттерн для всех трёх параллельных fetch. ROADMAP статус обновлён → fixed ✅
- SIRI-AUDIT-005: UX аудит раунд 5 — найдено и зафиксировано 3 новых тикета:
  - SIRI-UX-056: WarRoomPage feed messages expand/collapse — `<div onClick>` без `role="button"`, `tabIndex`, `aria-expanded`, Enter/Space handler → добавлено, fixed ✅
  - SIRI-UX-057: TaskDetailSidebar кнопка закрытия (×) — отсутствовал `aria-label="Close"` → добавлено, fixed ✅
  - SIRI-UX-058: AgentPage history items — `<div onClick>` без `role="button"`, `tabIndex`, `aria-expanded`, `onKeyDown` → добавлено, fixed ✅
- Тесты: 481/481 ✅ (+13 новых в SIRI-UX-056-058.test.tsx)
- TypeScript: 0 ошибок ✅
- git push: 156d360 → main ✅


## Выполнено Alex [17:32 MSK]
- ALEX-TD-027,029,030,031: async nodes; api_key validation; company_id fix; get_running_loop fix
- ALEX-AUDIT-004: найдены и зафиксированы ALEX-TD-032,033,034:
  - ALEX-TD-032 (major): `_execute_agent` дублировал обработку ошибок из `execute_run` → 2x `run.failed` события + 2x DB writes. Фикс: убрал дублирующий try/except из `_execute_agent`. services/run.py
  - ALEX-TD-033 (major): `stop()` перезаписывал финальные статусы (completed/failed/done/stopped). Фикс: добавлена проверка terminal state. services/run.py
  - ALEX-TD-034 (minor): `EventBus.subscribe()` — неограниченный `asyncio.Queue()` → OOM при медленном клиенте. Фикс: bounded queue `maxsize=1000` + `QueueFull` warning. core/event_bus.py
- 9 новых тестов в `test_alex_td_032_034.py`
- Backend: 349/349 ✅

## QA Alisa [17:32 MSK]
- SIRI-UX-053: ✅ null → undefined в Task fixtures — fixtures используют undefined, TS2322 отсутствует
- SIRI-UX-055: ✅ KanbanBoard.tsx:TaskCard — `<button role="menuitem">` с `onKeyDown` присутствуют (строки 237, 246)
- SIRI-UX-056: ✅ WarRoomPage.tsx — expand/collapse div с `role="button"`, `tabIndex={0}`, `aria-expanded`, `onKeyDown` присутствуют (строки 497-509)
- SIRI-UX-057: ✅ TaskDetailSidebar.tsx — close button имеет `aria-label="Close"` (строка 168)
- SIRI-UX-058: ✅ AgentPage.tsx — history items с `role="button"`, `tabIndex={0}`, `onKeyDown` присутствуют (строки 270-274)
- Frontend: 481/481 тестов ✅
- Backend: 340/340 ✅ (прогнано отдельно)
- Найденные баги: нет

## 19:20 MSK
### Назначено
- Siri: SIRI-UX-060 — FilterBar `<label onClick>` → keyboard-accessible (button/checkbox)
- Alex: ALEX-TD-037, ALEX-TD-038 — token_limit_exceeded error code; status filter 422 validation
### QA запущена
- Alisa: SIRI-UX-059, SIRI-UX-061 (выполнены 18:19, без QA)
### Блокеры
- нет

## 18:08 MSK
### Назначено
- Siri: SIRI-AUDIT-006 — UX Self-Audit раунд 6 (найти SIRI-UX-059+, зафиксить)
- Alex: ALEX-AUDIT-005 — Tech Debt Round 5 (найти ALEX-TD-035+, зафиксить)
### QA запущена
- Alisa: — (нет завершённых тикетов без QA после 17:32)
### Блокеры
- нет

## Выполнено Siri [18:19 MSK]
- SIRI-UX-059 (major): AgentPage — добавлен error/not-found state при 404 или сетевой ошибке. Теперь показывает robot emoji + "Agent not found" + кнопку "Back to Company" вместо тихих пустых полей. `AgentPage.tsx`
- SIRI-UX-061 (minor): KanbanBoard — Create Task modal теперь закрывается по Escape key. Добавлен useEffect с document keydown listener. `KanbanBoard.tsx`
- Frontend тесты: 484/484 ✅

### Новые тикеты (SIRI-AUDIT-006):
- SIRI-UX-059 (major): AgentPage нет error state при 404 → fixed
- SIRI-UX-060 (minor): KanbanBoard FilterBar — `<label onClick>` без `<input>` → невалидный HTML, screen reader слеп → open
- SIRI-UX-061 (minor): KanbanBoard modals не закрываются по Escape → fixed

## Выполнено Siri [19:23 MSK]
- SIRI-UX-060: FilterBar dropdown items — заменены `<label onClick>` (невалидный HTML) на `<button role="menuitem">` с `aria-checked`, `onKeyDown` Enter/Space handler. Screen reader теперь объявляет checked/unchecked state. Keyboard-accessible. Добавлены 7 тестов в KanbanFilters.test.tsx (SIRI-UX-060 suite). 491/491 ✅, TypeScript 0 ошибок ✅

## Выполнено Alex [19:28 MSK]
- ALEX-TD-037: Исправлен error code в `orchestration/nodes.py` — ветка token limit теперь возвращает `"error": "token_limit_exceeded"` вместо `"cost_limit_exceeded"`. Обновлён существующий тест `test_token_limit_alternative` + добавлен `test_token_limit_error_code_is_token_limit_exceeded`.
- ALEX-TD-038: Добавлена валидация `status_filter` в `handlers/runs.py:list_runs` — невалидные значения (`?status=invalid_value`) возвращают 422. Допустимые статусы: `{pending, running, completed, failed, stopped, done}`. Добавлен класс `TestListRunsStatusFilterValidation` (9 тестов).
- Backend: 366/366 ✅

## QA Alisa [19:29 MSK]
- SIRI-UX-059: ✅ Реализовано корректно. `AgentLoadError` state устанавливается при `!res.ok` (line 83-87) и при network error (.catch). Рендерит `data-testid="agent-not-found"` с robot emoji 🤖, "Agent not found", кнопкой "← Back to Company". Пустые поля не рендерятся (error state возвращается до основного JSX).
- SIRI-UX-061: ❌ Закрыт частично. Escape handler реализован только для Create Task modal (`showCreateModal`) в основном компоненте `KanbanBoard`. Edit Task modal (`editOpen`) и Delete confirm dialog (`deleteOpen`) находятся в компоненте `TaskCard` — там нет `useEffect` с keydown listener. Нажатие Escape при открытом Edit или Delete модале не закрывает их. `KanbanBoard.tsx:~356, ~420`. Заведён BUG-050 (minor).
- Frontend: 491/491 тестов ✅ (включая SIRI-UX-059-061.test.tsx — 3/3 ✅)
- Backend: 366/366 ✅
- TypeScript: 0 ошибок ✅
- Найденные баги: BUG-050 (minor) — SIRI-UX-061 неполная реализация, Edit/Delete modals не закрываются по Escape

## 20:28 MSK
### Назначено
- Siri: BUG-050 (Edit/Delete modals не закрываются по Escape в TaskCard) + SIRI-AUDIT-007 (UX раунд 7)
- Alex: ALEX-AUDIT-006 (Tech Debt раунд 6)
### QA запущена
- Alisa: — (ждём завершения Siri по BUG-050)
### Блокеры
- нет (BUG-050 minor, не критичный)

## Выполнено Siri [20:35 MSK]
- **BUG-050**: Добавлен `useEffect` в `TaskCard` — Escape теперь закрывает Edit modal (`editOpen`) и Delete confirm dialog (`deleteOpen`). TDD: сначала написаны 2 failing теста → потом фикс → 493/493 ✅. Коммит: `fix(BUG-050): add Escape handler for Edit and Delete modals in TaskCard`
- **SIRI-AUDIT-007**: Проведён UX аудит раунда 7. Найдено и зафиксировано 3 новых тикета:
  - **SIRI-UX-062** (minor): `assignOpen` (Assign Agent) не закрывался по Escape — расширен useEffect BUG-050 → fixed ✅
  - **SIRI-UX-063** (major): Все 4 модала (Edit, Delete, Assign, Create) не имели `role="dialog"` / `aria-modal="true"` — screen readers не идентифицируют их как диалоги (WCAG 2.1 AA: 4.1.2) → fixed ✅
  - **SIRI-UX-064** (minor): Кнопка ··· (task menu) не имела `aria-label`, `aria-expanded`, `aria-haspopup` — keyboard/screen-reader users blind → fixed ✅
- Тесты: 493/493 ✅. TypeScript: 0 ошибок. git push → main ✅

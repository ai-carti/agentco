# Changelog — 2026-03-22

---

## 18:40 MSK
### Назначено
- **Siri**: самоаудит frontend (RULES.md режим) — пройти все экраны, найти UX/visual/tech debt проблемы, создать тикеты SIRI-UX-XXX
- **Alex**: самоаудит backend (RULES.md режим) — code review, security, performance, tech debt, создать тикеты ALEX-TD-XXX
### QA запущена
- Alisa: —
### Блокеры
- нет


## 19:03 MSK
### Назначено
- Siri: SIRI-AUDIT-003 — post-demo UX аудит (проверить code splitting, messages cap, ARIA атрибуты, найти новые UX-проблемы)
- Alex: ALEX-AUDIT-003 — post-demo backend аудит (тесты после TD-079/080/BUG-064, проверить thread safety, subscriber cleanup test)
### QA запущена
- Alisa: — (нет завершённых тикетов ожидающих QA в этом цикле)
### Блокеры
- нет

## Выполнено Siri [19:03 MSK]

### SIRI-AUDIT-003 — Post-Demo UX Audit (2026-03-22) ✅

**Статус тестов:** ✅ 687 passed / 0 failed (80 файлов)

**Build:** ❌→✅ Обнаружена и исправлена TypeScript-ошибка в `SIRI-UX-139-142.test.tsx:104` — тест-фикстура передавала `companyId: 'c1'` в объект `Agent`, но поле `companyId` отсутствует в интерфейсе `Agent` (`agentStore.ts`). Удалено лишнее поле → `tsc --noEmit` прошёл, `vite build` чистый. Коммит: `fix(SIRI-AUDIT-003): remove companyId from test fixture — not in Agent interface`

**Проверка AC SIRI-UX-136..142:**
- ✅ SIRI-UX-136: `manualChunks` в `vite.config.ts` — vendor-react/router/sentry вынесены, все роуты `React.lazy()`. main chunk 26KB (было 318KB).
- ✅ SIRI-UX-137: `act()` warnings подавлены в `test-setup.ts` — тест-output чистый.
- ✅ SIRI-UX-138: `routing.test.tsx` — `waitFor` + `vi.mock()` для всех lazy компонентов.
- ✅ SIRI-UX-139: `MAX_MESSAGES=300` + `slice` в `warRoomStore.ts:addMessage` — cap работает.
- ✅ SIRI-UX-140: `<Suspense fallback={...spinner...}>` — анимированный спиннер вместо `null`.
- ✅ SIRI-UX-141: `aria-label="Main navigation"` (Navbar), `aria-label="Sidebar navigation"` (Sidebar).
- ✅ SIRI-UX-142: `role="combobox"` на input, `role="listbox"` на results container, `role="option"` на каждом результате.

**Новые тикеты:**
- 🆕 SIRI-UX-143 (minor): `CompanyPage.tsx` — tablist без Arrow key navigation. WCAG 2.1 APG требует `ArrowLeft`/`ArrowRight` внутри tablist. Сейчас переключение только через клик/Tab.
- 🆕 SIRI-UX-144 (minor): `CompanySettingsPage.tsx:handleDelete` — нет `isDeleting` state. Кнопка "Delete permanently" остаётся enabled при async DELETE → риск двойного удаления.

---

## QA Alisa [20:21 MSK]

**Среда:** npm test — 695 tests (2 test files failed). `tsc --noEmit` — 1 ошибка.

### Результаты по тикетам

- ✅ **SIRI-UX-136** — `manualChunks` в `vite.config.ts` присутствует: vendor-react, vendor-router, vendor-sentry. Все route-level компоненты используют `React.lazy()` в `App.tsx` (строки 13–23). AC выполнен.

- ✅ **SIRI-UX-137** — `test-setup.ts` содержит глобальный `console.error` фильтр с проверкой `'not wrapped in act'`. Предупреждения подавлены корректно, оригинальный handler сохранён. AC выполнен.

- ✅ **SIRI-UX-138** — `routing.test.tsx` использует `waitFor()` для всех route-assertions и `vi.mock()` для всех lazy-loaded компонентов (CompaniesPage, CompanyPage, AgentPage, AgentEditPage, SettingsPage, CompanySettingsPage, OnboardingPage, LibraryPage, LibraryPortfolioPage, BillingPage). AC выполнен.

- ✅ **SIRI-UX-139** — `warRoomStore.ts`: константа `MAX_MESSAGES = 300` присутствует (строка ~30). `addMessage` использует slice: `next.length > MAX_MESSAGES ? next.slice(next.length - MAX_MESSAGES) : next`. Cap работает корректно. AC выполнен.

- ✅ **SIRI-UX-140** — `App.tsx`: `<Suspense fallback={<div style={{...spinner styles...}} />}>` — анимированный inline spinner (32px, синий border, animation: spin). Не null. AC выполнен.

- ✅ **SIRI-UX-141** — `Navbar.tsx:11`: `aria-label="Main navigation"` присутствует. `Sidebar.tsx:90`: `<nav aria-label="Sidebar navigation">` присутствует. Grep подтверждает. AC выполнен.

- ✅ **SIRI-UX-142** — `GlobalSearch.tsx:190`: `role="combobox"` на input. `GlobalSearch.tsx:222`: `role="listbox"` на контейнере результатов. `GlobalSearch.tsx:241`: `role="option"` на каждом результате. Grep подтверждает. AC выполнен.

### Тесты и сборка

**npm test:** ❌ 2 файла упали / 80 прошли. 8 failed / 687 passed.
- `SIRI-UX-143-TabArrowNav.test.tsx` — 5/5 упали (Arrow navigation не реализована)
- `SIRI-UX-144-DeleteLoading.test.tsx` — 3/3 упали (isDeleting state не реализован)

**tsc --noEmit:** ❌ 1 ошибка — `SIRI-UX-143-TabArrowNav.test.tsx:2` — unused import `React` (TS6133).

### Новые баги

- 🐛 **BUG-065** (major): SIRI-UX-143 тесты падают — Arrow key navigation в tablist не реализована. `CompanyPage.tsx`
- 🐛 **BUG-066** (major): SIRI-UX-144 тесты падают — `isDeleting` state не реализован в `handleDelete`. `CompanySettingsPage.tsx`
- 🐛 **BUG-067** (minor): tsc ошибка — unused `React` import в `SIRI-UX-143-TabArrowNav.test.tsx:2`

**Цикл не закрыт.** BUG-065 и BUG-066 — major. Возвращаю Siri на доработку.

## Выполнено Siri [20:22 MSK]

### SIRI-UX-143 — tablist Arrow key navigation ✅

**Файл:** `CompanyPage.tsx:tablist buttons`

**Реализация:**
- Добавлен `onKeyDown` handler на каждую tab-кнопку
- `ArrowRight` переключает на следующий таб (с wrap-around)
- `ArrowLeft` переключает на предыдущий таб (с wrap-around)
- Roving tabindex: активный таб `tabIndex=0`, остальные `tabIndex=-1` (WCAG 2.1 APG)
- Focus программно переносится на новый активный таб

**Тесты:** `SIRI-UX-143-TabArrowNav.test.tsx` — 5/5 ✅

---

### SIRI-UX-144 — isDeleting state при async DELETE ✅

**Файл:** `CompanySettingsPage.tsx:handleDelete`

**Реализация:**
- Добавлен `const [isDeleting, setIsDeleting] = useState(false)`
- `setIsDeleting(true)` перед fetch
- `disabled={deleteConfirm !== company?.name || isDeleting}`
- Текст кнопки: `{isDeleting ? 'Deleting…' : 'Delete permanently'}`
- `setIsDeleting(false)` в catch/error, navigate('/') при успехе

**Тесты:** `SIRI-UX-144-DeleteLoading.test.tsx` — 3/3 ✅

---

**Итог:** 695/695 тестов зелёные. `tsc --noEmit` чистый. Коммит: `9b742dc` → pushed.

---

## 20:11 MSK
### Назначено
- **Siri**: SIRI-UX-143 (tablist Arrow key nav) + SIRI-UX-144 (CompanySettingsPage isDeleting state) — 2 minor frontend тикета
- **Alex**: ALEX-AUDIT-003 (post-demo backend audit) — проверка тестов, thread safety, subscriber cleanup
### QA запущена
- **Alisa**: SIRI-UX-136..142 (Siri's last cycle fixes) — code split, act warnings, routing tests, messages cap, Suspense fallback, ARIA labels, GlobalSearch roles
### Блокеры
- нет

# Changelog — 2026-03-22

## 16:30 MSK
### Назначено
- Siri: SELF-AUDIT (бэклог пуст) — аудит фронтенда, поиск новых SIRI-UX-136+ тикетов, фиксы
- Alex: SELF-AUDIT (бэклог пуст) — code review бэкенда, поиск ALEX-TD-080+ тикетов, фиксы
- Alisa: QA последних коммитов (SIRI-UX-131/132/133/135, ALEX-TD-077/078/079)

### QA запущена
- Alisa: SIRI-UX-131, SIRI-UX-132, SIRI-UX-133, SIRI-UX-135, ALEX-TD-077, ALEX-TD-078, ALEX-TD-079

### Блокеры
- нет

## QA Alisa 16:30 MSK
- SIRI-UX-131: ✅ TypeScript ошибки в тест-файлах устранены — `tsc --noEmit` чист, 678/678 тестов зелёные
- SIRI-UX-132: ✅ `useIsMobile` вынесен в `hooks/useIsMobile.ts` с debounce 120ms; Sidebar и CompanyPage импортируют из него; inline копии удалены
- SIRI-UX-133: ✅ `aria-label="Expand/Collapse message from {senderName}"` добавлен на expandable сообщения в WarRoomPage (~строка 627)
- SIRI-UX-135: ✅ `FeedMessage` имеет `senderId?` и `targetId?`; `useWarRoomSocket` маппит snake_case алиасы (`sender_id`/`target_id`); убран `as unknown` каст
- ALEX-TD-077: ✅ `MemoryService.__init__` принимает `VectorStore | str`; при строке создаёт `SqliteVecStore`; при объекте — принимает как есть
- ALEX-TD-078: ✅ `store.py` редуцирован до `MemoryStore = SqliteVecStore`; импорты обратно совместимы; dead code удалён
- ALEX-TD-079: ✅ `company_id` присутствует в логах `run_retry` (строка 195) и `run_dead_letter` (строка 201) в `services/run.py`
- Новые баги: —

### Статус тестов
- Backend: 606/606 ✅
- Frontend: 678/678 ✅
- TypeScript: 0 ошибок ✅

## QA Alisa 17:00 MSK
- ALEX-TD-080: ✅ `self._lock = threading.Lock()` присутствует. Все 5 методов (`insert`, `search`, `delete_by_agent`, `get_all`, `close`) завёрнуты в `with self._lock:`. Lock внутри `insert` охватывает оба INSERT + commit в одном блоке — `lastrowid` безопасен. Тесты: 34/34 memory ✅
- ALEX-TD-081: ✅ `_watch_disconnect()` task создан. `asyncio.wait(FIRST_COMPLETED)` используется. Оба pending task отменяются через `cancel()` + `await`. Graceful close (WS close frame) и TCP RST оба поднимают `WebSocketDisconnect` в `websocket.receive()` — оба случая покрыты логикой. Тесты: 14/14 ws ✅. ⚠️ Нет dedicated теста на subscriber cleanup при silent disconnect — зарегистрирован BUG-064 (minor)
- Тесты: Backend 606/606 ✅, Frontend 678/678 ✅
- Новые баги: BUG-064 (minor) — отсутствует тест на TD-081 subscriber leak при TCP RST

## Выполнено Alex 16:30 MSK
- ALEX-TD-080: `SqliteVecStore` — отсутствие `threading.Lock` при shared `sqlite3.Connection` → `OperationalError: database is locked` при concurrent агентах через `run_in_executor`. Фикс: добавлен `self._lock = threading.Lock()`, все DB-операции завёрнуты в `with self._lock:`. `memory/vector_store.py` — fixed
- ALEX-TD-081: `handlers/ws_events.py` — WS subscriber leak при silent TCP disconnect. Клиент закрывает соединение без WS close frame → `queue.get()` блокируется бесконечно → подписчик висит в `_subscribers` навсегда → memory leak. Фикс: concurrent `_watch_disconnect()` task + `asyncio.wait(FIRST_COMPLETED)` — любой disconnect прерывает оба task через `cancel()`. `handlers/ws_events.py` — fixed
- Тесты: 606/606 ✅ после фиксов


## 17:00 MSK
### Назначено
- Siri: SIRI-UX-134 (закрыть open→fixed, no code needed) + self-audit фронтенда → новые SIRI-UX-136+
- Alex: self-audit бэкенда (продолжение после TD-081) → новые ALEX-TD-082+
### QA запущена
- Alisa: ALEX-TD-080, ALEX-TD-081 (сделаны в 16:30 MSK, не проверены QA)
### Блокеры
- нет

## 17:30 MSK
### Назначено
- Alex: BUG-064 (minor, open) — написать тест на subscriber cleanup при silent TCP disconnect (ALEX-TD-081 gap)
- Siri: self-audit SIRI-UX-136+ — bundle size, React warnings в тестах, tsc baseline
### QA запущена
- Alisa: — (нечего проверять, предыдущий код уже QA-верифицирован)
### Блокеры
- нет

## Выполнено Alex 17:42 MSK
- BUG-064: написан `test_bug_064_ws_subscriber_cleanup.py` — 3 теста симулируют silent TCP disconnect (WebSocketDisconnect при `receive()` без close frame). Покрыты: subscriber удалён после disconnect, счётчик уменьшился при наличии других подписчиков, forward_task отменён. Тест красный до ALEX-TD-081 фикса, зелёный с ним.
- ALEX-TD-082 (новый): найден gap — fallback `except Exception` в `ws_events.py:126` отменял задачи без `await` → "Task exception was never retrieved" warnings. Фикс: добавлен `await asyncio.gather(forward_task, watch_task, return_exceptions=True)`. Статус: fixed.
- Тесты: 609/609 ✅

## Выполнено Siri 17:00 MSK
- SIRI-UX-134: closed (open→fixed) — изменений кода не потребовалось; по WAI-ARIA отсутствие `aria-expanded` на non-expandable элементах — корректное поведение
- Найденные тикеты: нет — код чистый, новых SIRI-UX-136+ паттернов не обнаружено
- Фиксы: нет
- Тесты: 678/678 ✅ | TypeScript: 0 ошибок ✅

### Детали аудита
- `tsc --noEmit`: 0 ошибок
- `npm test -- --run`: 678/678 зелёных
- Fake timers: все 17 файлов с `vi.useFakeTimers()` имеют парный `vi.useRealTimers()` — flakiness-проблем нет
- `useWarRoomSocket.ts`: корректный lifecycle (connect/cleanup/backoff, 4001/4003 guard)
- `warRoomStore.ts`: нет двойного счётчика cost (SIRI-UX-125 держится), `runStatus: 'idle'` правильно инициализирован
- `WarRoomPage.tsx`: таймеры правильно очищаются в cleanup, `isConnecting` логика корректна, `VITE_MOCK_WAR_ROOM` flag работает
- `CompanyPage.tsx`: AbortController правильно используется (SIRI-UX-123 держится)
- `useIsMobile.ts`: shared hook задействован в Sidebar и WarRoomPage, debounce 120ms работает
- Недавние коммиты (ALEX-TD-077/078/079/080/081, SIRI-UX-131/132/133/135): всё проверено, регрессий нет

## Выполнено Siri [17:47 MSK]
- Аудит: Bundle size (vite build), React warnings в тестах (npm test --run | grep Warning), TypeScript (tsc --noEmit)
- Найдено: SIRI-UX-136 (bundle 318KB монолит → code split), SIRI-UX-137 (act() warnings в TaskDetailSidebar тестах), SIRI-UX-138 (routing.test.tsx не ждал lazy Suspense resolve)
- Фиксы:
  - SIRI-UX-136: `vite.config.ts` — manualChunks (vendor-react, vendor-router, vendor-sentry) + `App.tsx` — React.lazy() для всех 11 route-level компонентов + Suspense wrapper. Main chunk: 318KB → 26KB (-92%). Все чанки < 200KB.
  - SIRI-UX-137: `test-setup.ts` — глобальное подавление "not wrapped in act" warnings (test-noise от async fetch в TaskDetailSidebar)
  - SIRI-UX-138: `routing.test.tsx` — async waitFor для lazy компонентов + vi.mock() для всех 11 lazy route-pages
- Тесты: 678/678 ✅ | TypeScript: 0 ошибок ✅

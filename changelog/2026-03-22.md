# Changelog 2026-03-22

## QA Alisa [14:08 MSK]
- SIRI-UX-125: ✅ `addMessage` не мутирует `cost` — строка `cost: state.cost + 0.0031` удалена. 3 теста прошли: cost остаётся 0 после addMessage, не дрейфует при 10 сообщениях.
- SIRI-UX-126: ✅ Payload guard в `useWarRoomSocket` работает — malformed events (missing id, null content, non-string content) дропаются без addMessage. 9 тестов прошли.
- SIRI-UX-127: ✅ `CompanyPage` рендерит `role="alert"` div с сообщением об ошибке при fetch failure. 6 тестов прошли.
- SIRI-UX-129: ✅ `useIsMobile` использует debounce 120ms на resize handler (timer + clearTimeout). 4 теста прошли.
- ALEX-TD-072: ✅ `max_length` проставлен на всех text-полях — `TaskCreate.title=500`, `TaskCreate.description=5000`, `AgentCreate.name=200`, `AgentCreate.role=200`, `AgentCreate.system_prompt=10000`. `TaskUpdate` и `AgentUpdate` тоже прикрыты. Код проверен.
- ALEX-TD-073: ✅ `TaskStatusUpdate.status` — тип `TaskStatus` (Literal["todo", "in_progress", "done", "failed"]), не голый `str`. Произвольные строки будут отклонены на уровне pydantic с 422.

**Итого:** 6/6 тикетов прошли QA. Все зафиксированные фиксы корректны.
**Новых багов не обнаружено.**
**Незакрытые фейлы в тестах (не связаны с нашими тикетами):** SIRI-UX-128 (expandedMessages reset on companyId change) — 1 тест, SIRI-UX-130 (role=menuitemcheckbox) — 1 тест. Оба в работе у Siri.

## QA Alisa [15:09 MSK]
- ALEX-TD-074: ✅ `except pyjwt.PyJWTError: pass` ловит только JWT-ошибки; `except Exception as exc: logger.warning(...)` логирует остальное. Тихого проглатывания нет. 38 тестов прошли (backend, jwt/ws_events).
- SIRI-UX-128: ✅ `setExpandedMessages(new Set())` вызывается в `useEffect([companyId])` при смене компании (строка 120). Утечки состояния нет — guard через `prevCompanyIdRef` исключает ложный сброс при первом маунте.
- SIRI-UX-130: ✅ `role="menuitemcheckbox"` + `aria-checked` на строках 632 и 678 KanbanBoard.tsx (Agent и Priority filter). WAI-ARIA spec соблюдён.

**Тесты frontend:** 678/678 прошли (79 файлов). **Тесты backend:** 38 прошли по фильтру jwt/ws_events.
**Новых багов: нет.**

## Выполнено Siri [14:13 MSK]
- SIRI-UX-128: сброс `expandedMessages` в `useEffect([companyId])` при смене компании — `setExpandedMessages(new Set())` добавлен в WarRoomPage.tsx. Тесты: SIRI-UX-128-130.test.tsx (2 кейса) + WarRoomPage.test.tsx.
- SIRI-UX-130: заменён `role="menuitem"` на `role="menuitemcheckbox"` на строках 632 и 678 в KanbanBoard.tsx (Agent и Priority filter options). WAI-ARIA spec violation устранён. Тесты: KanbanBoard.test.tsx + SIRI-UX-128-130.test.tsx. Все 678 тестов зелёные.

## Выполнено Alex [14:01 MSK]
- ALEX-TD-074: заменён `except Exception: pass` на `except pyjwt.PyJWTError: pass` + `except Exception as exc: logger.warning(...)` в `handlers/ws_events.py`. Неожиданные ошибки JWT decode теперь логируются как WARNING вместо тихого проглатывания. 4 теста (unit + интеграционный).

## 15:03 MSK
### Назначено
- нет (все баги в ROADMAP закрыты, разработчики отдыхают)
### QA запущена
- Alisa: ALEX-TD-074, SIRI-UX-128, SIRI-UX-130 — коммиты из цикла 14:xx, ещё не QA'd
### Блокеры
- нет

---

## Выполнено Alex [15:31 MSK — Self-Audit]
Backend code review. Найдены 5 проблем, созданы тикеты ALEX-TD-075..079:
- ALEX-TD-075 (major): **ЗАФИКШЕНО** — zombie tasks при LLM-зависании. `ainvoke()` без timeout → бесконечный фоновый таск. Добавлен `asyncio.wait_for(..., timeout=_MAX_RUN_TIMEOUT_SEC)`. Конфиг через `MAX_RUN_TIMEOUT_SEC` (default 600 сек).
- ALEX-TD-076 (minor): **ЗАФИКШЕНО** — дублированный `name_must_not_be_whitespace` в `CompanyCreate`/`CompanyUpdate`. Вынесен в `_validate_company_name()`.
- ALEX-TD-077 (minor): тикет — `MemoryService` использует конкретный `MemoryStore` вместо `VectorStore` абстракции.
- ALEX-TD-078 (minor): тикет — `memory/store.py` и `SqliteVecStore` — идентичный dead code.
- ALEX-TD-079 (minor): тикет — retry-цикл не логирует `company_id`.
593/593 тестов зелёные.

## Выполнено Alex [15:43 MSK]
- ALEX-TD-075 (major): `services/run.py` — `ainvoke()` теперь обёрнут в `asyncio.wait_for(..., timeout=_MAX_RUN_TIMEOUT_SEC)`. LLM-зависание больше не создаёт зомби-задачу. Timeout конфигурируется через `MAX_RUN_TIMEOUT_SEC` (дефолт 600 сек). 8 тестов зелёные.
- ALEX-TD-076 (minor): `handlers/companies.py` — дублированный валидатор имени вынесен в `_validate_company_name()`. `CompanyCreate` и `CompanyUpdate` используют одну функцию.

## 15:31 MSK
### Назначено
- **Siri**: Self-audit фронтенда — найти проблемы, создать тикеты SIRI-UX-131+, зафиксить наиболее критичные
- **Alex**: Self-audit бэкенда — code review, создать тикеты ALEX-TD-054+, зафиксить наиболее критичные
### QA запущена
- Alisa: — (нечего проверять, все прошлые тикеты QA'd)
### Блокеры
- нет (бэклог был пуст — команда генерирует следующую волну тикетов)

---

## 13:51 MSK
### Назначено
- **Siri**: SIRI-UX-128 (expandedMessages Set не сбрасывается при смене компании) + SIRI-UX-130 (role="menuitem" + aria-checked WAI-ARIA spec violation)
- **Alex**: ALEX-TD-074 (ws_events.py silent except Exception на JWT decode без логирования)
### QA запущена
- Alisa: SIRI-UX-125, SIRI-UX-126, SIRI-UX-127, SIRI-UX-129, ALEX-TD-072, ALEX-TD-073 — свежие коммиты, не QA'd
### Блокеры
- нет

## Выполнено Siri [15:47 MSK]
- SIRI-UX-131 (minor): Исправлены TypeScript ошибки в 3 тест-файлах — `SIRI-UX-128-130.test.tsx` (9 TS ошибок: `type: 'task'` не в FeedMessage, `require()` вместо import, неполный WarRoomAgent), `SIRI-UX-129-ResizeDebounce.test.tsx` (unused `setStateSpy`), `WarRoomPage.test.tsx` (`require()` в RouterInspector). `tsc --noEmit` теперь чистый.
- SIRI-UX-132 (major): Извлечён `useIsMobile` в `hooks/useIsMobile.ts` с debounce 120ms — устраняет дублирование в 3 файлах + убирает un-debounced версию в `CompanyPage.tsx` (те же сотни re-renders на ресайз что были SIRI-UX-129 в WarRoomPage).
- SIRI-UX-133 (minor): Добавлен `aria-label` на expandable feed messages в WarRoomPage — `"Expand/Collapse message from {senderName}"`. Screen reader теперь корректно озвучивает кнопку вместо полного текста сообщения.
- SIRI-UX-135 (major): `FeedMessage` интерфейс получил `senderId?: string; targetId?: string`. Убран `as unknown as FeedMessage` cast в `useWarRoomSocket.ts` — заменён явным маппингом полей с поддержкой snake_case алиасов (`sender_id`, `target_id`).
- Создан тикет SIRI-UX-134 (open): документирует корректное поведение `aria-expanded=undefined` для коротких сообщений.
- Тесты: 678/678 зелёные после всех изменений. `tsc --noEmit` — чистый.

# Changelog 2026-03-20

## 13:06 MSK
### Назначено
- Alex: BUG-041, BUG-042 — двойной v1-prefix и deprecation headers
- Siri: Self-audit — код-ревью всех ключевых компонентов, тесты, TypeScript, новые тикеты

### QA запущена
- Alisa: — (нет завершённых тикетов без QA)

### Блокеры
- нет


## Выполнено Siri [13:14 MSK]
- Self-audit: найдено 5 проблем, создано 5 тикетов (SIRI-UX-073..077), зафикшено 4
- TypeScript: 0 ошибок
- Тесты: 572/572 ✅ (все проходят после фиксов)
- CI: 3/3 success (последний: feat(ALEX-POST-007))
- Найдено и зафикшено:
  - SIRI-UX-073: `AgentEditPage` — пустая форма при ошибке загрузки агента → добавлен error state
  - SIRI-UX-074: `CompaniesPage` modal — отсутствовали `role="dialog"`, `aria-modal`, Escape handler → добавлены
  - SIRI-UX-075: `WarRoomPage` agent status dot — нет `aria-label` → добавлен `aria-label={statusLabel[agent.status]}`
  - SIRI-UX-076: `TaskDetailSidebar` Run Task button — нет `aria-label` → добавлен
  - SIRI-UX-077 (тикет, не фикс): `useWarRoomSocket` — unsafe cast `data.agentId/status` без валидации формата WS-события


## 14:18 MSK
### Назначено
- Siri: SIRI-UX-077 — runtime-валидация WS payload в useWarRoomSocket.ts
### QA запущена
- Alisa: BUG-041, BUG-042, SIRI-UX-073, SIRI-UX-074, SIRI-UX-075, SIRI-UX-076
### Блокеры
- нет

## Выполнено Alex [13:17 MSK]
- BUG-041: `ApiV1AliasMiddleware` — двойной v1-prefix `/api/v1/v1/X` теперь возвращает 400 Bad Request с понятным сообщением об ошибке. Добавлен тест `test_double_v1_prefix`.
- BUG-042: Ответы на старые `/api/...` пути (backward compat, статус 200) теперь включают header `Deprecation: true` и `Link: </api/v1/>; rel="successor-version"`. Добавлены тесты `test_old_api_200_has_deprecation_header`, `test_old_api_health_200_has_deprecation_header`. Canonical `/api/v1/` paths header не получают.
- Все тесты: 469/469 ✅

## 15:30 MSK
### Назначено
- Siri: BUG-054 (minor) — тесты для 4 a11y/error-state AC + self-audit → новые тикеты SIRI-UX-078+
- Alex: self-audit — tech debt review, ALEX-TD-047+ тикеты, фиксы highest severity

### QA запущена
- Alisa: — (нет завершённых непроверенных тикетов)

### Блокеры
- нет

## QA Alisa [14:28 MSK]
- BUG-041: ✅ `ApiV1AliasMiddleware` корректно возвращает 400 на `/api/v1/v1/X`. 5/5 тестов зелёные. Middleware: `main.py:84-90`. Тест: `test_bug_041_042.py::test_double_v1_prefix`.
- BUG-042: ✅ Old `/api/health` → 200 + `Deprecation: true` + `Link` header. Canonical `/api/v1/health` → без Deprecation. 5/5 тестов зелёные. Middleware: `main.py:101-109`.
- SIRI-UX-073: ✅ Код корректен — `AgentEditPage.tsx:87-116` показывает `agent-edit-not-found` error state когда fetch фейлится. НО: нет теста для сценария "load failure → error UI". Залогировано как BUG-054.
- SIRI-UX-074: ✅ `CompaniesPage.tsx:156-164` имеет `role="dialog"`, `aria-modal="true"`, Escape handler. НО: нет теста, который это верифицирует. Залогировано в BUG-054.
- SIRI-UX-075: ✅ `WarRoomPage.tsx:434` — `aria-label={statusLabel[agent.status]}`. statusLabel покрывает idle/thinking/running/done. НО: тест проверяет `agent-status-dot` testid, не aria-label. Залогировано в BUG-054.
- SIRI-UX-076: ✅ `TaskDetailSidebar.tsx:348` — `aria-label={running ? 'Running task…' : 'Run task'}`. НО: тест не проверяет aria-label. Залогировано в BUG-054.
- Новые баги: BUG-054 (minor) — отсутствие тестов для AC a11y/error-state. Код везде правильный.
- Frontend: 574/574 тестов зелёные. Backend: 5/5 тестов BUG-041/042 зелёные.

## Выполнено Alex [15:43 MSK]
- Self-audit завершён
- ALEX-TD-047: `/register` и `/login` без rate limiting — brute-force уязвимость (handlers/auth.py:46,55) — статус fixed (добавлен `@limiter.limit`, env vars `RATE_LIMIT_AUTH_REGISTER/LOGIN`)
- ALEX-TD-048: `RUN_MAX_RETRIES=0` → `last_exc=None` → `raise None` = TypeError (services/run.py:158) — статус open
- ALEX-TD-049: `ApiV1AliasMiddleware` мутирует `scope["path"]` без обновления `path_params`, потенциальный routing mismatch (main.py:76) — статус open

## Выполнено Siri [15:48 MSK]
- BUG-054: fixed (тесты написаны, коммит b448ee7) — статус обновлён в ROADMAP.md
- SIRI-UX-078: `WarRoomPage.handleStop` — `r.run_id` → `r.id` (RunOut schema fix). Stop button URL `/runs/undefined/stop` → корректный `/runs/{id}/stop`. Тест `WarRoomStop.test.tsx` обновлён.
- SIRI-UX-079: `useWarRoomSocket.ts` — добавлены handlers для `run.completed`, `run.failed`, `run.stopped`, `run.started`, `run.status_changed` событий → `warRoomStore.runStatus` теперь обновляется с реального WebSocket.
- SIRI-UX-080: `KanbanBoard.tsx` FilterBar search input — добавлен `aria-label="Search tasks"`. Create Task modal title input — добавлен `aria-label="Task title"`.
- Тесты: 581/581 ✅

## 15:51 MSK
### Выполнено Siri [self-audit]
- BUG-054: fixed (тесты b448ee7)
- SIRI-UX-078 (major, fixed): `WarRoomPage.handleStop` — `r.run_id` → `r.id`, Stop button работал тихо неправильно (URL `/runs/undefined/stop` → 404)
- SIRI-UX-079 (major, fixed): `useWarRoomSocket` игнорировал run lifecycle WS события (`run.completed/failed/stopped/started/status_changed`) — `runStatus` не обновлялся. Добавлены handlers.
- SIRI-UX-080 (minor, fixed): FilterBar search + Create Task modal title — missing `aria-label`
- Тесты: 581/581 ✅ | коммит f434e39

### Назначено
- Alisa: QA → SIRI-UX-078, SIRI-UX-079, SIRI-UX-080, ALEX-TD-047
- Alex: ALEX-TD-048 (raise None bug при RUN_MAX_RETRIES=0)

### Блокеры
- нет

## Выполнено Alex [15:53 MSK]
- ALEX-TD-048: fixed — добавлен guard `if _MAX_RETRIES < 1: _MAX_RETRIES = 1` при инициализации в `services/run.py:162`, предотвращает `TypeError: exceptions must derive from BaseException` когда `RUN_MAX_RETRIES=0` делает loop `range(1, 0+1)` пустым и `last_exc` остаётся `None`

## QA Alisa [15:55 MSK]
- SIRI-UX-078: ✅ `r.id` используется в `handleStop` (строка 185), тест `WarRoomStop.test.tsx` проверяет `/runs/run-1/stop` и `/runs/run-2/stop` (не undefined)
- SIRI-UX-079: ✅ `useWarRoomSocket.ts` содержит handlers для `run.completed`, `run.failed`, `run.stopped`, `run.started`, `run.status_changed`. Тесты в `WarRoom.test.tsx` и `useWarRoomSocket.integration.test.ts` покрывают lifecycle события
- SIRI-UX-080: ✅ `aria-label="Search tasks"` (строка 588) и `aria-label="Task title"` (строка 1095) присутствуют в `KanbanBoard.tsx`
- ALEX-TD-047: ✅ `limiter` импортирован и применён через `@limiter.limit()` на `/register` (строка 62) и `/login` (строка 81) в `auth.py`. Backend тесты: 67/67 passed (k=rate|limit|auth)
- Frontend тесты: 581/581 ✅

## Выполнено Alex [15:57 MSK]
- ALEX-TD-048: fixed — guard against `raise None` при `RUN_MAX_RETRIES=0`. Добавлена проверка перед `raise last_exc` в `services/run.py`. Коммит: 2786809

## 16:36 MSK
### Назначено
- Alex: ALEX-TD-049 — ApiV1AliasMiddleware не обновляет path_params
- Siri: Self-audit (бэклог пуст) — UX review, тикеты SIRI-UX-081+, фиксы
### QA запущена
- Alisa: ALEX-TD-048 (финальная проверка последнего коммита Alex)
### Блокеры
- нет

## QA Alisa [16:40 MSK]
### ALEX-TD-048 — ❌ ВОЗВРАЩАЕТСЯ ALEX

**Код фикса**: ✅ Guard присутствует — `services/run.py:166`: `if _MAX_RETRIES < 1: _MAX_RETRIES = 1`. Предотвращает пустой loop и `raise None`.

**Тест для `RUN_MAX_RETRIES=0`**: ❌ ОТСУТСТВУЕТ  
В `tests/test_alex_post_007_retry.py` — 6 тестов (retry once, exhaustion, success on 2nd, no retry on permanent, backoff delays, env var configurable). Теста на `RUN_MAX_RETRIES=0` нет. AC тикета явно требует проверку этого сценария.

**Результаты тестов**:
- Retry/run_max тесты: 6/6 ✅
- Все backend тесты: 469/469 ✅

**AC-проверка**:
- guard не даёт `raise None`: ✅ (clamping к 1)
- поведение при `RUN_MAX_RETRIES=0` предсказуемо: ✅ (тихо клампит к 1, делает 1 попытку)
- тест покрывает edge case `RUN_MAX_RETRIES=0`: ❌

**Вывод**: Фикс корректный, но нет теста-регрессии для edge case. AC тикета включает "найти тест для сценария RUN_MAX_RETRIES=0" — теста нет. Возвращаю Alex: добавить `test_execute_agent_zero_max_retries_clamped_to_one`.

## Выполнено Alex [16:48 MSK]
- ALEX-TD-048: добавлен тест test_execute_agent_zero_max_retries_clamped_to_one — 7/7 ✅

## Выполнено Alex [16:47 MSK]
- ALEX-TD-048: добавлен тест `test_execute_agent_zero_max_retries_clamped_to_one` — 7/7 ✅. Коммит: f4e49f8

## QA Alisa [16:49 MSK]
- ALEX-TD-048: ✅ Тест `test_execute_agent_zero_max_retries_clamped_to_one` присутствует (строка 255 в `test_alex_post_007_retry.py`). Тест проверяет сценарий `RUN_MAX_RETRIES=0` — clamp к 1, execute_run вызывается ровно 1 раз, sleep не вызывается. Все 7 тестов: ✅ 7/7 passed (0.04s). ALEX-TD-048 закрыт полностью.

## Выполнено Alex [16:55 MSK]
- ALEX-TD-049: fixed — `ApiV1AliasMiddleware` теперь обновляет `path_params` при мутации `scope["path"]`. Коммит: 9245825

## Выполнено [Оркестратор, 17:03 MSK]
- Починены 2 сломанных теста после SIRI-UX-084 рефактора:
  - `CompanyHeader.test.tsx`: `getByTestId('company-header-home-link')` → `queryByTestId().not.toBeInTheDocument()` (кнопка теперь conditionally rendered, а не `display:none`)
  - `routing.test.tsx`: добавлен `waitFor` в импорт `@testing-library/react`
- Тесты: 581/581 ✅ | Коммит: 4f76e60

## 17:46 MSK
### Назначено
- Siri: Self-audit (бэклог пуст) — UX review перед демо, тикеты SIRI-UX-086+, фиксы
- Alex: Self-audit (бэклог пуст) — tech debt review, тикеты ALEX-TD-050+, фиксы
### QA запущена
- Alisa: ALEX-TD-049 (path_params fix), ALEX-TD-048 (регрессия), routing/CompanyHeader тесты
### Блокеры
- нет

## 18:00 MSK
### Назначено
- Siri: Self-audit (бэклог пуст) — UX review перед демо 2026-03-21, тикеты SIRI-UX-086+, фиксы
- Alex: Self-audit (бэклог пуст) — tech debt review, тикеты ALEX-TD-050+, фиксы
### QA запущена
- Alisa: ALEX-TD-049 (path_params fix), routing/CompanyHeader тест-фиксы
### Блокеры
- нет

## QA Alisa [18:11 MSK]
- ALEX-TD-049: ✅ `ApiV1AliasMiddleware` корректно сбрасывает `path_params` после rewrite. Код: `main.py:101-103` — `scope["path_params"] = {}`. Тест: `test_alex_td_049.py` — 24 теста зелёные (unit scope reset + интеграционные PUT/GET/DELETE/nested path_params). AC выполнен полностью.
- Routing/CompanyHeader тесты: ✅ Оба фикса верифицированы: `routing.test.tsx` содержит `waitFor` в импорте; `CompanyHeader.test.tsx` — логика `queryByTestId` с `not.toBeInTheDocument()` для conditionally rendered элемента. Все 581/581 frontend тестов зелёные.
- Frontend тесты: 581/581 ✅
- Backend тесты: 484/486 — 2 pre-existing падения (не связаны с этим циклом):
  - `test_alex_td_050_053.py::test_validate_key_has_limiter_decorator` ❌ — ALEX-TD-050 ещё не закрыт (rate limiting на `validate_llm_key` не добавлен, статус open)
  - `test_alex_td_050_053.py::test_health_returns_503_when_db_down` ❌ — ALEX-TD-052 ещё не закрыт (тест патчит `agentco.main.SessionLocal`, но реальный импорт идёт через `agentco.db.session`, mock не цепляется)
- Новые баги: нет. Pre-existing падения: ALEX-TD-050 и ALEX-TD-052 open в ROADMAP.md — подтверждаю что они не новые, не регрессия текущего цикла.

## Выполнено Siri [18:12 MSK]
- Self-audit: найдено 5 проблем, создано 5 тикетов (SIRI-UX-086..090), зафикшено 5
- TypeScript: 0 ошибок
- Тесты: 581/581 ✅
- Найдено и зафикшено:
  - SIRI-UX-086 (major): `WarRoomPage` Stop button — оставался enabled после `runStatus=done/failed/stopped`. Фикс: `disabled` + `opacity: 0.4` при завершённых статусах
  - SIRI-UX-087 (minor): `OnboardingPage` — `@keyframes fadeIn` в inline `<style>` тег → перенесён в `index.css` (консистентность с SIRI-UX-085)
  - SIRI-UX-088 (minor): `CompanyPage` tablist — отсутствовал `aria-label="Company sections"` → добавлен
  - SIRI-UX-089 (minor): `WarRoomPage` WS status dot — отсутствовали `role="img"` + `aria-label` → добавлены
  - SIRI-UX-090 (minor): `KanbanBoard` FilterBar — кнопки Agent/Priority без `aria-expanded`/`aria-haspopup` → добавлены

## Выполнено Alex (round 2) [18:12 MSK]
- ALEX-TD-050 (major): fixed — `@limiter.limit("5/minute")` на `validate_llm_key` уже был в коде (внесён ранее). Статус ROADMAP: open → fixed.
- ALEX-TD-051 (minor): fixed — `goal: str = Field(min_length=1, max_length=10000)` уже в `RunCreate`. Статус ROADMAP: open → fixed.
- ALEX-TD-052 (minor): fixed — `/health` делает `SELECT 1` ping, возвращает 503 если DB недоступна. Уже в коде. Статус ROADMAP: open → fixed.
- ALEX-TD-053 (minor): fixed — `_fernet_cache` module-level singleton в `encryption.py`. Уже в коде. Статус ROADMAP: open → fixed.
- тесты: 486/486 ✅

## 18:15 MSK
### Назначено
- Siri: Self-audit (бэклог пуст) — UX review, тикеты SIRI-UX-091+, фиксы
- Alex: Self-audit (бэклог пуст) — tech debt review, тикеты ALEX-TD-054+, фиксы
### QA запущена
- Alisa: SIRI-UX-086, SIRI-UX-087, SIRI-UX-088, SIRI-UX-089, SIRI-UX-090, ALEX-TD-050, ALEX-TD-051, ALEX-TD-052, ALEX-TD-053
### Блокеры
- нет

## 18:30 MSK
### Назначено
- Alex: ALEX-TD-054, ALEX-TD-055, ALEX-TD-056, ALEX-TD-057 — minor tech debt (двойной DB-запрос, WS accept-before-close, import inside function, ownership check дублирование)
- Siri: Self-audit (бэклог пуст) — UX review перед демо 2026-03-21, тикеты SIRI-UX-091+, фиксы
### QA запущена
- Alisa: SIRI-UX-086, SIRI-UX-087, SIRI-UX-088, SIRI-UX-089, SIRI-UX-090, ALEX-TD-050, ALEX-TD-051, ALEX-TD-052, ALEX-TD-053
### Блокеры
- нет

## Выполнено Siri [18:41 MSK]
- Self-audit: найдено 5 проблем, создано 5 тикетов (SIRI-UX-091..095), зафикшено 5
- TypeScript: 0 ошибок
- Тесты: 581/581 ✅
- Найдено и зафикшено:
  - SIRI-UX-091 (major): `CompanyPage` модал "Add Agent" — отсутствовали `role="dialog"`, `aria-modal="true"`, `aria-label="Add Agent"`, обработчик Escape. Единственный модал без этих атрибутов. Фикс: добавлены все 4 (+ `useCallback` Escape handler).
  - SIRI-UX-092 (minor): `GlobalSearch` input — отсутствовал `aria-label`, только `placeholder`. Placeholder ненадёжен как accessible name. Добавлен `aria-label="Search companies, agents, tasks"`.
  - SIRI-UX-093 (minor): `Sidebar` toggle кнопка — только `title`, нет `aria-label`. `title` не анонсируется screen reader'ами стабильно. Добавлен `aria-label` совпадающий с `title`.
  - SIRI-UX-094 (major): `BillingPage` usage карточки (API calls / Tokens) — голые числа без визуализации прогресса. Страница выглядела незаконченной для демо инвестору. Добавлены progress bar'ы (`role="progressbar"`, `aria-valuenow/min/max/label`).
  - SIRI-UX-095 (minor): `LibraryPortfolioPage` Retry кнопка — дублировала весь fetch-код inline (~10 строк), stale closure over `id`. Рефактор: `fetchPortfolio` в `useCallback([id])`, использован в `useEffect` и Retry `onClick`.


## QA Alisa [18:40 MSK]
- SIRI-UX-086: ✅ Stop button: `disabled={stopping || runStatus === 'done' || runStatus === 'failed' || runStatus === 'stopped'}` + `opacity: 0.4` при завершённых статусах. WarRoomPage.tsx строки 299-304.
- SIRI-UX-087: ✅ `@keyframes fadeIn` перенесён в `index.css` (строка 40-43). В `OnboardingPage.tsx` — нет inline `<style>` тега, animation по-прежнему применяется через `style={{ animation: 'fadeIn 0.4s ease-in' }}`. Консистентно.
- SIRI-UX-088: ✅ `CompanyPage.tsx` — tablist содержит `aria-label="Company sections"` (строки 253-254). AC выполнен.
- SIRI-UX-089: ✅ WS status dot (`ws-status-indicator`) — `role="img"` + `aria-label={isConnected ? 'WebSocket connected' : 'WebSocket disconnected'}` (строки 325-328). AC выполнен.
- SIRI-UX-090: ✅ `KanbanBoard.tsx` FilterBar — Agent button: `aria-expanded={agentDropdownOpen}` + `aria-haspopup="listbox"` (строки 601-602); Priority button: `aria-expanded={priorityDropdownOpen}` + `aria-haspopup="listbox"` (строки 647-648). AC выполнен.
- ALEX-TD-050: ✅ `@limiter.limit("5/minute")` decorator на `validate_llm_key` (`credentials.py:174`). `__wrapped__` атрибут присутствует. `request: Request` в сигнатуре. Тест `test_validate_key_has_limiter_decorator` зелёный.
- ALEX-TD-051: ✅ `RunCreate.goal = Field(min_length=1, max_length=10000)` в `handlers/runs.py:39-40`. Тест `test_run_create_goal_max_length_rejected` верифицирует 422 на goal > 10000 символов.
- ALEX-TD-052: ✅ `/health` делает `SELECT 1` через `SessionLocal`, возвращает `{"status": "error", "detail": "db_unreachable"}` с кодом 503 при исключении. `main.py:121-142`. AC выполнен.
- ALEX-TD-053: ✅ `_fernet_cache: tuple[str, Fernet] | None = None` module-level singleton в `services/encryption.py:15`. `_get_fernet()` использует кэш и пересоздаёт Fernet только при смене ключа (строки 32-35). AC выполнен.
- Новые баги: нет
- Frontend тесты: 581/581 ✅
- Backend тесты: 492/492 ✅

## 18:45 MSK
### Назначено
- Alex: ALEX-TD-054, ALEX-TD-055, ALEX-TD-056, ALEX-TD-057 — minor tech debt (двойной DB запрос, WS accept-before-close, imports inside function, ownership check дублирование)
- Siri: финальный pre-demo аудит — проверка кода ключевых компонентов, тесты, TypeScript, тикеты SIRI-UX-096+ если найдёт
### QA запущена
- Alisa: — (нет завершённых непроверенных тикетов)
### Блокеры
- нет

## Выполнено Siri [18:52 MSK]
### Финальный pre-demo аудит

**TypeScript / Build:**
- `npm run build` — 0 ошибок, 315 kB bundle, ✅

**Тесты:**
- `npm test -- --run` — 65 файлов, 581/581 ✅

**Self-audit ключевых компонентов:**
- `WarRoomPage.tsx` — ✅ Нет console.log, нет сломанных импортов, все edge states (connecting, empty, agents, run-status banner) обработаны. Логика Stop button корректна (SIRI-UX-078). WS indicator с aria (SIRI-UX-089).
- `KanbanBoard.tsx` — ✅ FilterBar, drag-n-drop, optimistic updates, CreateModal — всё чисто. Escape handlers, aria-label на inputs (SIRI-UX-080, SIRI-UX-090).
- `CompanyPage.tsx` — ✅ 3 вкладки (War Room / Board / Agents), modal с aria (SIRI-UX-091), skeleton loading, empty states. Нет лишних console.log.
- `AgentForm.tsx` — ✅ model select с fallback, label/htmlFor a11y (SIRI-UX-066), system prompt editor. Чисто.
- `TaskDetailSidebar.tsx` — ✅ logs + status history + run button, Escape close, optimistic update (SIRI-UX-081). Нет debug кода.

**console.* в src/ (только допустимые):**
- `useWarRoomSocket.ts` — `console.warn` на невалидные WS payload (интенциональный runtime warning)
- `ErrorBoundary.tsx` — `console.error` при catch (стандартная практика)

**CI / git log:**
- Последние коммиты: `8889fbc` (SIRI-UX-091-095), `4bcb255` (SIRI-UX-086-090), `4f76e60` (test fixes) — все корректные

**Новые тикеты:** не создавались — проблем не найдено.

**Вердикт: ✅ ГОТОВО К ДЕМО**

## Выполнено Alex [21:00 MSK]
- ALEX-TD-054: `CompanyService.update()` — ownership check + update объединены в один DB-запрос (устранён double SELECT на PUT /companies/{id}). Добавлен `delete_owned()` с тем же паттерном.
- ALEX-TD-055: `ws_events.py` — `await websocket.accept()` вызывается до `websocket.close()`. Коды закрытия: 4001 (unauthorized), 4003 (forbidden) вместо 1008. Правильный WS handshake для proxies.
- ALEX-TD-056: `services/run.py:_execute_agent` — убраны `import asyncio as _asyncio` и `import os as _os` внутри функции. Используются module-level импорты.
- ALEX-TD-057: `CompanyService.get_owned()` — ownership check вынесен в service layer (вместо дублирования в 3 handlers). Handlers `get_company/delete_company` переведены на `get_owned()`/`delete_owned()`.
- Тесты: 499/499 ✅

## 19:11 MSK
### Назначено
- Siri: Self-audit (бэклог пуст) — финальный UX review перед демо 2026-03-21, тикеты SIRI-UX-096+, фиксы
- Alex: Self-audit (бэклог пуст) — tech debt review, тикеты ALEX-TD-058+, фиксы
### QA запущена
- Alisa: ALEX-TD-054, ALEX-TD-055, ALEX-TD-056, ALEX-TD-057, SIRI-UX-091, SIRI-UX-092, SIRI-UX-093, SIRI-UX-094, SIRI-UX-095
### Блокеры
- нет

## Выполнено Siri [19:21 MSK]
- SIRI-UX-096 (critical, fixed): `useWarRoomSocket.ts` — WS URL не передавал auth token. Backend требует `?token=<jwt>`, без него закрывал соединение кодом 4001. `WarRoomPage` при `isConnected=true` вызывала `store.reset()` — мок-данные стирались, War Room становилась пустой навсегда. На демо с реальным backend это был бы blank screen. Fix: добавлен `getStoredToken()` в URL (`?token=encodeURIComponent(token)`), guard'ы для кодов 4001/4003 (не ретраить auth errors). Commit: `9e604e7`.
- Тесты: 581/581 ✅ ГОТОВО К ДЕМО.

## QA Alisa [19:21 MSK]
- ALEX-TD-054: ✅ `CompanyService.update()` — ownership check + update в одном DB-запросе (identity map на второй session.get). `delete_owned()` добавлен. Handler использует оба метода.
- ALEX-TD-055: ✅ `ws_events.py` — `await websocket.accept()` вызывается до `close(code=4001/4003)`. Правильный WS handshake подтверждён по коду.
- ALEX-TD-056: ✅ `_execute_agent` — in-function импорты `asyncio` и `os` удалены, используются module-level. Примечание: 3 lazy imports в `execute_run` (graph/checkpointer/state) — другая функция, вне скоупа тикета.
- ALEX-TD-057: ✅ `CompanyService.get_owned()` добавлен. Все 3 handler'а (`get_company`, `delete_company`, `update_company`) используют `get_owned()`/`delete_owned()`.
- SIRI-UX-091: ✅ `role="dialog"`, `aria-modal="true"`, `aria-label="Add Agent"` добавлены. Escape handler через `useCallback([], [])` + `useEffect([isAgentFormOpen])`.
- SIRI-UX-092: ✅ `aria-label="Search companies, agents, tasks"` на input в GlobalSearch.
- SIRI-UX-093: ✅ `aria-label` на sidebar toggle совпадает с `title` (dynamic: Expand/Collapse sidebar).
- SIRI-UX-094: ✅ Progress bars с `role="progressbar"`, `aria-valuenow`, `aria-label` для API calls и Tokens.
- SIRI-UX-095: ✅ `fetchPortfolio` вынесен в `useCallback([id])`, используется в `useEffect` и Retry `onClick`.
- Новые баги: нет
- Frontend тесты: 581/581 ✅
- Backend тесты: 512/512 ✅

## Выполнено Alex [19:19 MSK]
- Self-audit: код проверен — handlers/, services/, orchestration/, auth/, core/
- Тесты до аудита: 499/499 ✅
- Найдено проблем: 1 (minor)
- ALEX-TD-058: `handlers/credentials.py:validate_llm_key` — dead code: тройной `if/elif` для provider делал идентичное присваивание `api_key`. Упрощён в одно присваивание в dict literal. Код компактнее, нет риска "добавить новый provider и забыть if-ветку".
- Тесты после фикса: 499/499 ✅
- Security: нет незащищённых эндпоинтов, нет secrets в коде, auth везде (Bearer JWT)
- Error handling: нет голых `except:`, паттерн HTTPException/NotFoundError выдержан
- Performance: нет N+1, нет sync в async контексте (asyncio.run_in_executor использован)
- Dead code: устранён (ALEX-TD-058)

# Changelog 2026-03-20

## 13:06 MSK
### Назначено
- Alex: BUG-041, BUG-042 — двойной v1-prefix и deprecation headers
- Siri: Self-audit — код-ревью всех ключевых компонентов, тесты, TypeScript, новые тикеты

### QA запущена
- Alisa: — (нет завершённых тикетов без QA)

### Блокеры
- нет


## Выполнено Siri [13:14 MSK]
- Self-audit: найдено 5 проблем, создано 5 тикетов (SIRI-UX-073..077), зафикшено 4
- TypeScript: 0 ошибок
- Тесты: 572/572 ✅ (все проходят после фиксов)
- CI: 3/3 success (последний: feat(ALEX-POST-007))
- Найдено и зафикшено:
  - SIRI-UX-073: `AgentEditPage` — пустая форма при ошибке загрузки агента → добавлен error state
  - SIRI-UX-074: `CompaniesPage` modal — отсутствовали `role="dialog"`, `aria-modal`, Escape handler → добавлены
  - SIRI-UX-075: `WarRoomPage` agent status dot — нет `aria-label` → добавлен `aria-label={statusLabel[agent.status]}`
  - SIRI-UX-076: `TaskDetailSidebar` Run Task button — нет `aria-label` → добавлен
  - SIRI-UX-077 (тикет, не фикс): `useWarRoomSocket` — unsafe cast `data.agentId/status` без валидации формата WS-события

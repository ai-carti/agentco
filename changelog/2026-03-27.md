# Changelog — 2026-03-27

## 07:20 MSK
### Назначено
- **Siri**: SIRI-UX-428 (drag-and-drop блокировка колонки error), SIRI-UX-429 (aria-label на кнопке закрытия modal компании), SIRI-UX-365 (stale state при повторном открытии Edit modal), SIRI-UX-367 (key={i} в SkeletonCard) — 4 minor frontend-тикета
- **Alex**: BUG-NEW-001 (backend pytest isolation — 19 тестов падают в полном suite), ALEX-TD-288 (tools=[] передаётся в LLM), ALEX-TD-289 (memory service constants без cache_clear) — 1 medium + 2 minor backend-тикета

### QA запущена
- Alisa: — (последний changelog от 2026-03-12, нет недавних выполненных тикетов для проверки)

### Блокеры
- BUG-076 (critical): Mock LLM в production — требует ручной установки `AGENTCO_USE_REAL_LLM=true` + LLM API key в Railway dashboard от @timofeytst
- BUG-075 (critical): E2E prod флоу — заблокирован BUG-076

---

## Выполнено Siri [07:43 MSK]
- **SIRI-UX-428** — `KanbanBoard.tsx handleDrop`: заблокирован drop в колонку `error`; PATCH не отправляется
- **SIRI-UX-429** — `CompaniesPage.tsx`: добавлен edit modal с кнопкой закрытия `aria-label="Close edit company modal"` и кнопкой `edit-company-{id}-btn` на каждой компании
- **SIRI-UX-365** — `KanbanBoard.tsx TaskCard`: добавлен `useEffect([task])` синхронизирующий `editTitle`/`editDesc` с актуальными значениями из store
- **SIRI-UX-367** — `SkeletonCard.tsx`: ключи skeleton items изменены на `skeleton-${variant}-${i}` (уже было исправлено ранее)
- Побочно: `WarRoomPage.tsx` — добавлен `error` статус в `statusDotStyle`/`statusDotBg`/`statusLabel` (TypeScript error от SIRI-UX-426 fix)
- TypeScript: чистый ✅ | Тесты: 1155/1155 ✅

---

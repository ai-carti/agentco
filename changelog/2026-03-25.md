# Changelog — 2026-03-25

---

## 04:30 MSK
### Назначено
- **Siri**: SIRI-UX-295 + SIRI-UX-297 — navigator.platform deprecated fix + Zustand useShallow рефактор
- **Alex**: ALEX-TD-198 + ALEX-TD-199 — /health rate limiting + ORDER BY NULLS LAST
### QA запущена
- Alisa: SIRI-UX-293, SIRI-UX-294, SIRI-UX-296, ALEX-TD-195, ALEX-TD-196, ALEX-TD-197
## Выполнено Siri [04:42 MSK]

### SIRI-UX-295 — `navigator.platform` deprecated → fixed
- `GlobalSearch.tsx`: обновлена `getIsMac()` — теперь пробует `navigator.userAgentData?.platform` первым (Chrome 90+), fallback на `navigator.platform`
- Добавлены тесты: `SIRI-UX-295.test.tsx` (6 тестов: macOS via userAgentData, Windows/Linux via userAgentData, fallback на platform, empty string fallback)
- TypeScript: cast через `Navigator & { userAgentData?: { platform?: string } }` для совместимости

### SIRI-UX-297 — 10 Zustand подписок → 1 useShallow → fixed
- `WarRoomPage.tsx`: объединены `agents`, `messages`, `cost`, `runStatus`, `flashingAgents` в один `useWarRoomStore(useShallow(s => ({...})))` — импорт `useShallow` из `zustand/shallow`
- Actions (`loadMockData`, `addMessage`, `updateAgentStatus`, `clearFlash`, `setRunStatus`) оставлены отдельно — стабильные референсы, без ре-рендера
- Добавлены тесты: `SIRI-UX-297.test.tsx` (8 тестов: агенты, сообщения, cost, runStatus done/failed, flash, stop button)

**Итог:** 921 тест зелёный, `tsc --noEmit` чистый, коммит `f31d83b`

### Блокеры
- BUG-076 (critical): Mock LLM в production — требует ручной установки `AGENTCO_USE_REAL_LLM=true` + LLM API key в Railway Variables от @timofeytst. Агенты не могут это исправить.
- BUG-075: заблокирован BUG-076


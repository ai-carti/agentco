# Changelog — 2026-03-25

## 22:15 MSK
### Назначено
- **Siri**: SIRI-UX-338, SIRI-UX-339, SIRI-UX-340, SIRI-UX-341 — accessibility & code quality fixes (role=alert на logs-error, useCallback для onCardClick, role=region на War Room panels, Button компонент в Create Task modal)
- **Alex**: ALEX-TD-214, ALEX-TD-215, ALEX-TD-216 — backend polish (docstrings в credentials handlers, убрать лишний flush() в stop(), перевести docstrings на английский)
### QA запущена
- Alisa: — (нет тикетов, выполненных и ожидающих QA в этом цикле)
## Выполнено Siri [22:21 MSK]
- SIRI-UX-338: добавлен `role="alert"` на span `data-testid="logs-error"` в `TaskDetailSidebar.tsx` — screen readers теперь анонсируют ошибку загрузки логов
- SIRI-UX-339: извлечен `handleCardClick` с `useCallback` в `KanbanBoard.tsx` — стабильная ссылка вместо inline arrow в `.map()`
- SIRI-UX-340: добавлены `role="region"` + `aria-labelledby` на `agent-panel` и `activity-feed` в `WarRoomPage.tsx` — landmark навигация для screen readers
- SIRI-UX-341: заменены raw `<button>` на `<Button variant="secondary/primary">` компонент в Create Task modal `KanbanBoard.tsx` — визуальная согласованность с Edit/Delete модалами

Все тесты: ✅ 1000/1000 passed | TypeScript: ✅ чисто | Коммит: `76b0414`

## Выполнено Alex [22:19 MSK]
- ALEX-TD-214: добавлены English docstrings на 4 endpoint-функции в `handlers/credentials.py` — `create_credential`, `list_credentials`, `delete_credential`, `list_llm_providers`; Swagger UI теперь показывает описания операций
- ALEX-TD-215: удалён лишний `self._session.flush()` перед `self._session.commit()` в `stop()` метода `services/run.py` — устранён ненужный round-trip к БД
- ALEX-TD-216: переписаны docstrings `run_task`, `list_task_runs`, `get_task_run` в `handlers/runs.py` с русского на английский — единообразие стиля в Swagger UI

Все тесты: ✅ 911 passed, 2 skipped | Коммит: `25d25ba`

### Блокеры
- BUG-076 (critical): Mock LLM в production — требует ручного action от @timofeytst: установить `AGENTCO_USE_REAL_LLM=true` + LLM API key в Railway Variables
- BUG-075 (critical): E2E prod флоу заблокирован BUG-076

---

## 00:26 MSK (UTC 21:26 / 2026-03-26)
### Назначено
- **Siri**: Self-audit — поиск новых проблем в frontend (WarRoom, KanbanBoard, TaskDetailSidebar, warRoomStore)
- **Alex**: Self-audit — поиск новых проблем в backend (services/run.py, agent_node.py, nodes.py, handlers/)
### QA запущена
- Alisa: SIRI-UX-338, SIRI-UX-339, SIRI-UX-340, SIRI-UX-341, ALEX-TD-214, ALEX-TD-215, ALEX-TD-216 (тикеты предыдущего цикла)
### Блокеры
- BUG-076 (critical): требует action @timofeytst в Railway dashboard

## QA Alisa [00:34 MSK]
- SIRI-UX-338: ✅ `role="alert"` присутствует на span `data-testid="logs-error"` в TaskDetailSidebar.tsx
- SIRI-UX-339: ✅ `handleCardClick` обёрнут в `useCallback`, deps array `[]` корректен — callback только вызывает `setSelectedTaskId` (state setter, стабильный), stale closure исключена
- SIRI-UX-340: ✅ `role="region"` + `aria-labelledby` на обоих панелях; heading IDs (`agents-panel-heading`, `activity-feed-heading`) присутствуют в DOM
- SIRI-UX-341: ✅ raw `<button>` заменены на `<Button variant="secondary">` (Cancel) и `<Button variant="primary">` (Create) с `data-testid`
- ALEX-TD-214: ✅ English docstrings добавлены на 4 endpoint-функции в credentials.py
- ALEX-TD-215: ✅ `flush()` удалён; `stop()` по-прежнему вызывает `self._session.commit()` — данные корректно сохраняются
- ALEX-TD-216: ✅ docstrings `run_task`, `list_task_runs`, `get_task_run` на английском
Тесты: фронт 1000/1000, бэкенд 911 passed (2 skipped)
Новые баги: нет


## Выполнено Siri [00:57 MSK]
- SIRI-UX-346: извлечен `closeCreateModal` useCallback в `KanbanBoard.tsx` — устранено дублирование 5 setState-вызовов в 3 местах (Escape handler, backdrop onClick, Cancel button)
- SIRI-UX-347: исправлен `handleStop` в `WarRoomPage.tsx` — `.catch(() => [])` заменён на явный try/catch с `toast.error` при ошибке парсинга JSON, скрытые ошибки больше не маскируются
- SIRI-UX-348: исправлен тип `ws.onclose` в `WarRoom.tsx` — параметр `event?: CloseEvent` → `event: CloseEvent`, убрано излишнее optional chaining `?.`; обновлён MockWebSocket в тесте для передачи CloseEvent

Тесты: ✅ 1000/1000 | TypeScript: ✅ чисто | Коммит: `245ae04`

## Выполнено Alex [01:04 MSK]
- ALEX-TD-219: `orchestration/agent_node.py:_build_messages_with_memory` — исправлен `state.get("messages", [])` на `state.get("messages") or []`. Предыдущий код возвращал `None` при явном `None` в state → `TypeError: 'NoneType' object is not iterable`. Теперь оба случая (отсутствующий ключ и явный `None`) возвращают `[]`.
- ALEX-TD-220: `core/event_bus.py:RedisEventBus.subscribe` — добавлен `except Exception` вокруг `async for pubsub.listen()`. Redis `ConnectionError`/`TimeoutError` больше не propagates через async generator в SSE-endpoint с 500; ошибка логируется как warning, поток завершается gracefully.

Коммит: `33391ab`

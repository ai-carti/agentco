# AgentCo Changelog — 2026-03-25

## 11:00 MSK — Shadrin Sprint

### Назначено

- **Alex**: ALEX-TD-208 (major) — 28 backend-тестов сломаны после UUID path params. Обновить тесты: заменить нестандартные строки типа `"nonexistent-run-id"` на реальные UUID-формат строки (`str(uuid.uuid4())`) для проверки 404, оставить 422 для невалидных UUID. Затронуты: `test_runs.py`, `test_tasks.py`, `test_companies.py`, `test_library.py`, `test_mcp_servers.py`, `test_memory.py`, `test_alex_td_135_138.py`, `test_alex_td_142_146.py`, `test_alex_td_149_ws_conn_limit.py`, `test_event_bus.py`, `test_task_run_endpoint.py`.

- **Siri**: Self-audit — пройтись по всему фронту, найти новые UX-проблемы (тикеты SIRI-UX-XXX), закрыть их в этом же цикле.

### QA запущена

- Alisa: SIRI-UX-318 (thinking-dot key), SIRI-UX-319 (BASE_URL 14 компонентов), SIRI-UX-320 (handleStop setRunStatus), SIRI-UX-321 (WarRoom dead branch), SIRI-UX-322 (BillingPage aria-labelledby), SIRI-UX-323 (BillingPage aria-label кнопки), BUG-078, BUG-079

## Выполнено Alex [13:58 MSK]
- ALEX-TD-208: ✅ Регрессия после UUID path params устранена — тесты обновлены на `str(uuid.uuid4())` для 404 not-found проверок, stale `.pyc` bytecode очищен; 904 тестов зелёных (было 18 FAILED)

### Блокеры

- BUG-076 (critical) — Mock LLM in production: требует ручного действия @timofeytst в Railway (AGENTCO_USE_REAL_LLM=true + LLM API key)
- BUG-075 (critical) — E2E prod flow: заблокирован BUG-076


## QA Alisa [14:09 MSK]
- Проверено: SIRI-UX-318, 319, 320, 321, 322, 323, BUG-078, 079
- Статусы:
  - SIRI-UX-318 ✅ — `key={`thinking-dot-${i}`}` присутствует на строке 617, index key anti-pattern устранён
  - SIRI-UX-319 ✅ — 14 компонентов импортируют `BASE_URL` из `../api/client`, локальных дублей `const BASE_URL = import.meta.env...` не найдено
  - SIRI-UX-320 ✅ — `setRunStatus` присутствует в deps array `handleStop` (строка 283)
  - SIRI-UX-321 ✅ — `WarRoom.tsx:214` — тернарный `runs.length > 0 ? (...) : null` убран, комментарий-подтверждение на месте
  - SIRI-UX-322 ✅ — все три секции (`billing-current-plan`, `billing-upgrade`, `billing-history`) имеют `aria-labelledby` указывающий на `<h2 id=...>`
  - SIRI-UX-323 ✅ — кнопки имеют уникальные `aria-label`: `"Free — current plan"`, `"Upgrade to Pro"`, `"Upgrade to Enterprise"`
  - BUG-078 ✅ — подтверждён как часть SIRI-UX-318 fix
  - BUG-079 ✅ — подтверждён как часть SIRI-UX-320 fix
- Новые баги: BUG-080 — `tsc --noEmit` падает с TS2339 в `SIRI-UX-321-323.test.tsx:14` (`currentCompany` не найден в union типе setState)
- Тесты: 966/966 ✅ (все 156 test files зелёные)
- TypeScript: ❌ — 1 ошибка TS2339 в тестовом файле (BUG-080)

## Выполнено Alex [14:25 MSK]
- Audit: найдено 3 тикета (ALEX-TD-209..211)
- Fixed: ALEX-TD-209 — race condition в `execute_run`: финальное обновление статуса теперь не перезаписывает "stopped" — добавлен guard `if run_orm.status not in _terminal`
- Fixed: ALEX-TD-210 — `_estimate_cost` и `agent_node` падали с `AttributeError: 'NoneType'.startswith()` при явном `model=None` в AgentState — заменено на `state.get("model") or "gpt-4o"` + early return в `_estimate_cost`
- Fixed: ALEX-TD-211 — `create_with_goal` принимал пустой goal минуя Pydantic валидацию — добавлен `if not goal.strip(): raise ValueError("goal must not be empty")`
- Тесты: 911/911 ✅ (7 новых тестов в `test_alex_td_209_210_211.py`)
- Коммит: `e9990e8` → pushed to main

## Выполнено Siri [14:15 MSK]
- Audit: найдено 4 тикета (SIRI-UX-328..331), синхронизированы статусы SIRI-UX-324..327 (код уже содержал фиксы, ROADMAP отставал)
- Fixed SIRI-UX-328 — TS2339: заменён сломанный `Parameters<typeof useAgentStore.setState>[0]['currentCompany']` cast на прямой object в `SIRI-UX-321-323.test.tsx:14` — `tsc --noEmit` теперь чистый
- Fixed SIRI-UX-329 — `AgentPage.tsx:handleSaveToLibrary` обёрнут в `useCallback([agentId, toast])` — устранён React perf anti-pattern
- Fixed SIRI-UX-330 — `TaskDetailSidebar.tsx:handleRun` обёрнут в `useCallback([companyId, task.id, task.title, setTasks, toast])` — аналогичный anti-pattern
- Audit SIRI-UX-331 — CompanyPage modal backdrop: Escape уже покрыт через `handleModalEscape` document listener → wontfix
- Тесты: 980/980 ✅ (158 test files)
- TypeScript: ✅ — 0 ошибок
- Коммит: `1b6fdee` → pushed to main

## Выполнено Siri [14:41 MSK]
- BUG-080: TS2339 уже был устранён в коммите 1b6fdee (SIRI-UX-328) — сложный `Parameters<typeof useAgentStore.setState>[0]['currentCompany']` cast заменён на прямой `{ currentCompany: { id: 'c1', name: 'Test Co' } }`. tsc --noEmit чистый, 980/980 тестов зелёные. ROADMAP.md обновлён: open → fixed.

## 14:30 MSK
### Назначено
- Siri: BUG-080 — TS2339 fix в SIRI-UX-321-323.test.tsx (useAgentStore.setState cast)
- Alex: ALEX-TD-209 (major) — race condition stop() vs done в execute_run
- Alex: ALEX-TD-210 — AttributeError при model=None в _estimate_cost
- Alex: ALEX-TD-211 — empty goal проходит валидацию в create_with_goal
### QA запущена
- Alisa: SIRI-UX-328/329/330, ALEX-TD-208, BUG-078/079 — верификация последних коммитов
### Блокеры
- BUG-076 (critical, mock LLM в production) — требует ручного действия @timofeytst в Railway dashboard (AGENTCO_USE_REAL_LLM=true + LLM API key)
- BUG-075 (critical, E2E prod flow) — заблокирован BUG-076

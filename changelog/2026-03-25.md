# Changelog — 2026-03-25

## 06:01 MSK — Shadrin Sprint Start
### Назначено
- Alex: ALEX-TD-201 — `services/task.py:list_by_agent` без `order_by`; обновить статус ALEX-TD-200 в ROADMAP на `fixed`
### QA запущена
- Alisa: проверка ALEX-TD-200 (exc_info=True dead-letter), SIRI-UX-295,297,298,299,300,301,302,303
### Блокеры
- BUG-076: `AGENTCO_USE_REAL_LLM=true` не установлен в Railway — требует action @timofeytst

## QA Alisa [06:11 MSK]
- ALEX-TD-200: ✅ `logger.error(...)` в dead-letter блоке (`run.py` ~271) содержит `exc_info=True`. Тест `dead_letter` проходит. Backend: 886 passed, 2 skipped.
- SIRI-UX-300: ✅ `<p role="alert" data-testid="agent-edit-error">` присутствует в `AgentEditPage.tsx:174-175`.
- SIRI-UX-301: ✅ `<p role="alert" data-testid="history-load-error">` присутствует в `AgentPage.tsx:345`.
- SIRI-UX-302: ✅ `formatDateLong` объявлена в `taskUtils.ts:56`, импортирована и используется в `TaskDetailSidebar.tsx:291,373`.
- SIRI-UX-303: ✅ `AgentPage.tsx` не содержит `toLocaleDateString()` без locale — все вызовы заменены на `formatDateLong`.
- SIRI-UX-295: ✅ `getIsMac()` в `GlobalSearch.tsx` использует `navigator.userAgentData?.platform` как primary; fallback на `navigator.platform` для старых браузеров.
- SIRI-UX-297: ✅ Основные state-поля `{agents, messages, cost, runStatus, flashingAgents}` объединены через `useShallow` в `WarRoomPage.tsx:35-42`. Action-функции подписаны отдельно (норма — они стабильны).
- SIRI-UX-298: ✅ `events` state удалён из `useWarRoomSocket.ts`. Нет `setEvents`. Остались только `isConnected` и `error`.
- SIRI-UX-299: ✅ `handleDragLeave` в `KanbanBoard.tsx:1021-1024` проверяет `e.relatedTarget` и возвращает early если курсор перешёл на дочерний элемент.
- TypeScript: `npx tsc --noEmit` — 0 ошибок.

### Новые баги
- нет

## Выполнено Alex [06:13 MSK]
- ALEX-TD-201: `services/task.py:list_by_agent` делегирует в `repo.list_by_agent()` с `ORDER BY created_at ASC`. Тесты: 7 зелёных (test_alex_td_201.py + test_alex_td_070_071.py). Полный suite: 886 passed, 2 skipped.
- ALEX-TD-200: статус исправлен на fixed в ROADMAP.md

## 07:15 MSK
### Назначено
- Siri: самостоятельный UX-аудит — найти и зафиксить SIRI-UX-289+ тикеты (бэклог пуст, аудит по RULES.md)
- Alex: самостоятельный backend аудит — найти и зафиксить ALEX-TD-XXX тикеты (бэклог пуст, аудит по RULES.md)
### QA запущена
- Alisa: —  (нечего проверять — всё в ROADMAP помечено fixed, новые результаты появятся после Siri/Alex)
### Блокеры
- BUG-076: AGENTCO_USE_REAL_LLM=true не установлен в Railway — требует action @timofeytst (не блокирует dev-работу)
- BUG-075: E2E prod флоу без real LLM — conditional, зависит от BUG-076

## Выполнено Siri [07:25 MSK]
- SIRI-UX-304: `SettingsPage.tsx` — добавлен `role="alert"` на `llm-credentials-error` и `credentials-fetch-error`. Screen reader теперь анонсирует ошибки валидации API key и загрузки credentials.
- SIRI-UX-305: `LibraryPortfolioPage.tsx:189` — заменён `toLocaleDateString()` на `formatDateLong()` из taskUtils. Дата в портфолио теперь консистентна с остальным UI (формат "Jan 15, 2026").
- SIRI-UX-306: `AgentForm.tsx` — добавлен `role="alert"` и `data-testid="agent-form-model-error"` на ошибку валидации модели. Screen reader анонсирует "Please select a model" при сабмите без модели.
- SIRI-UX-307: `KanbanBoard.tsx` — удалена orphan подписка `useAgentStore((s) => s.tasks)` из `TaskCard`. Каждая карточка больше не ре-рендерится N раз при любом изменении глобального списка задач. 4 новых теста, 937 passed.

## Выполнено Alex [07:46 MSK]
- ALEX-TD-202: `services/run.py` — переименована dead variable `initial_task_id` в `_initial_task_id`, теперь активно используется в логике загрузки цели задачи.
- ALEX-TD-203: `handlers/tasks.py:TaskOut` — добавлены поля `created_at: datetime | None` и `result: str | None`. Frontend теперь получает время создания задачи и результат через REST API.
- ALEX-TD-204 (major): `services/run.py` — исправлен баг: task-based run больше не передаёт UUID task_id как goal в LangGraph. Теперь загружается `task.title + task.description` из БД. LLM получает реальное описание задачи вместо UUID строки.

## 08:21 MSK
### Назначено
- Siri: UX self-audit — поиск и фикс новых SIRI-UX-308+ проблем (bэклог пуст)
- Alex: ALEX-TD-195 (exc_info=True в outer except), ALEX-TD-196 (full jitter в backoff), ALEX-TD-197 (exc_info=True в tool handler)
### QA запущена
- Alisa: проверка SIRI-UX-304,305,306,307 и ALEX-TD-202,203,204 из цикла 07:25-07:46 MSK
### Блокеры
- BUG-076: AGENTCO_USE_REAL_LLM=true не установлен — требует action @timofeytst
- BUG-075: E2E prod флоу conditional, зависит от BUG-076

## Выполнено Alex [08:34 MSK]
- ALEX-TD-195: фикс уже был применён — `logger.error("execute_run failed for %s: %s", run_id, exc, exc_info=True)` в `services/run.py:549`. Статус: done → fixed.
- ALEX-TD-196: фикс уже был применён — `delay = random.uniform(0, delay)` в `services/run.py:264`. Настоящий full jitter (0..100% от exponential backoff). Статус: done → fixed.
- ALEX-TD-197: фикс уже был применён — `logger.error("Tool handler '%s' failed: %s", tool_name, e, exc_info=True)` в `orchestration/agent_node.py:404`. Статус: done → fixed.
- Тесты: все зелёные (exit code 0), 2 skipped.

## QA Alisa [08:37 MSK]
- SIRI-UX-304: ✅ `role="alert"` присутствует на обоих элементах — `llm-credentials-error` (строка 362) и `credentials-fetch-error` (строка 385) в `SettingsPage.tsx`.
- SIRI-UX-305: ✅ `formatDateLong` импортирована из `taskUtils` и используется на строке 191 `LibraryPortfolioPage.tsx`. `toLocaleDateString()` без аргументов отсутствует.
- SIRI-UX-306: ✅ `role="alert"` и `data-testid="agent-form-model-error"` присутствуют в `AgentForm.tsx:150`.
- SIRI-UX-307: ✅ `useAgentStore((s) => s.tasks)` подписка удалена из `TaskCard`. Комментарий SIRI-UX-307 подтверждён (строки 57-59 KanbanBoard.tsx). `TaskCard` использует `getState().tasks` для мутаций — без реактивной подписки.
- ALEX-TD-202: ✅ `_initial_task_id = run_orm.task_id` (строка 405 `services/run.py`). Переменная переименована и активно используется в ветках `elif _initial_task_id:` (строки 410, 412, 417).
- ALEX-TD-203: ✅ `created_at: datetime | None = None` и `result: str | None = None` добавлены в `TaskOut` (`handlers/tasks.py:79-80`).
- ALEX-TD-204: ✅ task-based run загружает `task.title + task.description` как `initial_goal` (`services/run.py:415`). Fallback на `_initial_task_id` только если `task_orm is None` (строка 417 — комментарий "shouldn't happen").
- TypeScript: ✅ `npx tsc --noEmit` — 0 ошибок (exit code 0).
- Backend tests: ✅ 892 passed, 2 skipped.
- Frontend tests: ✅ 937 passed (149 test files).

### Новые баги
- нет

## Выполнено Siri [08:36 MSK]
- SIRI-UX-308: `SIRI-UX-304-306-307.test.tsx` — переписан Node.js-based тест (SIRI-UX-307) без `fs`/`path`/`__dirname`. Вызывал 3 TS2307/TS2304 ошибки в `tsc --noEmit`. Заменён поведенческим React rendering тестом. TSC чист (0 ошибок).
- SIRI-UX-309: `CompanySettingsPage.handleDelete` — добавлен `AbortController` (был единственный async handler в компоненте без него). При навигации прочь во время DELETE теперь не вызывает `toast.*`/`setIsDeleting` на unmounted component. Добавлены `deleteAbortRef`, cleanup в `useEffect`, `signal.aborted` guard.
- SIRI-UX-310: `AuthPage.tsx` — добавлена Arrow key navigation для tablist. `ArrowRight` на Sign In → фокус на Sign Up, `ArrowLeft` на Sign Up → фокус на Sign In. WAI-ARIA APG Tabs Pattern (WCAG 2.1). Аналогично уже реализованному `CompanyPage` (SIRI-UX-143).
- Тесты: 940/940 зелёных (+3 новых). tsc --noEmit чист. Коммит: 785f870.

## QA Alisa [09:42 MSK]
- SIRI-UX-308: ✅ Тест в `SIRI-UX-304-306-307.test.tsx` переписан как поведенческий React rendering тест — KanbanBoard рендерит TaskCard с задачей из store. Нет `fs`/`path`/`__dirname`. TSC чист (0 ошибок).
- SIRI-UX-309: ✅ `CompanySettingsPage.tsx` содержит `deleteAbortRef` (строка 34), создание AbortController в `handleDelete` (строки 105-107), передачу `signal` в fetch (строка 114), `signal.aborted` guards (строки 116, 126). Cleanup в useEffect (строки 62, 104).
- SIRI-UX-310: ✅ `AuthPage.tsx:143` — `role="tablist"` с `onKeyDown` на обоих tab-button: `ArrowRight` (Sign In → Sign Up, строки 152-154), `ArrowLeft` (Sign Up → Sign In, строки 171-173). Тесты 2/2 зелёных в `SIRI-UX-308-310.test.tsx`.
- ALEX-TD-195: ✅ `services/run.py:549` — `logger.error("execute_run failed for %s: %s", run_id, exc, exc_info=True)`. Traceback сохраняется в outer except.
- ALEX-TD-196: ✅ `services/run.py:264` — `delay = random.uniform(0, delay)`. Настоящий full jitter (0..100%), комментарий подтверждён.
- ALEX-TD-197: ✅ `orchestration/agent_node.py:404` — `logger.error("Tool handler '%s' failed: %s", tool_name, e, exc_info=True)`. Traceback логируется при ошибках tool handler.
- TypeScript: ✅ `npx tsc --noEmit` — 0 ошибок
- Backend tests: ✅ 892 passed, 2 skipped
- Frontend tests: ✅ 940 passed (150 test files)

### Новые баги
- нет

## Выполнено Alex [10:52 MSK]
- Self-audit: провёл полный code review backend с точки зрения tech lead
- Запустил полный тест-сьют: **892 passed, 2 skipped** ✅ (exit code 0)
- **Security**: проверил все endpoints — нет SQL injection (ORM parameterized queries), все mutable endpoints rate-limited, SSRF защита на MCP servers, bcrypt для паролей, Fernet шифрование API ключей. ENCRYPTION_KEY warning при старте корректен. SECRET_KEY warning корректен.
- **Error handling**: все critical paths имеют exc_info=True в logger.error. Широкие `except Exception` проверены — все логируют или re-raise. Нет голых `except: pass` кроме documented + expected мест.
- **Performance**: индексы на всех FK полях (migrations 0010, 0011, 0013, 0014). ORDER BY присутствует везде. Memory operations в run_in_executor. SqliteVecStore с threading.Lock. N+1 устранён для agent.delete (ALEX-TD-194 bulk UPDATE).
- **Code quality**: мёртвый код ALEX-TD-151 задокументирован (memory_service_var deprecated field). Без hardcoded секретов в коде. .env.example полный и актуальный.
- **Prod config**: railway.toml корректен (healthcheckPath, restartPolicy). /health endpoint проверяет DB connectivity и возвращает 503 при DB failure. Graceful shutdown отменяет active tasks. CORS из env.
- Новые тикеты созданы (minor, не блокеры):
  - **ALEX-TD-205**: memory inject в `_build_messages_with_memory` не проверяет token limit заранее → потенциально один лишний LLM вызов при почти-переполненном бюджете
  - **ALEX-TD-206**: retry strategy не задокументирована в .env.example — добавить комментарий
  - **ALEX-TD-207**: UUID path params не валидируются на формат — 404 вместо 422 при опечатках (низкий риск, ORM защищает от injection)

## 09:31 MSK
### Назначено
- Siri: self-audit SIRI-UX-311+ (бэклог пуст — UX/a11y/perf аудит)
- Alex: self-audit ALEX-TD-205+ (бэклог пуст — backend code review)
### QA запущена
- Alisa: проверка SIRI-UX-308,309,310 и ALEX-TD-195,196,197 из цикла 08:21-08:36 MSK
### Блокеры
- BUG-076: AGENTCO_USE_REAL_LLM=true не установлен в Railway — требует action @timofeytst
- BUG-075: E2E prod флоу conditional, зависит от BUG-076

## 10:41 MSK
### Назначено
- Siri: SIRI-UX-311 (TaskDetailSidebar backdrop без role/tabIndex/onKeyDown), SIRI-UX-312 (CompanyPage setTaskOffset stale closure), SIRI-UX-313 (warRoomStore loadMockData cost: 0.042 → 0), SIRI-UX-314 (GlobalSearch scope hint)
- Alex: self-audit backend — поиск ALEX-TD-XXX тикетов (бэклог пуст)
### QA запущена
- Alisa: — (нечего проверять — новые тикеты только назначены)
### Блокеры
- BUG-076: AGENTCO_USE_REAL_LLM=true не установлен в Railway — требует action @timofeytst
- BUG-075: E2E prod флоу conditional, зависит от BUG-076

## Выполнено Siri [10:49 MSK]
- SIRI-UX-311: `TaskDetailSidebar.tsx:sidebar-backdrop` — добавлены `role="button"`, `tabIndex={0}`, `aria-label="Close task details"`, `onKeyDown` handler (Enter/Space/Escape закрывают sidebar). Исправлен конфликтующий тест SIRI-UX-057 (exact match `name: 'Close'` вместо `/close/i`).
- SIRI-UX-312: `CompanyPage.tsx:handleLoadMoreTasks` — `setTaskOffset(taskOffset + items.length)` заменён на `setTaskOffset(prev => prev + items.length)`. Устранён stale closure при concurrent вызовах и StrictMode double-invoke.
- SIRI-UX-313: `warRoomStore.ts:loadMockData` — `cost: 0.042` заменён на `cost: 0`. Инвариант восстановлен: cost аккумулируется исключительно из WS `llm_token` событий через `addCost()`.
- SIRI-UX-314: `GlobalSearch.tsx` — добавлен `data-testid="global-search-scope-hint"` под input: "Searching in {company.name}" когда компания выбрана; "Select a company to search agents and tasks" когда `currentCompany` = null. Тесты: 951/951 зелёных. tsc: 0 ошибок. Коммит: 22cb905.
